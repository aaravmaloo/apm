{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"APM Documentation","text":"<p>APM is a Go-based password manager with two binaries:</p> <ul> <li><code>pm</code> for personal vaults</li> <li><code>pm-team</code> for shared organizational vaults</li> </ul> <p>The source tree implements more than a basic password CLI. It includes sessions, recovery, cloud sync, plugins, MCP access, Windows autofill, shell injection, and a TUI alongside the core encrypted vault.</p>"},{"location":"#what-apm-currently-does","title":"What APM currently does","text":"<ul> <li>Stores 25 personal secret types in one encrypted vault.</li> <li>Uses explicit unlock sessions with expiry and inactivity controls.</li> <li>Supports delegated ephemeral sessions for automation and AI-agent access.</li> <li>Syncs encrypted vault blobs to Google Drive, GitHub, and Dropbox.</li> <li>Exposes a built-in MCP server with scoped tokens and mutation previews.</li> <li>Runs a manifest-based plugin system with hooks and runtime-added commands.</li> <li>Offers Windows autofill and autocomplete support plus shell-side secret injection.</li> <li>Provides a separate team edition with departments, approvals, roles, and shared entries.</li> </ul>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>go build -o pm .\npm setup\npm unlock\npm add\npm get github\npm lock\n</code></pre> <p>Team edition:</p> <pre><code>cd team\ngo build -o pm-team .\n</code></pre>"},{"location":"#documentation-map","title":"Documentation map","text":""},{"location":"#getting-started","title":"Getting Started","text":"<ul> <li>Installation</li> <li>First steps</li> <li>Features</li> <li>Getting help</li> </ul>"},{"location":"#guides","title":"Guides","text":"<ul> <li>Vault management</li> <li>Cloud synchronization</li> <li>Using <code>.apmignore</code></li> <li>Injecting secrets into your shell</li> <li>Generating TOTP codes</li> <li>Managing sessions</li> <li>Using plugins</li> <li>MCP integration</li> <li>Team edition</li> <li>Import and export</li> <li>Windows autofill</li> </ul>"},{"location":"#concepts","title":"Concepts","text":"<ul> <li>Architecture</li> <li>Encryption</li> <li>Vault format</li> <li>Secret types</li> <li>Security profiles</li> <li>Policy engine</li> <li>Sessions</li> <li>Cloud sync</li> <li>Plugins</li> <li>MCP</li> <li>Recovery</li> </ul>"},{"location":"#reference","title":"Reference","text":"<ul> <li>CLI reference</li> <li>Storage</li> <li>Environment variables</li> <li>Plugin API</li> <li>MCP tools</li> <li>Policies</li> <li><code>.apmignore</code></li> </ul>"},{"location":"#team","title":"Team","text":"<ul> <li>RBAC and roles</li> <li>Departments</li> <li>Approval workflows</li> </ul>"},{"location":"#important-implementation-notes","title":"Important implementation notes","text":"<ul> <li>The current personal vault format is <code>APMVAULT</code> v4.</li> <li>Built-in profiles are <code>standard</code>, <code>hardened</code>, <code>paranoid</code>, and <code>legacy</code>.</li> <li>Personal <code>pm add</code> supports 25 entry types; team <code>pm-team add</code> currently supports 22 shared entry types.</li> <li>Plugin commands can extend the <code>pm</code> command surface at runtime.</li> </ul>"},{"location":"PLUGIN_API/","title":"Plugin API","text":"<p>This document redirects to the full Plugin API reference.</p> <p>See Plugin API Reference for the complete documentation including:</p> <ul> <li>Manifest schema</li> <li>Full permission catalog (100+ permissions)</li> <li>Step commands</li> <li>Hook system</li> <li>Runtime permission overrides</li> </ul>"},{"location":"autofill_windows/","title":"APM Autofill on Windows","text":"<p>APM autofill is a Windows-only daemon that provides system-wide credential injection without a browser extension. It detects active window context, matches credentials from your vault, and injects keystrokes via a global hotkey.</p>"},{"location":"autofill_windows/#architecture-overview","title":"Architecture Overview","text":"<pre><code>graph LR\n    A[User presses Ctrl+Shift+L] --&gt; B[Hotkey Listener]\n    B --&gt; C[Context Capture]\n    C --&gt; D[Window Title + Process + Focused Field]\n    D --&gt; E[Profile Matcher]\n    E --&gt; F{Matches?}\n    F --&gt;|Single| G[Inject Sequence]\n    F --&gt;|Multiple| H[Show Candidates]\n    F --&gt;|None| I[Show Notification]\n    H --&gt; G</code></pre> <p>The daemon is a local HTTP server running on a loopback address with bearer-token authentication. It captures active window context using Windows UI Automation APIs and matches against autofill profiles derived from your vault entries.</p>"},{"location":"autofill_windows/#getting-started","title":"Getting Started","text":""},{"location":"autofill_windows/#enable-autofill-recommended","title":"Enable Autofill (Recommended)","text":"<pre><code>pm autocomplete enable\n</code></pre> <p>This does two things:</p> <ol> <li>Registers autostart \u2014 The daemon launches automatically when you log in</li> <li>Starts the daemon \u2014 Begins running immediately</li> </ol>"},{"location":"autofill_windows/#manual-control","title":"Manual Control","text":"<pre><code>pm autocomplete start    # Start the daemon manually\npm autocomplete stop     # Stop the daemon\npm autocomplete status   # Check daemon state + autostart status\npm autocomplete disable  # Remove autostart registration\n</code></pre>"},{"location":"autofill_windows/#alternative-commands","title":"Alternative Commands","text":"<pre><code>pm autofill start        # Start with default settings\npm autofill stop         # Stop the daemon\npm autofill status       # Show daemon status\npm autofill list-profiles  # Show all autofill profiles\n</code></pre>"},{"location":"autofill_windows/#unlocking-the-daemon","title":"Unlocking the Daemon","text":"<p>The daemon starts in a locked state and rejects all fill requests until unlocked:</p> <pre><code>pm unlock\n</code></pre> <p>This unlocks both the CLI session and the autofill daemon. The daemon decrypts the vault in memory and holds it for the session duration.</p> <p>To lock:</p> <pre><code>pm lock\n</code></pre> <p>This locks both the CLI session and wipes the daemon's in-memory vault data.</p> <p>Warning</p> <p>The daemon holds decrypted data in memory while unlocked. Always lock when stepping away from your machine.</p>"},{"location":"autofill_windows/#hotkey","title":"Hotkey","text":"<p>Default: Ctrl+Shift+L</p> <p>You can customize the hotkey when starting the daemon:</p> <pre><code>pm autofill start --hotkey \"ctrl+alt+p\"\n</code></pre> <p>When pressed, the daemon:</p> <ol> <li>Captures the current window context (title, process name, focused field)</li> <li>Searches for matching autofill profiles</li> <li>If a single match is found, injects the keystroke sequence</li> <li>If multiple matches are found, shows a selection notification</li> </ol>"},{"location":"autofill_windows/#autofill-profiles","title":"Autofill Profiles","text":"<p>Profiles are derived from your vault's password entries. Each profile maps a credential to context matchers:</p> Field Description <code>service</code> Display name (from vault account name) <code>domain</code> Primary domain for browser matching <code>domains</code> Additional domains <code>entry_account</code> Vault entry account name <code>entry_username</code> Username to inject <code>totp_account</code> Linked TOTP entry for 2FA <code>process_names</code> Process name matchers (e.g., <code>chrome.exe</code>) <code>window_contains</code> Window title substring matchers <code>sequence</code> Custom injection sequence template <p>View all profiles:</p> <pre><code>pm autofill list-profiles\n</code></pre>"},{"location":"autofill_windows/#fill-sequences","title":"Fill Sequences","text":"<p>The default injection sequence is:</p> <pre><code>{USERNAME}{TAB}{PASSWORD}{ENTER}\n</code></pre> <p>This types the username, presses Tab to move to the password field, types the password, and presses Enter.</p> <p>You can customize sequences per profile. Available tokens:</p> Token Action <code>{USERNAME}</code> Types the username <code>{PASSWORD}</code> Types the password <code>{TOTP}</code> Types the linked TOTP code <code>{TAB}</code> Presses the Tab key <code>{ENTER}</code> Presses Enter <code>{DELAY:N}</code> Waits N milliseconds"},{"location":"autofill_windows/#context-detection","title":"Context Detection","text":"<p>The daemon uses multiple signals to detect credential-like contexts:</p> <ol> <li>Window title \u2014 Checks for keywords like \"login\", \"sign in\", \"password\", \"credentials\"</li> <li>Process name \u2014 Identifies browsers and common applications</li> <li>Focused field \u2014 Uses UI Automation to detect password-type input fields</li> <li>Domain extraction \u2014 Parses browser window titles for URLs</li> </ol>"},{"location":"autofill_windows/#popup-hints","title":"Popup Hints","text":"<p>When the daemon detects a credential-like context (even without a hotkey press), it can show a popup notification suggesting that a match is available.</p> <pre><code>pm autocomplete window enable   # Enable popup hints\npm autocomplete window disable  # Disable popup hints\npm autocomplete window status   # Check hint status\n</code></pre>"},{"location":"autofill_windows/#totp-linking","title":"TOTP Linking","text":"<p>Link an existing TOTP entry to a domain so autofill can include the 2FA code:</p> <pre><code>pm autocomplete link-totp\n</code></pre> <p>This interactive command lets you:</p> <ol> <li>Select a TOTP entry from your vault</li> <li>Specify the domain or service it belongs to</li> <li>APM then includes the TOTP code in fill requests for that domain</li> </ol>"},{"location":"autofill_windows/#security-model","title":"Security Model","text":"Property Implementation Network scope Loopback only (127.0.0.1) Authentication Bearer token generated at startup Vault state Held in memory only; wiped on lock/stop Clipboard Not used for core typing flows Session timeout Configurable expiry + inactivity lockout IPC Local HTTP on random port, token-protected <p>No Clipboard</p> <p>APM autofill injects keystrokes directly without using the clipboard. This prevents clipboard-monitoring malware from intercepting your credentials. The clipboard is only used when you explicitly copy via <code>pm get</code> or the C key in the interactive browser.</p>"},{"location":"autofill_windows/#troubleshooting","title":"Troubleshooting","text":""},{"location":"autofill_windows/#daemon-wont-start","title":"Daemon won't start","text":"<ul> <li>Check if another instance is already running: <code>pm autofill status</code></li> <li>Ensure no other application is using the daemon's port</li> </ul>"},{"location":"autofill_windows/#hotkey-doesnt-work","title":"Hotkey doesn't work","text":"<ul> <li>Make sure the daemon is running and unlocked</li> <li>Check if another application has claimed the same hotkey combination</li> <li>Try a different hotkey: <code>pm autofill start --hotkey \"ctrl+alt+p\"</code></li> </ul>"},{"location":"autofill_windows/#no-match-found","title":"No match found","text":"<ul> <li>Verify the entry exists in your vault: <code>pm get [service]</code></li> <li>Check profiles: <code>pm autofill list-profiles</code></li> <li>Ensure the window title or domain matches a profile's matchers</li> </ul>"},{"location":"autofill_windows/#next-steps","title":"Next Steps","text":"<ul> <li>TOTP Guide \u2014 Managing 2FA codes</li> <li>Sessions \u2014 Session lifecycle and the daemon</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>Thanks for contributing to APM. This page covers the fastest path to a clean build and a smooth review.</p>"},{"location":"contributing/#build","title":"Build","text":"<pre><code># Standard build\ngo build -o pm.exe\n\n# Face ID build (requires native OpenCV + dlib)\ngo build -tags faceid -o pm.exe\n</code></pre> <p>Face ID is behind a build tag because it depends on native OpenCV/dlib libraries that are not required for the core CLI.</p>"},{"location":"contributing/#pull-requests","title":"Pull Requests","text":"<ul> <li>Keep changes focused and scoped.</li> <li>Include a short description of what changed and why.</li> <li>Mention any manual test steps you ran.</li> </ul>"},{"location":"contributing/#ai-generated-code","title":"AI-Generated Code","text":"<p>AI-generated changes take longer to review. If possible, write the code yourself and use AI only as a collaborator or reviewer while building APM.</p>"},{"location":"concepts/","title":"Concepts","text":"<p>Deep technical explanations of how APM works under the hood. Each page covers the theory, design decisions, and internal mechanics of a core subsystem.</p>"},{"location":"concepts/#architecture","title":"Architecture","text":"<p>The four-layer design of APM: CLI layer, domain layer, integration layer, and extension layer.</p>"},{"location":"concepts/#encryption","title":"Encryption","text":"<p>Argon2id key derivation, AES-GCM and XChaCha20-Poly1305 support, HMAC-SHA256 integrity, nonce handling, and the DEK recovery slot.</p>"},{"location":"concepts/#vault-format","title":"Vault Format","text":"<p>The V4 binary format specification: <code>APMVAULT</code> header, encrypted body, HMAC signature, and recovery metadata.</p>"},{"location":"concepts/#secret-types","title":"Secret Types","text":"<p>All 25+ structured entry types with their field schemas, validation, and display logic.</p>"},{"location":"concepts/#security-profiles","title":"Security Profiles","text":"<p>Standard, Hardened, Paranoid, and Legacy profiles \u2014 their Argon2id parameters, hardware requirements, and auto-detection.</p>"},{"location":"concepts/#policy-engine","title":"Policy Engine","text":"<p>YAML-based password and rotation policies with classification levels and enforcement.</p>"},{"location":"concepts/#sessions","title":"Sessions","text":"<p>Shell-scoped sessions, ephemeral delegated sessions, and their security boundaries.</p>"},{"location":"concepts/#cloud-synchronization","title":"Cloud Synchronization","text":"<p>Provider comparison, OAuth2 vs PAT, retrieval key mechanics, metadata consent, and end-to-end encryption guarantees.</p>"},{"location":"concepts/#plugins","title":"Plugins","text":"<p>Manifest-based architecture, 100+ permissions, step executor, hook lifecycle, and marketplace.</p>"},{"location":"concepts/#mcp-server","title":"MCP Server","text":"<p>Model Context Protocol internals \u2014 permission scopes, transaction guardrails, and token lifecycle.</p>"},{"location":"concepts/#recovery","title":"Recovery","text":"<p>Multi-factor recovery: email OTP, recovery keys, Shamir secret sharing, WebAuthn passkeys, and recovery codes.</p>"},{"location":"concepts/architecture/","title":"Architecture","text":"<p>APM is structured as a four-layer architecture that separates concerns between user interaction, domain logic, external integrations, and extensibility.</p>"},{"location":"concepts/architecture/#layer-diagram","title":"Layer Diagram","text":"<pre><code>graph TB\n    subgraph CLI[\"Layer 1: CLI\"]\n        A[main.go \u2014 Cobra command tree]\n    end\n\n    subgraph Domain[\"Layer 2: Domain\"]\n        B[vault.go \u2014 Vault &amp; Entries]\n        C[session.go \u2014 Sessions]\n        D[security.go \u2014 Profiles &amp; Alerts]\n        E[policy.go \u2014 Policy Engine]\n        F[trust.go \u2014 Trust Scoring]\n        G[health.go \u2014 Health Dashboard]\n        H[audit.go \u2014 Audit Log]\n        I[anomaly.go \u2014 Anomaly Detection]\n        J[vocab.go \u2014 Vocabulary Engine]\n        K[portability.go \u2014 Import/Export]\n        L[apmignore.go \u2014 Ignore Rules]\n        M[brute.go \u2014 Brute Force Test]\n        N[recovery_*.go \u2014 Recovery Subsystem]\n        O[ephemeral_session.go \u2014 Delegated Sessions]\n    end\n\n    subgraph Integration[\"Layer 3: Integration\"]\n        P[cloud.go \u2014 GDrive/GitHub/Dropbox]\n        Q[mcp.go \u2014 MCP Server]\n        R[autofill/ \u2014 Windows Daemon]\n    end\n\n    subgraph Extension[\"Layer 4: Extension\"]\n        S[plugins/engine.go \u2014 Step Executor]\n        T[plugins/manifest.go \u2014 Manifest &amp; Permissions]\n        U[plugins/definitions.go \u2014 Command Definitions]\n        V[cloud_plugins.go \u2014 Marketplace]\n    end\n\n    CLI --&gt; Domain\n    CLI --&gt; Integration\n    CLI --&gt; Extension\n    Integration --&gt; Domain\n    Extension --&gt; Domain</code></pre>"},{"location":"concepts/architecture/#layer-1-cli","title":"Layer 1: CLI","text":"<p>File: <code>main.go</code> (~3,600 lines)</p> <p>The CLI layer uses Cobra to define the entire command tree. It handles:</p> <ul> <li>User input parsing and validation</li> <li>Interactive prompts (password input, menus, confirmations)</li> <li>Output formatting (colored terminal output via <code>fatih/color</code>)</li> <li>Delegation to domain and integration layers</li> </ul>"},{"location":"concepts/architecture/#key-design-decisions","title":"Key Design Decisions","text":"<ul> <li>Single binary \u2014 All commands live in one file for straightforward distribution</li> <li>Interactive by default \u2014 Most commands use interactive prompts rather than requiring flags</li> <li>Explicit session checks \u2014 Every sensitive command verifies an active session before proceeding</li> </ul>"},{"location":"concepts/architecture/#layer-2-domain","title":"Layer 2: Domain","text":"<p>Directory: <code>src/</code></p> <p>The domain layer contains all business logic. It operates entirely in memory after vault decryption:</p> File Responsibility <code>vault.go</code> Vault struct, entry types, encrypt/decrypt logic <code>session.go</code> Shell-scoped session management <code>ephemeral_session.go</code> Delegated sessions with host/PID/agent binding <code>security.go</code> Profile management, alerts, email masking <code>policy.go</code> YAML policy loading and enforcement <code>trust.go</code> Per-secret trust scoring (telemetry + risk) <code>health.go</code> Vault health scoring algorithm <code>audit.go</code> Tamper-evident audit logging <code>anomaly.go</code> Unusual activity time detection <code>vocab.go</code> Note vocabulary with aliases and scoring <code>portability.go</code> JSON, CSV, TXT import/export <code>apmignore.go</code> Ignore rule parsing and filtering <code>brute.go</code> Brute-force test engine <code>recovery_passkey.go</code> WebAuthn passkey recovery <code>quorum_recovery.go</code> Shamir secret sharing (quorum recovery) <code>recovery_codes.go</code> One-time recovery codes"},{"location":"concepts/architecture/#layer-3-integration","title":"Layer 3: Integration","text":"Component File(s) Protocol Purpose Cloud Sync <code>cloud.go</code> REST / OAuth2 / PAT Multi-provider vault sync MCP Server <code>mcp.go</code> stdio (JSON-RPC) AI assistant vault access Autofill <code>autofill/*.go</code> HTTP (loopback) Windows keystroke injection"},{"location":"concepts/architecture/#cloud-provider-architecture","title":"Cloud Provider Architecture","text":"<p>Each cloud provider follows the same interface:</p> <ol> <li>Init \u2192 Configure OAuth/PAT credentials</li> <li>Sync \u2192 Upload encrypted vault blob</li> <li>Get \u2192 Download remote vault blob</li> <li>Reset \u2192 Clear provider configuration</li> </ol> <p>Credentials are stored encrypted inside the vault itself.</p>"},{"location":"concepts/architecture/#mcp-server-architecture","title":"MCP Server Architecture","text":"<p>The MCP server is spawned as a subprocess by AI clients. It:</p> <ol> <li>Accepts stdio transport</li> <li>Validates bearer tokens against the vault's token store</li> <li>Routes tool calls to domain layer (vault read/write)</li> <li>Enforces permission scope checks per tool</li> <li>Manages transaction state for write operations</li> </ol>"},{"location":"concepts/architecture/#autofill-architecture","title":"Autofill Architecture","text":"<p>The autofill daemon is a local HTTP server that:</p> <ol> <li>Listens on a random port on loopback</li> <li>Stores a shared bearer token for IPC</li> <li>Captures active window context via Windows APIs</li> <li>Matches credentials against vault profiles</li> <li>Injects keystrokes via <code>SendInput</code> (Windows)</li> </ol>"},{"location":"concepts/architecture/#layer-4-extension","title":"Layer 4: Extension","text":"Component File(s) Purpose Engine <code>plugins/engine.go</code> Executes plugin step pipelines Manifest <code>plugins/manifest.go</code> Validates plugin.json schemas Definitions <code>plugins/definitions.go</code> Built-in step command definitions Marketplace <code>cloud_plugins.go</code> Cloud-backed plugin distribution"},{"location":"concepts/architecture/#data-flow-vault-unlock-save","title":"Data Flow: Vault Unlock \u2192 Save","text":"<pre><code>sequenceDiagram\n    participant User\n    participant CLI\n    participant Session\n    participant Vault\n\n    User-&gt;&gt;CLI: pm unlock\n    CLI-&gt;&gt;Vault: Load encrypted bytes from disk\n    Vault-&gt;&gt;Vault: Parse header (magic, version, profile, salt)\n    Vault-&gt;&gt;Vault: Derive 96 bytes via Argon2id(password, salt)\n    Note over Vault: key[0:32] = encryption key&lt;br/&gt;key[32:64] = auth key&lt;br/&gt;key[64:96] = validator\n    Vault-&gt;&gt;Vault: Verify HMAC-SHA256(auth_key, metadata)\n    Vault-&gt;&gt;Vault: Decrypt AEAD(enc_key, ciphertext)\n    Vault-&gt;&gt;Vault: Verify validator matches stored validator\n    Vault--&gt;&gt;CLI: Decrypted Vault struct\n    CLI-&gt;&gt;Session: Create session file\n    CLI--&gt;&gt;User: \"Vault unlocked\"\n\n    Note over User: ... user performs operations ...\n\n    User-&gt;&gt;CLI: pm add (adds entry to in-memory vault)\n    CLI-&gt;&gt;Vault: Generate new salt\n    Vault-&gt;&gt;Vault: Derive keys from password + new salt\n    Vault-&gt;&gt;Vault: Serialize vault to JSON\n    Vault-&gt;&gt;Vault: Encrypt with AEAD(enc_key, json)\n    Vault-&gt;&gt;Vault: Compute HMAC-SHA256(auth_key, metadata)\n    Vault-&gt;&gt;Vault: Write header + ciphertext + HMAC to disk\n    Vault--&gt;&gt;CLI: Save complete</code></pre>"},{"location":"concepts/architecture/#security-boundaries","title":"Security Boundaries","text":"Boundary Inside Outside Vault encryption All entry data, credentials Master password, keys Session boundary Decrypted vault (in memory) Persisted data (on disk) Cloud boundary Encrypted blob (APMVAULT) Plaintext entries MCP boundary Permitted data (per scope) Unauthorized data Plugin boundary Permitted operations (per perm) Unauthorized operations"},{"location":"concepts/architecture/#next-steps","title":"Next Steps","text":"<ul> <li>Encryption \u2014 Detailed cryptographic design</li> <li>Vault Format \u2014 Binary format specification</li> </ul>"},{"location":"concepts/cloud-sync/","title":"Cloud Synchronization","text":"<p>APM synchronizes encrypted vaults across devices using cloud storage providers. This page covers the technical design, provider differences, and security guarantees.</p>"},{"location":"concepts/cloud-sync/#architecture","title":"Architecture","text":"<pre><code>graph LR\n    subgraph Local\n        A[Vault File] --&gt; B[.apmignore Filter]\n        B --&gt; C[Filtered Vault]\n        C --&gt; D[Encrypted Blob \u2014 APMVAULT format]\n    end\n\n    subgraph Providers\n        D --&gt; E[Google Drive \u2014 App Data Folder]\n        D --&gt; F[GitHub \u2014 Private Repo File]\n        D --&gt; G[Dropbox \u2014 App Folder]\n    end</code></pre> <p>The sync process:</p> <ol> <li>Load <code>.apmignore</code> rules</li> <li>Create a filtered copy of the vault (in memory)</li> <li>Encrypt the filtered vault using the same APMVAULT format</li> <li>Upload the encrypted blob to the configured provider(s)</li> </ol>"},{"location":"concepts/cloud-sync/#provider-comparison","title":"Provider Comparison","text":"Aspect Google Drive GitHub Dropbox Auth OAuth2/PKCE Personal Access Token OAuth2/PKCE Storage App Data Folder File in private repo App Folder Isolation Hidden from Drive UI Visible in repo Visible in app folder Version History Provider-managed Git commits \u2014 full history Provider-managed Token Storage Encrypted in vault Encrypted in vault Encrypted in vault Library <code>google.golang.org/api</code> <code>github.com/google/go-github</code> <code>dropbox-sdk-go-unofficial</code>"},{"location":"concepts/cloud-sync/#authentication-flows","title":"Authentication Flows","text":""},{"location":"concepts/cloud-sync/#oauth2pkce-google-drive-dropbox","title":"OAuth2/PKCE (Google Drive &amp; Dropbox)","text":"<pre><code>sequenceDiagram\n    participant User\n    participant APM\n    participant Browser\n    participant Provider\n\n    User-&gt;&gt;APM: pm cloud init gdrive\n    APM-&gt;&gt;Browser: Open OAuth consent URL\n    Browser-&gt;&gt;Provider: User grants consent\n    Provider-&gt;&gt;APM: Authorization code (via redirect)\n    APM-&gt;&gt;Provider: Exchange code for access token\n    APM-&gt;&gt;APM: Encrypt &amp; store token in vault</code></pre>"},{"location":"concepts/cloud-sync/#personal-access-token-github","title":"Personal Access Token (GitHub)","text":"<pre><code>sequenceDiagram\n    participant User\n    participant APM\n    participant GitHub\n\n    User-&gt;&gt;APM: pm cloud init github\n    APM-&gt;&gt;User: Prompt for PAT + repo name\n    User-&gt;&gt;APM: Provides PAT and repo\n    APM-&gt;&gt;GitHub: Validate token\n    APM-&gt;&gt;APM: Encrypt &amp; store in vault</code></pre>"},{"location":"concepts/cloud-sync/#retrieval-keys","title":"Retrieval Keys","text":"<p>When uploading a vault, APM generates a retrieval key \u2014 a unique identifier that helps locate the vault on the provider.</p>"},{"location":"concepts/cloud-sync/#metadata-consent","title":"Metadata Consent","text":"<p>For providers that support metadata (Google Drive, Dropbox), APM asks for explicit consent before storing a hash of the retrieval key:</p> <ul> <li>Consent given \u2014 A one-way hash of the retrieval key is stored in provider metadata, enabling lookup</li> <li>Consent denied \u2014 The vault is identified only by provider-specific IDs (file ID or path)</li> </ul> <p>This ensures no identifying information is stored without explicit user approval.</p>"},{"location":"concepts/cloud-sync/#security-guarantees","title":"Security Guarantees","text":""},{"location":"concepts/cloud-sync/#whats-encrypted","title":"What's Encrypted","text":"<p>The uploaded blob is the exact same APMVAULT format stored on disk:</p> <ul> <li><code>APMVAULT</code> header (magic, version, profile, salt, validator)</li> <li>AEAD-encrypted payload (<code>aes-gcm</code> or <code>xchacha20-poly1305</code>, depending on the vault profile)</li> <li>HMAC-SHA256 integrity signature</li> </ul>"},{"location":"concepts/cloud-sync/#whats-not-uploaded","title":"What's Not Uploaded","text":"<ul> <li>Master password</li> <li>Decrypted entries</li> <li>Session data</li> <li>Audit logs</li> <li>Ephemeral session tokens</li> </ul>"},{"location":"concepts/cloud-sync/#cloud-token-storage","title":"Cloud Token Storage","text":"<p>Provider credentials (OAuth tokens, PATs) are stored encrypted inside the vault. They're accessible only when the vault is unlocked, and they're synced across devices automatically (unless filtered by <code>.apmignore</code>).</p>"},{"location":"concepts/cloud-sync/#conflict-resolution","title":"Conflict Resolution","text":"<p>When downloading a remote vault that differs from the local copy:</p> Strategy Behavior Overwrite local Remote vault replaces local vault entirely Keep conflict copy Remote vault saved as timestamped file alongside Cancel No changes made <p>No Entry-Level Merge</p> <p>APM operates on whole-vault sync \u2014 it does not merge individual entries. This is by design: merging encrypted data safely would require leaking metadata about vault contents to detect conflicts.</p>"},{"location":"concepts/cloud-sync/#next-steps","title":"Next Steps","text":"<ul> <li>Cloud Sync Guide \u2014 Practical setup and usage</li> <li>Using .apmignore \u2014 Controlling upload filters</li> </ul>"},{"location":"concepts/encryption/","title":"Encryption","text":"<p>APM uses a zero-knowledge model with encrypted-at-rest storage and authenticated encryption. Your master password never leaves your machine \u2014 it's used locally to derive multiple cryptographic keys that protect every aspect of the vault.</p>"},{"location":"concepts/encryption/#key-derivation","title":"Key Derivation","text":""},{"location":"concepts/encryption/#argon2id","title":"Argon2id","text":"<p>APM uses Argon2id \u2014 the winner of the Password Hashing Competition \u2014 to derive encryption keys from your master password:</p> <pre><code>master_password + salt \u2192 Argon2id \u2192 96 bytes of key material\n</code></pre> <p>The 96-byte output is split into three distinct keys:</p> Bytes Key Name Purpose 0\u201331 Encryption Key Vault payload and DEK-slot AEAD encryption 32\u201363 Auth Key HMAC-SHA256 integrity verification 64\u201395 Validator Key Password correctness check before full decrypt"},{"location":"concepts/encryption/#why-three-keys","title":"Why Three Keys?","text":"<p>A single Argon2id invocation produces all three keys, but each serves a distinct security purpose:</p> <ol> <li>Encryption Key \u2014 Used only for the selected AEAD encrypt/decrypt path. Never exposed or stored.</li> <li>Auth Key \u2014 Used to compute an HMAC signature over vault metadata. Verifies that the vault hasn't been tampered with before attempting decryption.</li> <li>Validator Key \u2014 A hash of this key is stored in the vault header. APM checks it immediately after key derivation to confirm the correct password was entered, before attempting the expensive decryption step.</li> </ol>"},{"location":"concepts/encryption/#argon2id-parameters-by-profile","title":"Argon2id Parameters by Profile","text":"Profile Memory Cost Time Cost Parallelism Salt Size Standard 64 MB 3 2 16 bytes Hardened 256 MB 5 4 32 bytes Paranoid 512 MB 6 4 32 bytes Legacy PBKDF2 600,000 1 16 bytes <p>Each profile makes a deliberate trade-off between performance and brute-force resistance:</p> <ul> <li>Standard is suitable for most machines and resists commodity GPU attacks</li> <li>Hardened pushes memory requirements high enough to make ASIC/FPGA attacks impractical</li> <li>Paranoid is designed for servers and high-security environments</li> <li>Legacy uses PBKDF2 for backward compatibility only</li> </ul>"},{"location":"concepts/encryption/#encryption_1","title":"Encryption","text":""},{"location":"concepts/encryption/#aead-cipher-selection","title":"AEAD Cipher Selection","text":"<p>APM encrypts the serialized vault JSON with an AEAD cipher selected by the active profile. Built-in profiles currently default to <code>aes-gcm</code>, and custom profiles can switch to <code>xchacha20-poly1305</code>.</p>"},{"location":"concepts/encryption/#aes-256-gcm","title":"AES-256-GCM","text":"<p><code>aes-gcm</code> is the default compatibility path:</p> <pre><code>encryption_key + nonce + plaintext_json \u2192 AES-256-GCM \u2192 ciphertext\n</code></pre>"},{"location":"concepts/encryption/#xchacha20-poly1305","title":"XChaCha20-Poly1305","text":"<p><code>xchacha20-poly1305</code> is also supported for vault payloads and DEK slots:</p> <pre><code>encryption_key + nonce(24 bytes) + plaintext_json \u2192 XChaCha20-Poly1305 \u2192 ciphertext\n</code></pre> <p>Both modes provide:</p> <ul> <li>Confidentiality \u2014 The ciphertext reveals nothing about the plaintext</li> <li>Integrity \u2014 Built-in authentication tag detects any modification</li> <li>Non-malleability \u2014 Attackers cannot produce valid ciphertext without the key</li> </ul>"},{"location":"concepts/encryption/#nonce-handling","title":"Nonce Handling","text":"<p>A fresh random nonce is generated for every save operation. The nonce is stored in the vault header area and is not secret \u2014 its purpose is to ensure that encrypting the same vault twice produces different ciphertext.</p> <p>Nonce Reuse Protection</p> <p>APM rotates both the salt and the nonce on every save. That means the derived keys change each time, and the selected AEAD also gets a fresh nonce for the new ciphertext.</p>"},{"location":"concepts/encryption/#double-layer-integrity","title":"Double-Layer Integrity","text":"<p>APM provides two independent integrity checks:</p>"},{"location":"concepts/encryption/#layer-1-aead-authentication-tag","title":"Layer 1: AEAD Authentication Tag","text":"<p>Both supported AEAD modes include an authentication tag that's verified during decryption. If any byte of the ciphertext is modified, decryption fails immediately.</p>"},{"location":"concepts/encryption/#layer-2-hmac-sha256-signature","title":"Layer 2: HMAC-SHA256 Signature","text":"<p>An HMAC-SHA256 signature is computed over vault metadata using the 32-byte authentication key:</p> <pre><code>auth_key + vault_metadata \u2192 HMAC-SHA256 \u2192 32-byte signature\n</code></pre> <p>This signature is stored in the vault file and verified before decryption. It provides:</p> <ul> <li>Pre-decryption tamper detection \u2014 APM detects modification before attempting the expensive decrypt step</li> <li>Metadata protection \u2014 Covers the vault header, profile, and salt information that are stored unencrypted</li> </ul>"},{"location":"concepts/encryption/#password-validation","title":"Password Validation","text":"<p>Before decrypting the vault, APM performs a fast password check:</p> <ol> <li>Derive the validator key (bytes 64\u201395 of Argon2id output)</li> <li>Hash the validator key with SHA-256</li> <li>Compare against the stored validator bytes in the vault header</li> </ol> <p>This catches wrong passwords in milliseconds rather than waiting for a full GCM decryption failure. It's a UX optimization \u2014 not a security boundary (GCM still validates independently).</p>"},{"location":"concepts/encryption/#data-encryption-key-dek-slot","title":"Data Encryption Key (DEK) Slot","text":"<p>For account recovery, APM supports a DEK slot:</p> <pre><code>master_password \u2192 Argon2id \u2192 encryption_key \u2192 encrypts vault\nrecovery_key   \u2192 Argon2id \u2192 recovery_enc_key \u2192 encrypts DEK_slot(encryption_key)\n</code></pre> <p>The DEK slot stores the vault's encryption key, encrypted under the recovery key. This allows vault re-encryption during recovery without ever exposing the original master password:</p> <ol> <li>User provides recovery key</li> <li>APM decrypts the DEK slot to obtain the encryption key</li> <li>Vault is decrypted using the recovered encryption key</li> <li>User sets a new master password</li> <li>Vault is re-encrypted with new keys</li> </ol> <p>Zero-Knowledge Preserved</p> <p>The recovery key never needs to be stored on any server. It's generated locally and must be stored securely by the user.</p>"},{"location":"concepts/encryption/#data-encryption-standalone","title":"Data Encryption (Standalone)","text":"<p>APM provides standalone <code>EncryptData</code> and <code>DecryptData</code> functions for encrypting arbitrary data (used internally for export encryption, cloud token storage, etc.):</p> <pre><code>password + data \u2192 Argon2id(password, random_salt) \u2192 AES-256-GCM(key, data) \u2192 encrypted_blob\n</code></pre> <p>The output format: <code>salt (16 bytes) + nonce (12 bytes) + ciphertext</code></p>"},{"location":"concepts/encryption/#cryptographic-flow-summary","title":"Cryptographic Flow Summary","text":"<pre><code>graph TD\n    MP[Master Password] --&gt; KDF[Argon2id with Salt]\n    KDF --&gt; EK[Encryption Key \u2014 32 bytes]\n    KDF --&gt; AK[Auth Key \u2014 32 bytes]\n    KDF --&gt; VK[Validator Key \u2014 32 bytes]\n\n    EK --&gt; GCM[AEAD Encrypt/Decrypt]\n    AK --&gt; HMAC[HMAC-SHA256 Verify/Sign]\n    VK --&gt; VALID[Password Validation Check]\n\n    GCM --&gt; VAULT[Encrypted Vault Data]\n    HMAC --&gt; SIG[Integrity Signature]\n    VALID --&gt; CHECK[Fast Wrong-Password Detection]</code></pre>"},{"location":"concepts/encryption/#next-steps","title":"Next Steps","text":"<ul> <li>Vault Format \u2014 How these components are laid out in the binary file</li> <li>Security Profiles \u2014 Profile parameters and auto-detection</li> <li>Recovery \u2014 How DEK recovery works in practice</li> </ul>"},{"location":"concepts/mcp/","title":"MCP Server","text":"<p>APM includes a native Model Context Protocol (MCP) server that enables AI assistants to interact with your encrypted vault through a standardized tool-calling interface.</p>"},{"location":"concepts/mcp/#architecture","title":"Architecture","text":"<pre><code>graph TB\n    subgraph \"AI Client (Claude/Cursor/Windsurf)\"\n        A[AI Agent]\n    end\n\n    subgraph \"APM MCP Server\"\n        B[Token Validator]\n        C[Permission Checker]\n        D[Tool Router]\n        E[Transaction Manager]\n    end\n\n    subgraph \"Vault Layer\"\n        F[Session Manager]\n        G[Vault Read/Write]\n    end\n\n    A --&gt;|stdio JSON-RPC| B\n    B --&gt; C\n    C --&gt; D\n    D --&gt; E\n    D --&gt; G\n    E --&gt; G\n    G --&gt; F</code></pre>"},{"location":"concepts/mcp/#transport","title":"Transport","text":"<p>The MCP server uses stdio transport \u2014 it reads JSON-RPC messages from stdin and writes responses to stdout. It's spawned as a subprocess by the AI client.</p>"},{"location":"concepts/mcp/#permission-scopes","title":"Permission Scopes","text":"<p>Each token has one or more permission scopes that control tool access:</p> Scope Tools Available <code>read</code> <code>list_entries</code>, <code>search_entries</code>, <code>get_entry</code> (metadata only) <code>secrets</code> All <code>read</code> tools + <code>decrypt_entry</code> (password values), <code>get_totp</code> <code>write</code> All <code>read</code> + <code>add_entry</code>, <code>edit_entry</code>, <code>delete_entry</code>, <code>manage_spaces</code>, <code>install_plugin</code>, <code>cloud_sync</code> <code>admin</code> All scopes + <code>manage_profiles</code>, <code>cloud_config</code>, <code>get_history</code>, <code>get_audit_logs</code> <p>Scopes are cumulative \u2014 <code>admin</code> includes everything from <code>write</code>, which includes everything from <code>read</code>.</p>"},{"location":"concepts/mcp/#transaction-guardrails","title":"Transaction Guardrails","text":"<p>All write operations use a two-phase commit to prevent unintended changes:</p>"},{"location":"concepts/mcp/#phase-1-preview","title":"Phase 1: Preview","text":"<p>When the AI calls a write tool (e.g., <code>add_entry</code>), APM:</p> <ol> <li>Creates a preview of the change</li> <li>Generates a unique <code>tx_id</code></li> <li>Returns the preview to the AI for review</li> </ol>"},{"location":"concepts/mcp/#phase-2-commit","title":"Phase 2: Commit","text":"<p>The AI must send a second call with:</p> <ul> <li>The same <code>tx_id</code></li> <li>An explicit <code>approve: true</code> flag</li> </ul> <p>Only then does APM execute the mutation and return a receipt ID.</p> <pre><code>stateDiagram-v2\n    [*] --&gt; Pending: AI calls write tool\n    Pending --&gt; Preview: Server creates preview\n    Preview --&gt; Approved: AI sends approve\n    Preview --&gt; Expired: Timeout (no response)\n    Approved --&gt; Committed: Server executes mutation\n    Committed --&gt; [*]: Receipt returned\n    Expired --&gt; [*]: Transaction cancelled</code></pre>"},{"location":"concepts/mcp/#why-guardrails","title":"Why Guardrails?","text":"<p>AI agents can make mistakes. Transaction guardrails ensure:</p> <ul> <li>No accidental mutations \u2014 Every change requires explicit approval</li> <li>Audit trail \u2014 Each transaction generates a receipt ID</li> <li>Reversibility \u2014 The preview phase lets the AI (or user) catch errors</li> </ul>"},{"location":"concepts/mcp/#token-lifecycle","title":"Token Lifecycle","text":""},{"location":"concepts/mcp/#creation","title":"Creation","text":"<pre><code>pm mcp token\n</code></pre> <p>Creates a token with:</p> <ul> <li>A human-readable name for identification</li> <li>Selected permission scopes</li> <li>Optional expiry duration</li> <li>A hashed copy stored in the vault (the plaintext token is shown only once)</li> </ul>"},{"location":"concepts/mcp/#storage","title":"Storage","text":"<p>Tokens are stored as an array within the encrypted vault:</p> <pre><code>{\n  \"mcp_tokens\": [\n    {\n      \"name\": \"claude-desktop\",\n      \"token_hash\": \"sha256:...\",\n      \"permissions\": [\"read\", \"secrets\"],\n      \"created_at\": \"2026-01-15T10:00:00Z\",\n      \"last_used\": \"2026-01-16T08:30:00Z\",\n      \"use_count\": 42,\n      \"expires_at\": \"2026-02-15T10:00:00Z\"\n    }\n  ]\n}\n</code></pre>"},{"location":"concepts/mcp/#validation","title":"Validation","text":"<p>On every request:</p> <ol> <li>Extract the bearer token from the <code>--token</code> flag</li> <li>Hash the provided token with SHA-256</li> <li>Compare against stored token hashes</li> <li>Check expiry</li> <li>Verify scope permits the requested tool</li> </ol>"},{"location":"concepts/mcp/#revocation","title":"Revocation","text":"<pre><code>pm mcp revoke \"claude-desktop\"\n</code></pre> <p>Removes the token entry from the vault. The token is immediately rejected on subsequent requests.</p>"},{"location":"concepts/mcp/#session-integration","title":"Session Integration","text":"<p>The MCP server requires an active APM session to access vault data:</p> <ul> <li>Regular session \u2014 The MCP server uses the same session as the CLI (<code>pm unlock</code>)</li> <li>Ephemeral session \u2014 Set <code>APM_EPHEMERAL_ID</code> for host/PID/agent-bound delegation</li> </ul> <p>Tip</p> <p>For production deployments, use ephemeral sessions with host binding to restrict MCP access to the specific machine running the AI client.</p>"},{"location":"concepts/mcp/#next-steps","title":"Next Steps","text":"<ul> <li>MCP Tools Reference \u2014 All tool schemas and permissions</li> <li>MCP Integration Guide \u2014 Setup for Claude, Cursor, Windsurf</li> </ul>"},{"location":"concepts/plugins/","title":"Plugins","text":"<p>APM features a manifest-based plugin architecture that allows third-party extensions to interact with the vault, hook into lifecycle events, and register custom commands \u2014 all within a strict permission boundary.</p>"},{"location":"concepts/plugins/#architecture","title":"Architecture","text":"<pre><code>graph TB\n    subgraph Plugin\n        A[plugin.json \u2014 Manifest]\n        B[Command Definitions]\n    end\n\n    subgraph Engine\n        C[Manifest Validator]\n        D[Permission Checker]\n        E[Step Executor]\n    end\n\n    subgraph Runtime\n        F[Vault State]\n        G[File System]\n        H[Network]\n        I[System APIs]\n    end\n\n    A --&gt; C\n    C --&gt; D\n    B --&gt; E\n    E --&gt; D\n    D --&gt;|Permitted| F\n    D --&gt;|Permitted| G\n    D --&gt;|Permitted| H\n    D --&gt;|Permitted| I\n    D --&gt;|Denied| J[Permission Error]</code></pre>"},{"location":"concepts/plugins/#manifest-format","title":"Manifest Format","text":"<p>Every plugin is a directory containing a <code>plugin.json</code> manifest:</p> <pre><code>{\n  \"name\": \"my_plugin\",\n  \"version\": \"1.2.0\",\n  \"description\": \"A plugin that does something useful\",\n  \"author\": \"Developer Name\",\n  \"permissions_required\": [\n    \"vault.read\",\n    \"vault.write\",\n    \"file.storage\"\n  ],\n  \"allowed_file_types\": [\".json\", \".txt\", \".csv\"],\n  \"commands\": [\"backup\", \"restore\", \"analyze\"],\n  \"hooks\": [\"post:add\", \"pre:sync\"]\n}\n</code></pre>"},{"location":"concepts/plugins/#validation-rules","title":"Validation Rules","text":"<p>Manifests are validated against strict rules:</p> <ul> <li><code>name</code> \u2014 Required, non-empty</li> <li><code>version</code> \u2014 Must follow semantic versioning (<code>X.Y.Z</code> with optional pre-release)</li> <li><code>permissions_required</code> \u2014 Must only contain known permission strings (or wildcard patterns like <code>vault.*</code>)</li> <li><code>hooks</code> \u2014 Must follow the <code>pre:command</code> or <code>post:command</code> pattern</li> </ul>"},{"location":"concepts/plugins/#permission-system","title":"Permission System","text":"<p>APM uses a granular, hierarchical permission system with 100+ individual permissions organized across 14 domains. Plugins must declare their required permissions, and APM enforces them at runtime for every step execution.</p>"},{"location":"concepts/plugins/#vault-permissions-vault","title":"Vault Permissions (<code>vault.*</code>)","text":"<p>Core vault-level operations:</p> Permission Description <code>vault.read</code> Read vault entries and metadata <code>vault.write</code> Write/modify vault entries <code>vault.delete</code> Delete vault entries <code>vault.import</code> Import data into the vault <code>vault.export</code> Export data from the vault <code>vault.backup</code> Create vault backups <code>vault.restore</code> Restore from vault backups <code>vault.history</code> Access vault modification history <code>vault.lock</code> Lock the vault <code>vault.unlock</code> Unlock the vault <code>vault.sync</code> Trigger cloud synchronization"},{"location":"concepts/plugins/#vault-item-permissions-vaultitem","title":"Vault Item Permissions (<code>vault.item.*</code>)","text":"<p>CRUD operations on individual entries:</p> Permission Description <code>vault.item.create</code> Create new vault entries <code>vault.item.read</code> Read existing entries <code>vault.item.update</code> Update existing entries <code>vault.item.delete</code> Delete entries <code>vault.item.move</code> Move entries between spaces <code>vault.item.copy</code> Copy entries <code>vault.item.share</code> Share entries (team edition)"},{"location":"concepts/plugins/#vault-field-permissions-vaultitemfield","title":"Vault Field Permissions (<code>vault.item.field.*</code>)","text":"<p>Fine-grained field-level access control:</p> Permission Description <code>vault.item.field.password.read</code> Read password fields <code>vault.item.field.password.write</code> Write password fields <code>vault.item.field.username.read</code> Read username fields <code>vault.item.field.username.write</code> Write username fields <code>vault.item.field.url.read</code> Read URL fields <code>vault.item.field.url.write</code> Write URL fields <code>vault.item.field.notes.read</code> Read note content <code>vault.item.field.notes.write</code> Write note content <code>vault.item.field.totp.read</code> Read TOTP secrets <code>vault.item.field.totp.write</code> Write TOTP secrets <code>vault.item.field.tags.read</code> Read entry tags <code>vault.item.field.tags.write</code> Write entry tags <code>vault.item.field.metadata.read</code> Read entry metadata <code>vault.item.field.metadata.write</code> Write entry metadata <code>vault.item.field.custom.read</code> Read custom fields <code>vault.item.field.custom.write</code> Write custom fields"},{"location":"concepts/plugins/#network-permissions-network","title":"Network Permissions (<code>network.*</code>)","text":"<p>Outbound and inbound network access:</p> Permission Description <code>network.outbound</code> General outbound network access <code>network.inbound</code> Accept inbound connections <code>network.http</code> HTTP requests <code>network.https</code> HTTPS requests <code>network.ftp</code> FTP connections <code>network.sftp</code> SFTP connections <code>network.ssh</code> SSH connections <code>network.ws</code> WebSocket connections <code>network.wss</code> Secure WebSocket connections <code>network.tcp</code> Raw TCP connections <code>network.udp</code> UDP connections <code>network.icmp</code> ICMP (ping) <code>network.proxy</code> Proxy connections <code>network.dns</code> DNS queries <code>network.api.rest</code> REST API calls <code>network.api.grpc</code> gRPC API calls"},{"location":"concepts/plugins/#system-permissions-system","title":"System Permissions (<code>system.*</code>)","text":"<p>OS-level operations:</p> Permission Description <code>system.read</code> Read system information <code>system.write</code> Write system files <code>system.exec</code> Execute system commands <code>system.env.read</code> Read environment variables <code>system.env.write</code> Set environment variables <code>system.process.read</code> Read process information <code>system.process.write</code> Modify processes <code>system.process.kill</code> Kill processes <code>system.clipboard.read</code> Read from clipboard <code>system.clipboard.write</code> Write to clipboard <code>system.notification</code> Show system notifications <code>system.audio.record</code> Record audio <code>system.audio.play</code> Play audio <code>system.camera</code> Access camera <code>system.location</code> Access location services <code>system.power</code> Control power state <code>system.usb.read</code> Read USB devices <code>system.usb.write</code> Write to USB devices <code>system.bluetooth</code> Access Bluetooth <code>system.wifi</code> Access Wi-Fi interfaces"},{"location":"concepts/plugins/#cryptography-permissions-crypto","title":"Cryptography Permissions (<code>crypto.*</code>)","text":"<p>Cryptographic operations:</p> Permission Description <code>crypto.use</code> General cryptography access <code>crypto.hash</code> Compute hashes <code>crypto.random</code> Generate random data <code>crypto.encrypt</code> Encrypt data <code>crypto.decrypt</code> Decrypt data <code>crypto.sign</code> Create digital signatures <code>crypto.verify</code> Verify digital signatures <code>crypto.key.generate</code> Generate cryptographic keys <code>crypto.key.store</code> Store keys <code>crypto.key.load</code> Load stored keys <code>crypto.key.delete</code> Delete stored keys <code>crypto.cert.generate</code> Generate certificates <code>crypto.cert.validate</code> Validate certificates"},{"location":"concepts/plugins/#file-storage-file","title":"File Storage (<code>file.*</code>)","text":"Permission Description <code>file.storage</code> Read/write files (limited to allowed file types)"},{"location":"concepts/plugins/#plugin-management-plugin","title":"Plugin Management (<code>plugin.*</code>)","text":"Permission Description <code>plugin.list</code> List installed plugins <code>plugin.install</code> Install plugins <code>plugin.uninstall</code> Remove plugins <code>plugin.update</code> Update plugins <code>plugin.config.read</code> Read plugin configuration <code>plugin.config.write</code> Write plugin configuration <code>plugin.reload</code> Reload plugin state"},{"location":"concepts/plugins/#ui-permissions-ui","title":"UI Permissions (<code>ui.*</code>)","text":"<p>User interface interactions:</p> Permission Description <code>ui.prompt</code> Show user prompts <code>ui.alert</code> Show alert dialogs <code>ui.confirm</code> Show confirmation dialogs <code>ui.toast</code> Show toast notifications <code>ui.dialog</code> Show custom dialogs <code>ui.window.open</code> Open windows <code>ui.window.close</code> Close windows <code>ui.window.maximize</code> Maximize windows <code>ui.window.minimize</code> Minimize windows <code>ui.menu.add</code> Add menu items <code>ui.menu.remove</code> Remove menu items <code>ui.theme.set</code> Change UI theme <code>ui.font.set</code> Change UI font"},{"location":"concepts/plugins/#user-session-permissions-user","title":"User &amp; Session Permissions (<code>user.*</code>)","text":"Permission Description <code>user.read</code> Read user information <code>user.write</code> Modify user information <code>user.auth</code> Trigger authentication <code>user.session.read</code> Read session data <code>user.session.write</code> Modify session data <code>user.profile.read</code> Read user profile <code>user.profile.write</code> Modify user profile <code>user.biometric</code> Access biometric authentication"},{"location":"concepts/plugins/#audit-permissions-audit","title":"Audit Permissions (<code>audit.*</code>)","text":"Permission Description <code>audit.read</code> Read audit data <code>audit.write</code> Write audit entries <code>audit.log.read</code> Read audit logs <code>audit.log.write</code> Write to audit logs <code>audit.alert.read</code> Read audit alerts <code>audit.report</code> Generate audit reports"},{"location":"concepts/plugins/#database-permissions-db","title":"Database Permissions (<code>db.*</code>)","text":"Permission Description <code>db.read</code> Read internal database <code>db.write</code> Write to internal database <code>db.query</code> Execute database queries <code>db.schema.read</code> Read database schema <code>db.schema.write</code> Modify database schema"},{"location":"concepts/plugins/#ai-ml-permissions-ai","title":"AI / ML Permissions (<code>ai.*</code>)","text":"Permission Description <code>ai.model.load</code> Load ML models <code>ai.predict</code> Run model predictions <code>ai.train</code> Train models"},{"location":"concepts/plugins/#iot-hardware-permissions-iot","title":"IoT / Hardware Permissions (<code>iot.*</code>)","text":"Permission Description <code>iot.scan</code> Scan for IoT devices <code>iot.connect</code> Connect to IoT devices <code>iot.control</code> Control IoT devices"},{"location":"concepts/plugins/#cloud-permissions-cloud","title":"Cloud Permissions (<code>cloud.*</code>)","text":"Permission Description <code>cloud.sync</code> Trigger cloud sync <code>cloud.backup</code> Create cloud backups <code>cloud.restore</code> Restore from cloud <code>cloud.config.read</code> Read cloud configuration <code>cloud.config.write</code> Modify cloud configuration"},{"location":"concepts/plugins/#wildcard-permissions","title":"Wildcard Permissions","text":"<p>Plugins can request wildcard access to entire categories using the <code>.*</code> suffix:</p> <pre><code>\"permissions_required\": [\"vault.*\", \"network.*\", \"system.*\"]\n</code></pre> <p>This grants all permissions within the specified category. Use with caution \u2014 prefer specific permissions when possible.</p>"},{"location":"concepts/plugins/#runtime-overrides","title":"Runtime Overrides","text":"<p>Users can override plugin permissions using the interactive <code>pm plugins access</code> command \u2014 selectively enabling or disabling specific permissions for installed plugins. Overrides are stored in the encrypted vault and travel with it across devices via cloud sync.</p>"},{"location":"concepts/plugins/#step-executor","title":"Step Executor","text":"<p>The step executor runs plugin command pipelines. Each command is defined as a sequence of steps:</p> Step Type Description <code>vault.read</code> Read entries from the vault <code>vault.write</code> Write entries to the vault <code>vault.delete</code> Delete entries <code>file.read</code> Read a file from the filesystem <code>file.write</code> Write a file <code>exec</code> Execute a system command <code>http</code> Make an HTTP request <code>crypto.hash</code> Compute a hash <code>crypto.encrypt</code> Encrypt data <code>set</code> Set a variable <code>print</code> Output text <code>prompt</code> Ask the user for input <p>Steps support variable substitution \u2014 output from one step can feed into subsequent steps using <code>${variable_name}</code> syntax.</p>"},{"location":"concepts/plugins/#hook-system","title":"Hook System","text":"<p>Plugins can register hooks that fire during vault lifecycle events:</p> <pre><code>sequenceDiagram\n    participant User\n    participant APM\n    participant Plugin\n\n    User-&gt;&gt;APM: pm add (new entry)\n    APM-&gt;&gt;APM: Validate and create entry\n    APM-&gt;&gt;Plugin: Fire \"post:add\" hooks\n    Plugin-&gt;&gt;Plugin: Execute registered steps\n    Plugin--&gt;&gt;APM: Hook complete\n    APM--&gt;&gt;User: Entry added</code></pre> <p>Hooks are called in order of plugin registration. If a <code>pre:</code> hook fails, the operation is aborted.</p>"},{"location":"concepts/plugins/#marketplace","title":"Marketplace","text":"<p>Plugins are distributed via the cloud-backed marketplace:</p> <pre><code>pm plugins market          # Browse all available plugins\npm plugins search &lt;query&gt;  # Search the marketplace\npm plugins install &lt;name&gt;  # Install from marketplace\npm plugins push &lt;name&gt;     # Publish to marketplace\n</code></pre> <p>The marketplace stores plugin bundles on the same cloud providers used for vault sync.</p>"},{"location":"concepts/plugins/#next-steps","title":"Next Steps","text":"<ul> <li>Plugin API Reference \u2014 Full permission catalog and step commands</li> <li>Using Plugins Guide \u2014 Installation and management</li> </ul>"},{"location":"concepts/policy-engine/","title":"Policy Engine","text":"<p>APM includes a YAML-based policy engine that enforces security standards for password complexity, rotation schedules, and classification levels. Policies are loaded from files and applied automatically during entry creation and modification.</p>"},{"location":"concepts/policy-engine/#overview","title":"Overview","text":"<p>The policy engine consists of three components:</p> <pre><code>graph LR\n    A[YAML Policy Files] --&gt; B[Policy Loader]\n    B --&gt; C[Policy Engine]\n    C --&gt; D[Password Validation]\n    C --&gt; E[Rotation Enforcement]\n    C --&gt; F[Classification Mapping]</code></pre>"},{"location":"concepts/policy-engine/#policy-file-format","title":"Policy File Format","text":"<p>Policies are defined in YAML files:</p> <pre><code>name: corporate-standard\ndescription: Standard corporate password policy\n\npassword_policy:\n  min_length: 14\n  require_uppercase: true\n  require_numbers: true\n  require_symbols: true\n\nrotation_policy:\n  rotate_every_days: 90\n  notify_before_days: 14\n\nclassification:\n  production_db: critical\n  staging_api: elevated\n  dev_secrets: normal\n</code></pre>"},{"location":"concepts/policy-engine/#password-policy","title":"Password Policy","text":"<p>The <code>password_policy</code> section enforces complexity requirements:</p> Field Type Default Description <code>min_length</code> <code>int</code> 8 Minimum password length <code>require_uppercase</code> <code>bool</code> false Must contain uppercase letter <code>require_numbers</code> <code>bool</code> false Must contain digit <code>require_symbols</code> <code>bool</code> false Must contain special character <p>When a policy is active, APM validates new passwords during <code>pm add</code> and during entry edits performed from the interactive <code>pm get</code> browser:</p> <ul> <li>Pass \u2014 Password meets all requirements</li> <li>Fail \u2014 APM shows which requirements are not met and asks for a new password</li> </ul>"},{"location":"concepts/policy-engine/#rotation-policy","title":"Rotation Policy","text":"<p>The <code>rotation_policy</code> section enforces credential rotation schedules:</p> Field Type Default Description <code>rotate_every_days</code> <code>int</code> 0 Days between required rotations <code>notify_before_days</code> <code>int</code> 0 Days before due date to notify <p>When rotation is enforced:</p> <ul> <li><code>pm health</code> includes rotation status in the health score</li> <li><code>pm trust</code> penalizes secrets that haven't been rotated on schedule</li> <li>APM can display warnings for secrets approaching their rotation date</li> </ul>"},{"location":"concepts/policy-engine/#classification","title":"Classification","text":"<p>The <code>classification</code> section maps entry names to privilege levels:</p> <pre><code>classification:\n  aws_root: critical\n  prod_database: critical\n  staging_api: elevated\n  dev_token: normal\n</code></pre> <p>Classification levels affect trust scoring:</p> Level Trust Penalty Description <code>critical</code> \u221215 Highest-privilege credentials <code>root</code> \u221215 Root-level access <code>admin</code> \u221212 Administrative access <code>elevated</code> \u22128 Above-normal access <code>normal</code> 0 Standard credentials"},{"location":"concepts/policy-engine/#loading-policies","title":"Loading Policies","text":"<pre><code>pm policy load ./policies/\n</code></pre> <p>Loads all YAML files from the specified directory. APM merges multiple policy files, with later files overriding earlier ones for conflicting keys.</p> <p>No Persistent Storage</p> <p>Policies are loaded per-session. They are not stored in the vault. To apply policies consistently, load them at the start of each session or automate via scripting.</p>"},{"location":"concepts/policy-engine/#example-policy-files","title":"Example Policy Files","text":""},{"location":"concepts/policy-engine/#enterprise-standard","title":"Enterprise Standard","text":"<pre><code>name: enterprise-standard\npassword_policy:\n  min_length: 16\n  require_uppercase: true\n  require_numbers: true\n  require_symbols: true\nrotation_policy:\n  rotate_every_days: 60\n  notify_before_days: 14\nclassification:\n  aws_root: critical\n  production_db: critical\n  staging_key: elevated\n</code></pre>"},{"location":"concepts/policy-engine/#developer-team","title":"Developer Team","text":"<pre><code>name: dev-team\npassword_policy:\n  min_length: 12\n  require_uppercase: true\n  require_numbers: true\n  require_symbols: false\nrotation_policy:\n  rotate_every_days: 180\n  notify_before_days: 30\n</code></pre>"},{"location":"concepts/policy-engine/#next-steps","title":"Next Steps","text":"<ul> <li>Policies Reference \u2014 Full YAML schema specification</li> <li>Trust Scoring \u2014 How classification affects trust</li> </ul>"},{"location":"concepts/recovery/","title":"Recovery","text":"<p>The current <code>pm</code> implementation supports recovery as a set of separate mechanisms layered around the v4 vault header. Recovery metadata is stored in the vault record outside the main encrypted payload so APM can verify recovery state before normal unlock succeeds.</p>"},{"location":"concepts/recovery/#recovery-components-in-the-code","title":"Recovery components in the code","text":""},{"location":"concepts/recovery/#recovery-email","title":"Recovery email","text":"<p><code>pm auth email &lt;address&gt;</code> verifies an email address through SMTP and then stores recovery state in the vault.</p> <p>What happens in the current flow:</p> <ol> <li>A verification code is emailed.</li> <li>You confirm the code in the terminal.</li> <li>A recovery key is generated.</li> <li>The recovery key is shown once.</li> <li>Recovery metadata is saved with the vault.</li> </ol>"},{"location":"concepts/recovery/#recovery-key","title":"Recovery key","text":"<p>The recovery key is the main cryptographic recovery factor used by <code>pm auth recover</code>.</p> <p>Important behavior:</p> <ul> <li>it is generated in a human-readable grouped format</li> <li>it is displayed once during recovery setup</li> <li>it is used to unwrap the vault DEK during the recovery flow</li> </ul>"},{"location":"concepts/recovery/#additional-recovery-factors","title":"Additional recovery factors","text":"<p>The current code also supports:</p> <ul> <li>one-time recovery codes through <code>pm auth codes generate</code></li> <li>passkey registration and verification through <code>pm auth passkey ...</code></li> <li>trustee quorum shares through <code>pm auth quorum-setup</code> and <code>pm auth quorum-recover</code></li> </ul> <p>If both passkeys and recovery codes are configured, the recovery flow lets the user choose which second factor to use.</p>"},{"location":"concepts/recovery/#commands","title":"Commands","text":"<pre><code>pm auth email you@example.com\npm auth recover\npm auth codes generate\npm auth codes status\npm auth passkey register\npm auth passkey verify\npm auth passkey disable\npm auth quorum-setup\npm auth quorum-recover\npm auth alerts --enable\npm auth level 2\n</code></pre>"},{"location":"concepts/recovery/#security-levels-and-alerts","title":"Security levels and alerts","text":"<p>The current code exposes:</p> <ul> <li><code>pm auth level [1-3]</code></li> <li><code>pm auth alerts --enable</code></li> <li><code>pm auth alerts --disable</code></li> </ul> <p>Alert level controls gate which alert events are written. Recovery-related email verification uses SMTP during setup and recovery, but general alert delivery in the current implementation is logged locally rather than sent as email.</p>"},{"location":"concepts/recovery/#what-pm-auth-recover-actually-does","title":"What <code>pm auth recover</code> actually does","text":"<p>The implemented recovery flow in <code>main.go</code> is:</p> <ol> <li>verify the recovery email identity by hash match</li> <li>send a six-digit email code</li> <li>verify the recovery key</li> <li>optionally require a passkey or one-time recovery code</li> <li>decrypt the vault with the recovered DEK</li> <li>prompt for a new master password</li> <li>re-encrypt and save the vault</li> </ol> <p>Quorum recovery is a separate path that reconstructs the recovery key from trustee shares before the vault is re-encrypted under a new master password.</p>"},{"location":"concepts/recovery/#accuracy-notes","title":"Accuracy notes","text":"<ul> <li>Recovery metadata is available from the vault header in v4.</li> <li>The alert email address is stored as part of recovery metadata.</li> <li>One-time recovery codes are tracked for used and unused state.</li> <li>Recovery configuration is optional and must be set up explicitly.</li> </ul>"},{"location":"concepts/secret-types/","title":"Secret Types","text":"<p>The personal <code>pm</code> vault currently supports 25 entry types. Every type carries an optional <code>space</code> field so entries can be grouped inside the same vault.</p>"},{"location":"concepts/secret-types/#personal-entry-types","title":"Personal entry types","text":"Type Key fields Password <code>account</code>, <code>username</code>, <code>password</code> TOTP <code>account</code>, <code>secret</code> Token <code>name</code>, <code>token</code>, <code>type</code> Secure note <code>name</code>, <code>content</code> API key <code>name</code>, <code>service</code>, <code>key</code> SSH key <code>name</code>, <code>private_key</code> Wi-Fi <code>ssid</code>, <code>password</code>, <code>security_type</code>, <code>router_ip</code> Recovery codes <code>service</code>, <code>codes[]</code> Certificate <code>label</code>, <code>cert_data</code>, <code>private_key</code>, <code>issuer</code>, <code>expiry</code> Banking item <code>label</code>, <code>type</code>, <code>details</code>, <code>cvv</code>, <code>expiry</code> Document <code>name</code>, <code>file_name</code>, <code>content</code>, <code>password</code>, <code>tags</code>, <code>expiry</code> Government ID <code>type</code>, <code>id_number</code>, <code>name</code>, <code>expiry</code> Medical record <code>label</code>, <code>insurance_id</code>, <code>prescriptions</code>, <code>allergies</code> Travel document <code>label</code>, <code>ticket_number</code>, <code>booking_code</code>, <code>loyalty_program</code> Contact <code>name</code>, <code>phone</code>, <code>email</code>, <code>address</code>, <code>emergency</code> Cloud credential <code>label</code>, <code>access_key</code>, <code>secret_key</code>, <code>region</code>, <code>account_id</code>, <code>role</code>, <code>expiration</code> Kubernetes secret <code>name</code>, <code>cluster_url</code>, <code>namespace</code>, <code>expiration</code> Docker registry <code>name</code>, <code>registry_url</code>, <code>username</code>, <code>token</code> SSH config <code>alias</code>, <code>host</code>, <code>user</code>, <code>port</code>, <code>key_path</code>, <code>private_key</code>, <code>fingerprint</code> CI/CD secret <code>name</code>, <code>webhook</code>, <code>env_vars</code> Software license <code>product_name</code>, <code>serial_key</code>, <code>activation_info</code>, <code>expiration</code> Legal contract <code>name</code>, <code>summary</code>, <code>parties_involved</code>, <code>signed_date</code> Audio <code>name</code>, <code>file_name</code>, <code>content</code> Video <code>name</code>, <code>file_name</code>, <code>content</code> Photo <code>name</code>, <code>file_name</code>, <code>content</code>"},{"location":"concepts/secret-types/#practical-grouping-in-pm-add","title":"Practical grouping in <code>pm add</code>","text":"<p>The interactive add flow groups these types into:</p> <ul> <li>Identity and Personal</li> <li>Developer and Infrastructure</li> <li>Media and Files</li> <li>Finance and Legal</li> </ul> <p>You can either choose from the menu or call <code>pm add &lt;type&gt;</code> directly with aliases.</p>"},{"location":"concepts/secret-types/#team-edition-difference","title":"Team edition difference","text":"<p><code>pm-team add</code> supports a similar but smaller shared set. The team binary currently offers 22 shared entry types and does not include the media entry types present in the personal vault.</p>"},{"location":"concepts/secret-types/#notes","title":"Notes","text":"<ul> <li>Binary-bearing entries such as documents, audio, video, and photos are stored inside the vault.</li> <li>Trust scoring, history, search, and display behavior vary by entry type.</li> <li>Some docs from older versions only listed 22 types; the current personal CLI supports 25.</li> </ul>"},{"location":"concepts/security-profiles/","title":"Security Profiles","text":"<p>APM supports four built-in encryption profiles that control key derivation costs and default nonce sizing. Each profile represents a trade-off between performance and brute-force resistance. Profiles also carry the AEAD cipher used to encrypt the vault payload.</p>"},{"location":"concepts/security-profiles/#profiles-comparison","title":"Profiles Comparison","text":"Parameter Standard Hardened Paranoid Legacy Algorithm Argon2id Argon2id Argon2id PBKDF2-SHA256 Cipher AES-GCM AES-GCM AES-GCM AES-GCM Memory 64 MB 256 MB 512 MB N/A Iterations 3 5 6 600,000 Parallelism 2 4 4 1 Key Output 96 bytes 96 bytes 96 bytes 96 bytes Salt Size 16 bytes 32 bytes 32 bytes 16 bytes Nonce Size 12 bytes 12 bytes 24 bytes 12 bytes Min RAM Any \u22658 GB \u226516 GB Any Min Cores Any \u22654 \u22658 Any"},{"location":"concepts/security-profiles/#profile-details","title":"Profile Details","text":""},{"location":"concepts/security-profiles/#standard","title":"Standard","text":"<p>The default profile suitable for most machines. It uses moderate Argon2id parameters that resist commodity GPU attacks while keeping unlock times under 200ms.</p> <p>Recommended For</p> <p>Personal laptops, desktops, and most workstations.</p>"},{"location":"concepts/security-profiles/#hardened","title":"Hardened","text":"<p>Doubles the memory cost and adds parallelism for machines with \u22658 GB RAM and \u22654 CPU cores. Makes GPU/FPGA attacks significantly more expensive.</p> <p>Recommended For</p> <p>Developer workstations, servers used for credential management.</p>"},{"location":"concepts/security-profiles/#paranoid","title":"Paranoid","text":"<p>Maximum security parameters for high-value vaults on powerful machines. The 512 MB memory cost makes ASIC attacks impractical.</p> <p>Recommended For</p> <p>Infrastructure servers, DevOps teams managing production credentials, and users with high-value secrets (SSH root keys, cloud admin credentials).</p>"},{"location":"concepts/security-profiles/#legacy","title":"Legacy","text":"<p>Uses PBKDF2-SHA256 instead of Argon2id for backward compatibility with older APM vault formats. This profile is not recommended for new vaults.</p> <p>Warning</p> <p>PBKDF2 is significantly weaker against GPU attacks compared to Argon2id. Use this only if you need to interoperate with pre-V3 vaults.</p>"},{"location":"concepts/security-profiles/#auto-detection-and-recommendation","title":"Auto-Detection and Recommendation","text":"<p>During <code>pm setup</code>, APM probes your system hardware and recommends the optimal profile:</p> <pre><code>graph TD\n    A[Detect CPU cores and RAM] --&gt; B{\u226516 GB RAM AND \u22658 cores?}\n    B --&gt;|Yes| C[Recommend Paranoid]\n    B --&gt;|No| D{\u22658 GB RAM AND \u22654 cores?}\n    D --&gt;|Yes| E[Recommend Hardened]\n    D --&gt;|No| F[Recommend Standard]</code></pre> <p>The recommendation is a suggestion only \u2014 you can choose any profile regardless of hardware.</p>"},{"location":"concepts/security-profiles/#changing-profiles","title":"Changing Profiles","text":"<p>APM exposes several profile commands:</p> <pre><code>pm profile list\npm profile current\npm profile set hardened\npm profile edit\npm profile create myprofile\n</code></pre> <p>These flows let you:</p> <ol> <li>Inspect the active profile and built-in options</li> <li>Switch to a built-in profile</li> <li>Edit or create a custom profile</li> <li>Re-encrypt the vault with the updated parameters</li> </ol> <p>Re-encryption Required</p> <p>Changing profiles requires re-encrypting the entire vault. You'll need to enter your master password to complete the operation.</p>"},{"location":"concepts/security-profiles/#custom-profiles","title":"Custom Profiles","text":"<p>APM stores the current profile parameters in the vault header and keeps the active selection in the vault data. Custom profiles can change:</p> <ul> <li>KDF cost values</li> <li>Salt length</li> <li>Nonce length</li> <li>Encryption method (<code>aes-gcm</code> or <code>xchacha20-poly1305</code>)</li> </ul>"},{"location":"concepts/security-profiles/#viewing-current-profile","title":"Viewing Current Profile","text":"<pre><code>pm cinfo\n</code></pre> <p>Displays:</p> <ul> <li>Active profile name</li> <li>Active cipher</li> <li>Argon2id memory, time, and parallelism values</li> <li>Nonce size</li> <li>Vault format version</li> </ul>"},{"location":"concepts/security-profiles/#next-steps","title":"Next Steps","text":"<ul> <li>Encryption \u2014 How profiles feed into key derivation</li> <li>Vault Format \u2014 Where the profile metadata is stored in the file</li> </ul>"},{"location":"concepts/sessions/","title":"Sessions","text":"<p>Sessions in APM are time-bounded unlock windows that control when decrypted vault data is accessible. They form the primary security boundary between encrypted-at-rest vault storage and in-memory access.</p>"},{"location":"concepts/sessions/#session-model","title":"Session Model","text":"<pre><code>graph TB\n    subgraph \"Shell-Scoped Sessions\"\n        A[pm unlock] --&gt; B[Session File Created]\n        B --&gt; C{Check on every command}\n        C --&gt;|Valid| D[Execute Command]\n        C --&gt;|Expired| E[Reject \u2014 re-unlock needed]\n        C --&gt;|Inactive| F[Auto-lock]\n    end\n\n    subgraph \"Ephemeral Sessions\"\n        G[pm session issue] --&gt; H[Ephemeral Token Created]\n        H --&gt; I{Validate bindings}\n        I --&gt;|Match| J[Delegated Access]\n        I --&gt;|Mismatch| K[Reject]\n    end</code></pre>"},{"location":"concepts/sessions/#shell-scoped-sessions","title":"Shell-Scoped Sessions","text":""},{"location":"concepts/sessions/#how-they-work","title":"How They Work","text":"<ol> <li> <p><code>pm unlock</code> decrypts the vault and creates a session file containing:</p> <ul> <li>Session ID</li> <li>Unlock timestamp</li> <li>Expiry timestamp</li> <li>Inactivity timeout</li> <li>Read-only flag</li> <li>Hashed master password (for re-verification on sensitive operations)</li> </ul> </li> <li> <p>Every sensitive command checks the session file before proceeding</p> </li> <li>Sessions auto-expire after the configured duration or inactivity timeout</li> <li><code>pm lock</code> immediately destroys the session file and wipes memory</li> </ol>"},{"location":"concepts/sessions/#session-scoping","title":"Session Scoping","text":"<p>By default, APM uses a single global session file. To run independent sessions per terminal:</p> <pre><code>export APM_SESSION_ID=\"terminal_1\"\npm unlock  # Creates session file specific to this shell\n</code></pre> <p>Different values of <code>APM_SESSION_ID</code> produce separate session files, so multiple terminals can have independent lock/unlock states.</p>"},{"location":"concepts/sessions/#session-file-location","title":"Session File Location","text":"<pre><code>$TEMP/pm_session_global.json          # Global (no APM_SESSION_ID)\n$TEMP/pm_session_{SESSION_ID}.json    # Scoped\n</code></pre>"},{"location":"concepts/sessions/#session-parameters","title":"Session Parameters","text":"Parameter Default Description Session duration 30 min Maximum session lifetime Inactivity timeout 10 min Auto-lock after idle time Read-only mode false Prevents all write operations"},{"location":"concepts/sessions/#ephemeral-delegated-sessions","title":"Ephemeral Delegated Sessions","text":"<p>Ephemeral sessions are short-lived tokens for delegating vault access to external processes without sharing the master password.</p>"},{"location":"concepts/sessions/#use-cases","title":"Use Cases","text":"<ul> <li>CI/CD pipelines \u2014 Grant temporary read access for credential retrieval</li> <li>MCP server \u2014 Provide an AI agent with scoped vault access</li> <li>Pair programming \u2014 Share temporary access with a teammate</li> </ul>"},{"location":"concepts/sessions/#issuing","title":"Issuing","text":"<pre><code>pm session issue \\\n  --label \"CI Build\" \\\n  --scope read \\\n  --ttl 15m \\\n  --bind-host \\\n  --bind-pid 12345 \\\n  --agent \"github-actions\"\n</code></pre>"},{"location":"concepts/sessions/#security-bindings","title":"Security Bindings","text":"<p>Ephemeral sessions support three binding types:</p> Binding Verification Host SHA-256(hostname + username) must match PID Calling process ID must match the bound PID Agent <code>APM_ACTOR</code> env variable must match the bound agent <p>If any bound property doesn't match at validation time, the session is rejected.</p>"},{"location":"concepts/sessions/#lifecycle","title":"Lifecycle","text":"<pre><code>sequenceDiagram\n    participant Admin\n    participant APM\n    participant Delegate\n\n    Admin-&gt;&gt;APM: pm session issue --ttl 15m\n    APM--&gt;&gt;Admin: eps_TOKEN_ID\n    Admin-&gt;&gt;Delegate: Pass TOKEN_ID\n    Delegate-&gt;&gt;APM: APM_EPHEMERAL_ID=eps_TOKEN_ID pm get\n    APM-&gt;&gt;APM: Validate bindings + expiry\n    APM--&gt;&gt;Delegate: Entry data (if valid)\n    Note over APM: After 15 minutes...\n    Delegate-&gt;&gt;APM: APM_EPHEMERAL_ID=eps_TOKEN_ID pm get\n    APM--&gt;&gt;Delegate: Session expired</code></pre>"},{"location":"concepts/sessions/#session-store","title":"Session Store","text":"<p>Ephemeral sessions are stored in a JSON file:</p> <pre><code>$TEMP/.apm_ephemeral_sessions.json\n</code></pre> <p>The store tracks all issued sessions with their bindings, creation times, expiry, and revocation status.</p>"},{"location":"concepts/sessions/#managing-ephemeral-sessions","title":"Managing Ephemeral Sessions","text":"<pre><code>pm session list      # List all active sessions\npm session revoke ID # Revoke a specific session\n</code></pre>"},{"location":"concepts/sessions/#session-interaction-matrix","title":"Session Interaction Matrix","text":"Feature Requires Session Read-Only OK Ephemeral OK <code>pm get</code> \u2705 \u2705 \u2705 <code>pm add</code> \u2705 \u274c \u274c <code>pm get</code> edit/delete actions \u2705 \u274c \u274c <code>pm totp</code> \u2705 \u2705 \u2705 <code>pm cloud sync</code> \u2705 \u274c \u274c <code>pm health</code> \u2705 \u2705 \u2705 <code>pm trust</code> \u2705 \u2705 \u2705 <code>pm audit</code> \u274c \u2705 \u2705 <code>pm info</code> \u274c \u2705 \u2705 Autofill daemon \u2705 \u2705 \u274c MCP server \u2705 Per token \u2705"},{"location":"concepts/sessions/#next-steps","title":"Next Steps","text":"<ul> <li>Sessions Guide \u2014 Practical how-to guide</li> <li>MCP Integration \u2014 Using ephemeral sessions with AI agents</li> </ul>"},{"location":"concepts/vault-format/","title":"Vault Format","text":"<p>APM uses a custom binary format called Vault V4 for storing encrypted data on disk. This page describes the exact byte layout, integrity mechanisms, and backward compatibility.</p>"},{"location":"concepts/vault-format/#file-signature","title":"File Signature","text":"<p>Every APM vault file begins with the ASCII magic bytes:</p> <pre><code>APMVAULT\n</code></pre> <p>This 8-byte signature identifies the file as an APM vault and enables format detection by other tools.</p>"},{"location":"concepts/vault-format/#v4-binary-layout","title":"V4 Binary Layout","text":"<pre><code>APMVAULT\nversion (1 byte)\nprofile_len (2 bytes)\nprofile_json (variable)\nrecovery_len (2 bytes)\nrecovery_json (variable)\nsalt (profile-defined length)\nvalidator (32 bytes)\nnonce (profile-defined length)\nmaster_slot (32 bytes + AEAD tag)\nciphertext (variable)\nhmac_sha256 (32 bytes)\n</code></pre>"},{"location":"concepts/vault-format/#header-fields","title":"Header Fields","text":"Field Size Description Magic 8 bytes Fixed <code>APMVAULT</code> ASCII string Version 1 byte Format version (currently <code>4</code>) Profile JSON Variable Serialized <code>CryptoProfile</code>, including KDF, salt length, nonce length, cipher Recovery JSON Variable Unencrypted recovery metadata used for recovery and alert coordination Salt Variable Random salt for key derivation, sized by the active profile Validator 32 bytes Validator bytes used for fast wrong-password detection Nonce Variable AEAD nonce for the vault payload Master Slot 48 bytes Encrypted DEK slot for V4 vaults (32-byte DEK + AEAD tag) Ciphertext Variable AEAD-encrypted vault payload HMAC 32 bytes HMAC-SHA256 over the entire unsigned payload"},{"location":"concepts/vault-format/#profile-metadata","title":"Profile Metadata","text":"<p>The profile block is JSON, not a numeric profile ID. That allows APM to persist:</p> <ul> <li>KDF selection</li> <li>Time, memory, and parallelism</li> <li>Salt length</li> <li>Nonce length</li> <li>Encryption method (<code>aes-gcm</code> or <code>xchacha20-poly1305</code>)</li> </ul> <p>Older vaults that do not store a cipher are treated as <code>aes-gcm</code> when loaded.</p>"},{"location":"concepts/vault-format/#encrypted-payload","title":"Encrypted Payload","text":"<p>The payload uses the AEAD defined by the profile metadata. The nonce is stored separately before the master slot and ciphertext.</p>"},{"location":"concepts/vault-format/#hmac-signature","title":"HMAC Signature","text":"<p>The final 32 bytes are an HMAC-SHA256 signature computed using the authentication key over:</p> <ul> <li>Version byte</li> <li>Profile JSON block</li> <li>Recovery metadata block</li> <li>Salt</li> <li>Validator bytes</li> <li>Nonce</li> <li>Master slot</li> <li>Entire encrypted payload</li> </ul> <p>This provides pre-decryption tamper detection.</p>"},{"location":"concepts/vault-format/#encrypted-vault-contents","title":"Encrypted Vault Contents","text":"<p>The decrypted JSON payload contains the full <code>Vault</code> struct:</p> <pre><code>{\n  \"entries\": [\n    {\n      \"id\": \"uuid-v4\",\n      \"username\": \"...\",\n      \"password\": \"...\",\n      \"account\": \"...\",\n      \"space\": \"...\",\n      \"entry_type\": \"password\",\n      \"custom_fields\": {},\n      \"created_at\": \"2026-01-15T10:30:00Z\",\n      \"updated_at\": \"2026-01-15T10:30:00Z\"\n    }\n  ],\n  \"totp_entries\": [...],\n  \"totp_order\": [\"entry1\", \"entry2\"],\n  \"notes\": [...],\n  \"api_keys\": [...],\n  \"tokens\": [...],\n  \"ssh_keys\": [...],\n  \"ssh_configs\": [...],\n  \"cloud_credentials\": [...],\n  \"k8s_secrets\": [...],\n  \"docker_registries\": [...],\n  \"cicd_secrets\": [...],\n  \"recovery_codes_entries\": [...],\n  \"certificates\": [...],\n  \"banking_entries\": [...],\n  \"documents\": [...],\n  \"software_licenses\": [...],\n  \"legal_contracts\": [...],\n  \"gov_ids\": [...],\n  \"medical_records\": [...],\n  \"travel_entries\": [...],\n  \"contacts\": [...],\n  \"wifi_entries\": [...],\n  \"photos\": [...],\n  \"audio_entries\": [...],\n  \"video_entries\": [...],\n  \"recovery_email\": \"...\",\n  \"recovery_hash\": \"...\",\n  \"recovery_salt\": \"...\",\n  \"alerts_enabled\": true,\n  \"alert_email\": \"...\",\n  \"security_level\": 2,\n  \"current_space\": \"default\",\n  \"spaces\": [\"default\", \"Work\", \"Personal\"],\n  \"vocab_compressed\": \"base64...\",\n  \"cloud_config\": {...},\n  \"mcp_tokens\": [...],\n  \"plugin_permission_overrides\": {...},\n  \"secret_telemetry\": {...},\n  \"history\": [...],\n  \"current_profile_params\": {...}\n}\n</code></pre>"},{"location":"concepts/vault-format/#version-history","title":"Version History","text":"Version Changes V1 Simple AES-CBC encryption V2 Added HMAC integrity V3 Migrated to AES-GCM, added Argon2id profiles V4 Added <code>APMVAULT</code> magic, JSON profile metadata, recovery metadata, DEK slot"},{"location":"concepts/vault-format/#backward-compatibility","title":"Backward Compatibility","text":"<p>APM detects older vault formats automatically:</p> <ol> <li>V4 \u2014 Full <code>APMVAULT</code> header with magic bytes</li> <li>Pre-V4 \u2014 Falls back to legacy decryption (<code>decryptOldVault</code>) which uses hardcoded Argon2 parameters</li> </ol> <p>Legacy Vaults</p> <p>Legacy vaults (pre-V4) are supported for reading but cannot receive new features like recovery, trust scoring, or vocabulary. You should re-encrypt with <code>pm profile</code> to upgrade to V4.</p>"},{"location":"concepts/vault-format/#file-locations","title":"File Locations","text":"Platform Default Vault Path All <code>./vault.dat</code> (current directory) Override <code>APM_VAULT_PATH</code> env variable"},{"location":"concepts/vault-format/#next-steps","title":"Next Steps","text":"<ul> <li>Encryption \u2014 How keys are derived and data is encrypted</li> <li>Secret Types \u2014 All entry type schemas</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Welcome to APM. This section covers everything you need to go from zero to a fully operational encrypted vault.</p>"},{"location":"getting-started/#in-this-section","title":"In This Section","text":"<ul> <li> <p>Installation</p> <p>Install APM on macOS, Linux, or Windows \u2014 via one-line scripts, pre-built binaries, or building from source.</p> </li> <li> <p>First Steps</p> <p>Initialize your vault, add your first entries, search for credentials, generate passwords, and learn the session lifecycle.</p> </li> <li> <p>Features</p> <p>A bird's-eye view of every APM capability \u2014 from secret types and cloud sync to plugins and AI integration.</p> </li> <li> <p>Getting Help</p> <p>How to use inline help, report issues, and get support.</p> </li> </ul>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>A terminal (Bash, Zsh, PowerShell, or CMD)</li> <li>Go 1.21+ if building from source</li> <li>Windows, macOS, or Linux</li> </ul>"},{"location":"getting-started/#what-youll-accomplish","title":"What You'll Accomplish","text":"<p>By the end of this section you will have:</p> <ol> <li>Installed the <code>pm</code> binary on your system</li> <li>Created an encrypted vault with a strong master password</li> <li>Added, searched, and retrieved secrets</li> <li>Understood the lock/unlock session model</li> <li>Optionally set up cloud sync and autofill</li> </ol>"},{"location":"getting-started/features/","title":"Features","text":"<p>This page lists the capabilities that are implemented in the current codebase.</p>"},{"location":"getting-started/features/#personal-vault","title":"Personal vault","text":"<ul> <li>Local encrypted vault stored as <code>APMVAULT</code>, current format v4</li> <li>25 structured personal secret types</li> <li>Spaces for compartmentalizing entries inside one vault</li> <li>Interactive search, details, editing, deletion, and TOTP access</li> <li>Password generation, audit logging, trust scoring, and health scoring</li> </ul>"},{"location":"getting-started/features/#crypto-and-profiles","title":"Crypto and profiles","text":"<ul> <li>Built-in profiles: <code>standard</code>, <code>hardened</code>, <code>paranoid</code>, <code>legacy</code></li> <li>Cipher support: <code>aes-gcm</code> and <code>xchacha20-poly1305</code></li> <li>Versioned header metadata for vault crypto parameters</li> <li><code>pm cinfo</code> and <code>pm profile</code> for inspection and tuning</li> </ul>"},{"location":"getting-started/features/#sessions-and-automation","title":"Sessions and automation","text":"<ul> <li>Standard unlock sessions with expiry and inactivity controls</li> <li>Read-only session mode</li> <li>Shell-scoped sessions via <code>APM_SESSION_ID</code></li> <li>Ephemeral delegated sessions via <code>pm session issue</code></li> <li>Shell injection with <code>pm inject</code> and <code>.apminject</code></li> </ul>"},{"location":"getting-started/features/#recovery-and-auth","title":"Recovery and auth","text":"<ul> <li>Recovery email registration with SMTP verification</li> <li>Recovery key generated during setup and shown once</li> <li>One-time recovery codes</li> <li>Recovery passkey registration and verification</li> <li>Quorum share setup and trustee-based recovery</li> <li>Security alert level controls and recovery-related status commands</li> </ul>"},{"location":"getting-started/features/#cloud-sync","title":"Cloud sync","text":"<ul> <li>Google Drive sync</li> <li>GitHub sync</li> <li>Dropbox sync</li> <li>Provider diff and selective merge flow</li> <li>Provider-specific <code>.apmignore</code> filtering before upload</li> </ul>"},{"location":"getting-started/features/#extensibility","title":"Extensibility","text":"<ul> <li>Manifest-based plugins</li> <li>Hook execution around vault actions</li> <li>Plugin marketplace commands</li> <li>Runtime-added plugin commands</li> <li>Built-in MCP server with permission-scoped tokens</li> </ul>"},{"location":"getting-started/features/#desktop-integration","title":"Desktop integration","text":"<ul> <li>Windows autofill daemon</li> <li>Autocomplete popup and domain matching</li> <li>TOTP linking for autofill flows</li> <li>Optional Face ID command tree when built with face-recognition support</li> <li>Bubble Tea TUI entry point through <code>pm tui</code></li> </ul>"},{"location":"getting-started/features/#team-edition","title":"Team edition","text":"<p>The separate <code>pm-team</code> module adds:</p> <ul> <li>organizations</li> <li>departments</li> <li>roles and permission overrides</li> <li>approval workflows</li> <li>shared entry management</li> </ul>"},{"location":"getting-started/first-steps/","title":"First Steps","text":"<p>This walkthrough follows the code paths that exist in the current <code>pm</code> binary.</p>"},{"location":"getting-started/first-steps/#1-build-and-run-setup","title":"1. Build and run setup","text":"<pre><code>go build -o pm .\npm setup\n</code></pre> <p><code>pm setup</code> is the guided onboarding flow for:</p> <ul> <li>creating the vault if one does not exist</li> <li>choosing or changing the active crypto profile</li> <li>selecting the cipher for new vaults</li> <li>creating spaces</li> <li>loading plugins</li> <li>configuring cloud sync</li> </ul> <p>The default vault path is <code>vault.dat</code> beside the binary unless <code>APM_VAULT_PATH</code> is set.</p>"},{"location":"getting-started/first-steps/#2-choose-a-profile","title":"2. Choose a profile","text":"<p>The built-in profiles in the code today are:</p> Profile KDF metadata Memory Time Parallelism Default nonce <code>standard</code> <code>argon2id</code> 64 MB 3 2 12 <code>hardened</code> <code>argon2id</code> 256 MB 5 4 12 <code>paranoid</code> <code>argon2id</code> 512 MB 6 4 24 <code>legacy</code> <code>pbkdf2</code> metadata 0 600000 1 12 <p>The setup flow can also switch between <code>aes-gcm</code> and <code>xchacha20-poly1305</code>.</p>"},{"location":"getting-started/first-steps/#3-unlock-the-vault","title":"3. Unlock the vault","text":"<pre><code>pm unlock\n</code></pre> <p>Unlock creates a session with:</p> <ul> <li>a session duration</li> <li>an inactivity timeout</li> <li>optional read-only access</li> </ul> <p>Normal sessions are stored in temp files and encrypted with a per-user key in the config directory. If you set <code>APM_SESSION_ID</code>, you can keep multiple shell-scoped sessions.</p>"},{"location":"getting-started/first-steps/#4-add-an-entry","title":"4. Add an entry","text":"<pre><code>pm add\n</code></pre> <p>The current interactive add flow supports 25 entry types:</p> <ul> <li>Password</li> <li>TOTP</li> <li>Token</li> <li>Secure note</li> <li>API key</li> <li>SSH key</li> <li>Wi-Fi</li> <li>Recovery codes</li> <li>Certificate</li> <li>Banking item</li> <li>Document</li> <li>Government ID</li> <li>Medical record</li> <li>Travel document</li> <li>Contact</li> <li>Cloud credential</li> <li>Kubernetes secret</li> <li>Docker registry</li> <li>SSH config</li> <li>CI/CD secret</li> <li>Software license</li> <li>Legal contract</li> <li>Audio</li> <li>Video</li> <li>Photo</li> </ul> <p>You can also call <code>pm add &lt;type&gt;</code> directly with aliases such as <code>password</code>, <code>totp</code>, <code>note</code>, <code>apikey</code>, <code>sshkey</code>, <code>cloudcredentials</code>, <code>kubernetes</code>, or <code>photo</code>.</p>"},{"location":"getting-started/first-steps/#5-search-and-inspect","title":"5. Search and inspect","text":"<pre><code>pm get github\npm totp\npm gen\n</code></pre> <p>Useful early commands:</p> <ul> <li><code>pm get [query]</code> for interactive search and entry actions</li> <li><code>pm totp [entry_name]</code> for one-off TOTP retrieval</li> <li><code>pm gen</code> for password generation</li> <li><code>pm cinfo</code> for vault crypto parameters</li> <li><code>pm loaded</code> to inspect loaded plugins, policies, and <code>.apmignore</code></li> </ul>"},{"location":"getting-started/first-steps/#6-lock-when-done","title":"6. Lock when done","text":"<pre><code>pm lock\n</code></pre> <p><code>pm lock</code> removes the active unlock session and also tries to lock the autofill daemon if it is running.</p>"},{"location":"getting-started/first-steps/#7-optional-next-steps","title":"7. Optional next steps","text":""},{"location":"getting-started/first-steps/#cloud-sync","title":"Cloud sync","text":"<pre><code>pm cloud init\npm cloud sync\n</code></pre> <p>Supported providers:</p> <ul> <li>Google Drive</li> <li>GitHub</li> <li>Dropbox</li> </ul> <p>Google Drive and Dropbox each support <code>APM_PUBLIC</code> and <code>self_hosted</code> modes. GitHub uses a token plus <code>owner/repo</code>.</p>"},{"location":"getting-started/first-steps/#delegated-sessions","title":"Delegated sessions","text":"<pre><code>pm session issue\npm session list\npm session revoke &lt;id&gt;\n</code></pre> <p>Ephemeral sessions are useful for AI tools and automation. They can be bound to host, PID, and agent name.</p>"},{"location":"getting-started/first-steps/#recovery","title":"Recovery","text":"<pre><code>pm auth email you@example.com\npm auth codes generate\npm auth quorum-setup\n</code></pre> <p>Registering a recovery email also generates and displays the vault recovery key once.</p>"},{"location":"getting-started/first-steps/#windows-autofill","title":"Windows autofill","text":"<pre><code>pm autocomplete enable\npm autofill start\n</code></pre> <p>The Windows daemon uses a global hotkey and loopback-only local IPC.</p>"},{"location":"getting-started/help/","title":"Getting Help","text":"<p>APM provides multiple ways to get assistance, from built-in documentation to community support.</p>"},{"location":"getting-started/help/#inline-help","title":"Inline Help","text":"<p>Every command and subcommand supports the <code>--help</code> flag:</p> <pre><code># Top-level help\npm --help\n\n# Command-specific help\npm add --help\npm cloud --help\npm cloud init --help\npm mcp --help\npm auth --help\npm plugins --help\n</code></pre> <p>This displays usage syntax, available flags, and a brief description of each command.</p>"},{"location":"getting-started/help/#system-information","title":"System Information","text":"<p>View your current APM installation details:</p> <pre><code>pm info\n</code></pre> <p>This displays:</p> <ul> <li>APM version</li> <li>Go version and build info</li> <li>OS and architecture</li> <li>Vault file path</li> <li>Install directory</li> </ul> <p>For cryptographic parameters of your vault:</p> <pre><code>pm cinfo\n</code></pre> <p>This shows:</p> <ul> <li>Active encryption profile</li> <li>Active cipher</li> <li>Argon2id parameters (memory, time, parallelism)</li> <li>Nonce size</li> <li>Vault format version</li> </ul>"},{"location":"getting-started/help/#reporting-issues","title":"Reporting Issues","text":"<p>If you encounter a bug or have a feature request:</p> <ol> <li>Check existing issues: github.com/aaravmaloo/apm/issues</li> <li>Open a new issue with:<ul> <li>APM version (<code>pm info</code>)</li> <li>OS and architecture</li> <li>Steps to reproduce</li> <li>Expected vs. actual behavior</li> </ul> </li> </ol> <p>Security Vulnerabilities</p> <p>If you discover a security vulnerability, do not open a public issue. Instead, email aaravmaloo06@gmail.com directly with details of the vulnerability.</p>"},{"location":"getting-started/help/#contact","title":"Contact","text":"<ul> <li>Primary Maintainer: Aarav Maloo</li> <li>Security Alerts: aaravmaloo06@gmail.com</li> <li>GitHub: aaravmaloo/apm</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>APM can be installed via one-line scripts, pre-built binaries, or by building from source.</p>"},{"location":"getting-started/installation/#one-line-install-recommended","title":"One-Line Install (Recommended)","text":"macOS / LinuxWindows PowerShell <pre><code>curl -sSL https://raw.githubusercontent.com/aaravmaloo/apm/master/scripts/install.sh | bash\n</code></pre> <p>Install layout:</p> Platform Binary location Symlink macOS <code>/usr/local/opt/apm/apm</code> <code>/usr/local/bin/apm \u2192 /usr/local/opt/apm/apm</code> Linux <code>/opt/apm/apm</code> <code>/usr/local/bin/apm \u2192 /opt/apm/apm</code> <pre><code>Set-ExecutionPolicy Bypass -Scope Process -Force\niwr https://raw.githubusercontent.com/aaravmaloo/apm/master/scripts/install.ps1 -UseBasicParsing | iex\n</code></pre> <p>Install layout:</p> <ul> <li>Binary: <code>%LOCALAPPDATA%\\pm\\apm.exe</code></li> <li>The installer appends the directory to your user <code>PATH</code> if not already present</li> </ul> <p>After installation, verify with:</p> <pre><code>pm info\n</code></pre>"},{"location":"getting-started/installation/#pre-built-binaries","title":"Pre-built Binaries","text":"<p>Pre-built binaries for Windows, macOS, and Linux are available for each stable release:</p> <ol> <li>Go to the Releases page</li> <li>Download the binary for your OS and architecture</li> <li>Place it in a directory on your <code>PATH</code></li> <li>Verify with <code>pm info</code></li> </ol>"},{"location":"getting-started/installation/#build-from-source","title":"Build from Source","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Go 1.21 or later</li> <li>Git</li> <li>Windows, macOS, or Linux</li> </ul>"},{"location":"getting-started/installation/#steps","title":"Steps","text":"<pre><code># Clone the repository\ngit clone https://github.com/aaravmaloo/apm.git\ncd apm\n\n# Build the binary\ngo build -o pm main.go\n\n# Verify the build\n./pm info\n\n# Run the guided setup flow\n./pm setup\n</code></pre> <p>Tip</p> <p>On Windows, the output binary will be <code>pm.exe</code>. You can move it to a directory on your <code>PATH</code> for global access.</p>"},{"location":"getting-started/installation/#preview-and-beta-builds","title":"Preview and Beta Builds","text":"<p>Pre-built binaries for preview, beta, and canary builds are available from the Builds page.</p> <p>Warning</p> <p>Preview and beta builds may be unstable and could potentially corrupt your vault. These builds are intended for developers and testers only. Do not use them for production vaults.</p> <p>The APM version system follows these stages (e.g., for a v8 release):</p> Stage Tag Stability Canary <code>can-8</code> Experimental Beta <code>beta-8</code> Testing Pre-release <code>pre-8</code> Near-stable Release <code>v8</code> Stable"},{"location":"getting-started/installation/#updating","title":"Updating","text":"<p>APM includes a self-update mechanism:</p> <pre><code>pm update\n</code></pre> <p>This checks for the latest release on GitHub and downloads/replaces the binary automatically. You can also force a check with:</p> <pre><code>pm update --force\n</code></pre>"},{"location":"getting-started/installation/#uninstalling","title":"Uninstalling","text":"macOS / LinuxWindows <pre><code># Remove the binary and symlink\nsudo rm -f /usr/local/bin/apm\nsudo rm -rf /usr/local/opt/apm   # macOS\nsudo rm -rf /opt/apm              # Linux\n\n# Optionally remove config and vault data\nrm -rf ~/.config/apm\n</code></pre> <pre><code># Remove the binary directory\nRemove-Item -Recurse -Force \"$env:LOCALAPPDATA\\pm\"\n\n# Remove from PATH (manual step in System Properties \u2192 Environment Variables)\n\n# Optionally remove config data\nRemove-Item -Recurse -Force \"$env:APPDATA\\apm\"\n</code></pre>"},{"location":"getting-started/installation/#next-steps","title":"Next Steps","text":"<p>Once installed, proceed to First Steps to run setup and start managing secrets.</p>"},{"location":"guides/","title":"Guides","text":"<p>These guides focus on workflows that are actually present in the current binaries.</p>"},{"location":"guides/#personal-vault-workflows","title":"Personal vault workflows","text":"<ul> <li>Managing your vault</li> <li>Importing and exporting</li> <li>Managing sessions</li> <li>Generating TOTP codes</li> <li>Injecting secrets into your shell</li> </ul>"},{"location":"guides/#sync-and-integration","title":"Sync and integration","text":"<ul> <li>Cloud synchronization</li> <li>Using <code>.apmignore</code></li> <li>MCP integration</li> <li>Autofill on Windows</li> <li>Face ID</li> </ul>"},{"location":"guides/#extension-and-organization","title":"Extension and organization","text":"<ul> <li>Using plugins</li> <li>Team edition</li> </ul>"},{"location":"guides/#notes-on-scope","title":"Notes on scope","text":"<ul> <li><code>pm</code> is the personal CLI.</li> <li><code>pm-team</code> is a separate binary documented under Team.</li> <li>Plugin manifests can add new runtime commands, so the exact command list may be larger than the built-in docs surface.</li> </ul>"},{"location":"guides/apmignore/","title":"Using .apmignore","text":"<p>The <code>.apmignore</code> file controls what APM excludes from cloud upload payloads. It's processed before encryption and upload during <code>pm cloud sync</code>, allowing you to maintain local-only data while still syncing the rest of your vault.</p>"},{"location":"guides/apmignore/#file-location","title":"File Location","text":"<p>Place <code>.apmignore</code> in one of these locations (searched in order):</p> <ol> <li>Same directory as your vault file (most common)</li> <li>Current working directory when running <code>pm cloud sync</code></li> </ol> <p>APM loads the nearest applicable file.</p>"},{"location":"guides/apmignore/#sections","title":"Sections","text":""},{"location":"guides/apmignore/#spaces-ignore-entire-spaces","title":"<code>[spaces]</code> \u2014 Ignore Entire Spaces","text":"<p>Exclude whole spaces from cloud uploads:</p> <pre><code>[spaces]\nprivate\ntemp\narchive_*\n</code></pre> <p>Supports glob wildcards (<code>*</code> matches any characters).</p>"},{"location":"guides/apmignore/#entries-ignore-specific-entries","title":"<code>[entries]</code> \u2014 Ignore Specific Entries","text":"<p>Exclude entries using <code>space:type:name</code> patterns:</p> <pre><code>[entries]\nwork:password:legacy_admin\n*:notes:*draft*\npersonal:api_key:test_*\n*:ssh_key:*\n</code></pre> Component Description Wildcard Support <code>space</code> Space name <code>*</code> for any <code>type</code> Entry type <code>*</code> for any <code>name</code> Entry name <code>*</code> for any <p>Type Names</p> <p>Use lowercase type identifiers: <code>password</code>, <code>totp</code>, <code>token</code>, <code>notes</code>, <code>api_key</code>, <code>ssh_key</code>, <code>ssh_config</code>, <code>wifi</code>, <code>gov_id</code>, <code>medical</code>, <code>travel</code>, <code>contact</code>, <code>cloud_creds</code>, <code>k8s</code>, <code>docker</code>, <code>cicd</code>, <code>recovery_codes</code>, <code>certificate</code>, <code>banking</code>, <code>document</code>, <code>software_license</code>, <code>legal</code>, <code>photo</code>, <code>audio</code>, <code>video</code>.</p>"},{"location":"guides/apmignore/#cloud-specific-ignore-provider-specific-rules","title":"<code>[cloud-specific-ignore]</code> \u2014 Provider-Specific Rules","text":"<p>Exclude entries only for a specific provider:</p> <pre><code>[cloud-specific-ignore]\ndropbox:work:password:legacy_admin\ngithub:*:ssh_key:*\ngdrive:personal:notes:*journal*\n</code></pre> <p>Format: <code>provider:space:type:name</code></p> <p>Supported providers: <code>gdrive</code>, <code>github</code>, <code>dropbox</code></p>"},{"location":"guides/apmignore/#vocab-ignore-vocabulary-words","title":"<code>[vocab]</code> \u2014 Ignore Vocabulary Words","text":"<p>Exclude specific words from the vocabulary index during cloud exports:</p> <pre><code>[vocab]\ninternal_project_name\nsecret_codename\n</code></pre>"},{"location":"guides/apmignore/#misc-miscellaneous-flags","title":"<code>[misc]</code> \u2014 Miscellaneous Flags","text":"<pre><code>[misc]\nignore:vocab\n</code></pre> Flag Effect <code>ignore:vocab</code> Strip the entire compressed vocabulary from uploads"},{"location":"guides/apmignore/#complete-example","title":"Complete Example","text":"<pre><code># ============================================\n# .apmignore \u2014 APM Cloud Upload Filter\n# ============================================\n\n# Spaces to never upload\n[spaces]\nprivate\ntemp_*\n\n# Entries to exclude from all providers\n[entries]\nwork:password:local_dev_db\n*:notes:*scratch*\npersonal:ssh_key:id_ed25519_local\n\n# Provider-specific exclusions\n[cloud-specific-ignore]\ndropbox:work:password:legacy_admin\ngithub:*:document:*confidential*\n\n# Vocabulary words to exclude\n[vocab]\ninternal_project_alpha\n\n# Strip the entire vocabulary from uploads\n[misc]\nignore:vocab\n</code></pre>"},{"location":"guides/apmignore/#pattern-matching-rules","title":"Pattern Matching Rules","text":"<ul> <li>Patterns are case-insensitive for space and type matching</li> <li>Entry names are case-sensitive</li> <li><code>*</code> matches zero or more characters (glob-style)</li> <li>Comments start with <code>#</code> and are ignored</li> <li>Inline comments after entries are supported (after <code>#</code>)</li> <li>Empty lines are skipped</li> </ul>"},{"location":"guides/apmignore/#how-filtering-works","title":"How Filtering Works","text":"<p>When you run <code>pm cloud sync</code>:</p> <ol> <li>APM loads and parses <code>.apmignore</code></li> <li>A filtered copy of the vault is created in memory</li> <li>Entries matching ignore rules are removed from the copy</li> <li>If <code>ignore:vocab</code> is set, the vocabulary blob is stripped</li> <li>The filtered vault is encrypted and uploaded</li> </ol> <p>Local Vault Unaffected</p> <p>Filtering only affects the upload payload. Your local vault file is never modified by <code>.apmignore</code>. All ignored entries remain safely stored locally.</p>"},{"location":"guides/apmignore/#debugging","title":"Debugging","text":"<p>To verify what your <code>.apmignore</code> is filtering, check the audit log after a sync:</p> <pre><code>pm audit\n</code></pre> <p>Sync events include details about which provider received the upload and how many entries were in the filtered payload.</p>"},{"location":"guides/apmignore/#next-steps","title":"Next Steps","text":"<ul> <li>Cloud Synchronization \u2014 Full sync guide</li> <li>.apmignore Reference \u2014 Format specification</li> </ul>"},{"location":"guides/cloud-sync/","title":"Cloud Synchronization","text":"<p>APM syncs your encrypted vault to cloud providers so you can access credentials across all your trusted devices. Your master password never leaves your machine \u2014 only encrypted vault blobs are uploaded.</p>"},{"location":"guides/cloud-sync/#supported-providers","title":"Supported Providers","text":"Feature Google Drive GitHub Dropbox Authentication OAuth2 (PKCE) Personal Access Token OAuth2 (PKCE) Storage Location Application Data Folder Private Repository Application Folder Setup Speed Instant (browser OAuth) Manual (token + repo) Instant (browser OAuth) Privacy High (isolated from files) Exceptional (git history) High (isolated from files) Version History Limited (native) Comprehensive (git commits) Limited (native) Best For Mobile, fast sync Developers, power users Cross-platform"},{"location":"guides/cloud-sync/#initial-setup","title":"Initial Setup","text":""},{"location":"guides/cloud-sync/#google-drive","title":"Google Drive","text":"<pre><code>pm cloud init gdrive\n</code></pre> <p>This opens your browser for Google OAuth consent. APM uses the Application Data folder, which is isolated from your regular Drive files \u2014 you won't see the vault blob in your Drive UI.</p>"},{"location":"guides/cloud-sync/#github","title":"GitHub","text":"<pre><code>pm cloud init github\n</code></pre> <p>You'll need to provide:</p> <ol> <li>A Personal Access Token (PAT) with <code>repo</code> scope</li> <li>A private repository name (e.g., <code>my-vault-backup</code>)</li> </ol> <p>GitHub sync stores the vault as a file in the repo, giving you comprehensive version history via git commits.</p>"},{"location":"guides/cloud-sync/#dropbox","title":"Dropbox","text":"<pre><code>pm cloud init dropbox\n</code></pre> <p>This opens your browser for Dropbox OAuth consent. APM uses the Application folder, isolated from your regular Dropbox files.</p>"},{"location":"guides/cloud-sync/#all-providers-at-once","title":"All Providers At Once","text":"<pre><code>pm cloud init all\n</code></pre> <p>Initializes Google Drive, GitHub, and Dropbox in sequence.</p>"},{"location":"guides/cloud-sync/#syncing","title":"Syncing","text":""},{"location":"guides/cloud-sync/#manual-sync","title":"Manual Sync","text":"<pre><code>pm cloud sync\n</code></pre> <p>This uploads your current vault to all configured providers. The vault is filtered through <code>.apmignore</code> before encryption and upload.</p>"},{"location":"guides/cloud-sync/#downloading","title":"Downloading","text":"<pre><code>pm cloud get\n</code></pre> <p>Downloads the remote vault blob. You'll be prompted to choose how to handle it:</p> <ol> <li>Overwrite local \u2014 Replace your local vault with the remote one</li> <li>Keep local + conflict copy \u2014 Save the remote vault as a separate file</li> <li>Cancel \u2014 Abort the download</li> </ol>"},{"location":"guides/cloud-sync/#auto-sync","title":"Auto-Sync","text":"<pre><code>pm cloud autosync\n</code></pre> <p>Runs periodic sync loops in the foreground. Useful for keeping vaults synchronized across active machines.</p>"},{"location":"guides/cloud-sync/#provider-specific-sync","title":"Provider-Specific Sync","text":"<pre><code>pm cloud sync gdrive\npm cloud sync github\npm cloud sync dropbox\npm cloud get gdrive\npm cloud get github\n</code></pre>"},{"location":"guides/cloud-sync/#resetting-cloud-configuration","title":"Resetting Cloud Configuration","text":"<pre><code>pm cloud reset\n</code></pre> <p>Clears all cloud provider credentials and configuration from the vault.</p>"},{"location":"guides/cloud-sync/#retrieval-keys","title":"Retrieval Keys","text":"<p>When you first upload your vault, APM generates a retrieval key \u2014 a unique identifier that can be used to locate your vault on the provider. This is useful when setting up on a new device.</p>"},{"location":"guides/cloud-sync/#metadata-consent","title":"Metadata Consent","text":"<p>For Google Drive and Dropbox, APM can store a one-way hash of your retrieval key in provider metadata. This enables vault lookup by key hash. You'll be asked for explicit consent:</p> <ul> <li>If you consent: The vault can be found using the retrieval key hash</li> <li>If you decline: The vault is identified by its provider-specific ID (Drive file ID, Dropbox path) instead</li> </ul> <p>This ensures no identifying information is stored without your permission.</p>"},{"location":"guides/cloud-sync/#security-guarantees","title":"Security Guarantees","text":"<p>What Is Uploaded</p> <p>The cloud receives an encrypted binary blob \u2014 the same <code>APMVAULT</code> format stored on disk. It includes:</p> <ul> <li>Encrypted vault data (using the vault's configured AEAD cipher)</li> <li>HMAC-SHA256 integrity signature</li> <li>Salt and profile metadata (in the unencrypted header)</li> </ul> <p>What Is Never Uploaded</p> <ul> <li>Your master password</li> <li>Plaintext entries</li> <li>Decrypted vault contents</li> <li>Session data</li> </ul>"},{"location":"guides/cloud-sync/#using-apmignore-with-cloud-sync","title":"Using .apmignore with Cloud Sync","text":"<p>The <code>.apmignore</code> file is processed before encryption and upload. You can:</p> <ul> <li>Exclude entire spaces</li> <li>Exclude specific entries by <code>space:type:name</code> pattern</li> <li>Use provider-specific rules</li> <li>Strip vocabulary data</li> </ul> <p>See the .apmignore Guide for full details.</p>"},{"location":"guides/cloud-sync/#conflict-resolution","title":"Conflict Resolution","text":"<p>When <code>pm cloud get</code> finds that the remote vault differs from your local vault:</p> <ol> <li>Overwrite local \u2014 The remote vault replaces your local vault entirely</li> <li>Keep local + save conflict copy \u2014 The remote vault is saved alongside your vault as a timestamped copy</li> <li>Cancel \u2014 No changes are made</li> </ol> <p>Info</p> <p>Conflict handling is whole-vault only. APM does not perform entry-level merge. This is intentional \u2014 merging encrypted data safely requires entry-level conflict detection, which could leak metadata about vault contents.</p>"},{"location":"guides/cloud-sync/#troubleshooting","title":"Troubleshooting","text":""},{"location":"guides/cloud-sync/#oauth-token-expired","title":"\"OAuth token expired\"","text":"<p>Re-run <code>pm cloud init [provider]</code> to re-authenticate.</p>"},{"location":"guides/cloud-sync/#vault-not-found-on-provider","title":"\"Vault not found on provider\"","text":"<p>Either the vault was never uploaded, or the retrieval key/file ID has changed. Try uploading again with <code>pm cloud sync</code>.</p>"},{"location":"guides/cloud-sync/#hmac-verification-failed-after-download","title":"\"HMAC verification failed after download\"","text":"<p>The downloaded vault may have been corrupted during transfer. Try downloading again. If the problem persists, the remote vault may have been tampered with.</p>"},{"location":"guides/cloud-sync/#next-steps","title":"Next Steps","text":"<ul> <li>Using .apmignore \u2014 Filter what gets uploaded</li> <li>Sessions \u2014 Session management for sync workflows</li> <li>Cloud Sync Concepts \u2014 Deep technical details</li> </ul>"},{"location":"guides/faceid/","title":"Face ID (Optional)","text":"<p>Face ID enables biometric unlock using local face recognition. It is optional and only available when APM is built with the <code>faceid</code> build tag because it depends on native OpenCV and dlib libraries.</p> <p>It is a convenience layer over the normal unlock flow, not a replacement for the master password. If Face ID is unavailable or verification fails, you can still unlock with your password.</p>"},{"location":"guides/faceid/#build","title":"Build","text":"<pre><code># Standard build (no Face ID)\ngo build -o pm.exe\n\n# Face ID build\ngo build -tags faceid -o pm.exe\n</code></pre>"},{"location":"guides/faceid/#commands","title":"Commands","text":"<pre><code>pm faceid enroll\npm faceid status\npm faceid test\npm faceid remove\n</code></pre>"},{"location":"guides/faceid/#pm-faceid-enroll","title":"<code>pm faceid enroll</code>","text":"<p>Registers a local face profile for future unlock attempts.</p>"},{"location":"guides/faceid/#pm-faceid-status","title":"<code>pm faceid status</code>","text":"<p>Shows whether Face ID enrollment metadata is present and available.</p>"},{"location":"guides/faceid/#pm-faceid-test","title":"<code>pm faceid test</code>","text":"<p>Runs a verification check without changing enrollment.</p>"},{"location":"guides/faceid/#pm-faceid-remove","title":"<code>pm faceid remove</code>","text":"<p>Deletes the local Face ID enrollment metadata.</p>"},{"location":"guides/faceid/#storage","title":"Storage","text":"<ul> <li>Models are downloaded automatically to your user config directory under <code>apm/faceid/models</code>.</li> <li>Enrollment metadata is stored next to the vault at <code>faceid/enrollment.json</code>.</li> </ul>"},{"location":"guides/faceid/#typical-workflow","title":"Typical Workflow","text":"<pre><code>go build -tags faceid -o pm.exe\npm faceid enroll\npm faceid status\npm unlock\n</code></pre> <p>Face ID participates in the unlock path only when the binary was built with the <code>faceid</code> tag and enrollment data exists for the current vault.</p>"},{"location":"guides/faceid/#notes","title":"Notes","text":"<ul> <li>Face ID uses the default camera (index <code>0</code>).</li> <li>If Face ID fails, you can always unlock with your master password.</li> <li>Native OpenCV and dlib requirements are the reason this feature is behind a build tag.</li> </ul>"},{"location":"guides/import-export/","title":"Importing and Exporting","text":"<p>APM supports importing data from external sources and exporting your vault contents in multiple formats. This enables migration from other password managers and secure backup creation.</p>"},{"location":"guides/import-export/#supported-formats","title":"Supported Formats","text":"Format Import Export Encryption Support Notes JSON \u2705 \u2705 \u2705 Optional Full-fidelity, all entry types CSV \u2705 \u2705 \u274c Password entries only TXT \u2705 \u2705 \u274c Human-readable, optional redaction"},{"location":"guides/import-export/#exporting","title":"Exporting","text":""},{"location":"guides/import-export/#json-export","title":"JSON Export","text":"<pre><code>pm export json\n</code></pre> <p>Exports all vault entries to a JSON file with the full APM data structure. This is the most complete export format, preserving all entry types and their fields.</p> <p>With encryption:</p> <pre><code>pm export json --encrypt\n</code></pre> <p>This encrypts the export file with a separate password, producing a protected JSON file that can only be read with the export password.</p>"},{"location":"guides/import-export/#csv-export","title":"CSV Export","text":"<pre><code>pm export csv\n</code></pre> <p>Exports password entries only in a standard CSV format:</p> <pre><code>account,username,password\ngithub.com,aarav,s3cr3t_p4ss\n</code></pre> <p>Warning</p> <p>CSV export includes passwords in plaintext. Handle the output file with extreme care and delete it after use.</p>"},{"location":"guides/import-export/#txt-export","title":"TXT Export","text":"<pre><code>pm export txt\n</code></pre> <p>Exports a human-readable text file covering all entry types with formatted sections.</p> <p>With password redaction:</p> <pre><code>pm export txt --no-password\n</code></pre> <p>This creates a reference document with sensitive values replaced by <code>********</code>.</p>"},{"location":"guides/import-export/#importing","title":"Importing","text":""},{"location":"guides/import-export/#from-json","title":"From JSON","text":"<pre><code>pm import json backup.json\n</code></pre> <p>Imports entries from a JSON file matching APM's export format. All entry types are supported.</p> <p>From encrypted JSON:</p> <pre><code>pm import json encrypted_backup.json --decrypt\n</code></pre> <p>You'll be prompted for the export password.</p>"},{"location":"guides/import-export/#from-csv","title":"From CSV","text":"<pre><code>pm import csv passwords.csv\n</code></pre> <p>Expects columns for <code>account</code>, <code>username</code>, and <code>password</code>. This is compatible with exports from many other password managers (1Password, Bitwarden, Chrome, etc.).</p>"},{"location":"guides/import-export/#from-txt","title":"From TXT","text":"<pre><code>pm import txt vault_export.txt\n</code></pre> <p>Parses structured text files. The parser looks for <code>Account:</code>, <code>Username:</code>, and <code>Password:</code> labels.</p>"},{"location":"guides/import-export/#import-behavior","title":"Import Behavior","text":"<p>When importing:</p> <ul> <li>Duplicate detection \u2014 APM checks for existing entries with the same account name</li> <li>Space assignment \u2014 Imported entries are placed in the currently active space</li> <li>Validation \u2014 Entries are validated against any loaded password policies</li> <li>Audit logging \u2014 Each import action is recorded in the audit log</li> </ul>"},{"location":"guides/import-export/#migration-from-other-password-managers","title":"Migration from Other Password Managers","text":""},{"location":"guides/import-export/#from-chrome-brave-edge","title":"From Chrome / Brave / Edge","text":"<ol> <li>Export passwords from <code>chrome://settings/passwords</code>  (CSV format)</li> <li>Import: <code>pm import csv chrome_passwords.csv</code></li> </ol>"},{"location":"guides/import-export/#from-1password","title":"From 1Password","text":"<ol> <li>Export from 1Password (CSV format)</li> <li>Import: <code>pm import csv 1password_export.csv</code></li> </ol>"},{"location":"guides/import-export/#from-bitwarden","title":"From Bitwarden","text":"<ol> <li>Export from Bitwarden (JSON or CSV)</li> <li>Import: <code>pm import json bitwarden_export.json</code> or <code>pm import csv bitwarden_export.csv</code></li> </ol>"},{"location":"guides/import-export/#from-keepass","title":"From KeePass","text":"<ol> <li>Export from KeePass (CSV format)</li> <li>Import: <code>pm import csv keepass_export.csv</code></li> </ol> <p>Tip</p> <p>After importing, delete the export files from other password managers. They contain plaintext credentials.</p>"},{"location":"guides/import-export/#next-steps","title":"Next Steps","text":"<ul> <li>Vault Management \u2014 Organizing imported entries</li> <li>Cloud Sync \u2014 Sync your vault after importing</li> </ul>"},{"location":"guides/inject/","title":"Injecting Secrets Into Your Shell","text":"<p><code>pm inject</code> exports vault data into the current shell session as environment variables. It is meant for short-lived developer workflows such as local app startup, CI smoke testing, and ad hoc scripting where a process expects secrets through env vars.</p> <p>Unlike <code>pm get</code>, <code>pm inject</code> does not print the secret values directly for you to copy. It emits shell-specific export commands, and you evaluate those commands in your shell.</p>"},{"location":"guides/inject/#how-it-works","title":"How It Works","text":"<p><code>pm inject</code>:</p> <ol> <li>Unlocks the vault using your normal APM flow</li> <li>Resolves one or more vault entries by name</li> <li>Converts those entries into environment variables</li> <li>Stores a small local inject-session record so the variables can be removed later with <code>pm inject kill</code></li> </ol> <p>The command supports <code>bash</code>, <code>zsh</code>, <code>fish</code>, and <code>PowerShell</code>.</p>"},{"location":"guides/inject/#quick-start","title":"Quick Start","text":""},{"location":"guides/inject/#bash-zsh","title":"Bash / Zsh","text":"<pre><code>eval \"$(pm inject --inject github)\"\n</code></pre>"},{"location":"guides/inject/#fish","title":"Fish","text":"<pre><code>eval (pm inject --inject github)\n</code></pre>"},{"location":"guides/inject/#powershell","title":"PowerShell","text":"<pre><code>pm inject --inject github | Invoke-Expression\n</code></pre> <p>After the command runs, the entry becomes an environment variable in the current shell.</p> <p>For a password entry named <code>github</code>, the default variable name is:</p> <pre><code>GITHUB\n</code></pre>"},{"location":"guides/inject/#selecting-entries","title":"Selecting Entries","text":"<p>You can inject explicit entry names:</p> <pre><code>pm inject --inject github,aws-prod,openai-token\n</code></pre> <p>Entry lookup is by vault item name in the active space. If any requested entry is missing, the command fails rather than partially injecting.</p> <p>Supported entry sources include:</p> <ul> <li>Password entries</li> <li>TOTP entries</li> <li>Tokens</li> <li>Secure notes</li> <li>API keys</li> <li>SSH keys</li> <li>Wi-Fi passwords</li> <li>Recovery codes</li> <li>Certificates</li> <li>Cloud credential secret keys</li> <li>Docker registry tokens</li> <li>SSH config private keys</li> </ul>"},{"location":"guides/inject/#apminject-files","title":"<code>.apminject</code> Files","text":"<p>If you omit <code>--inject</code>, APM searches upward from the current directory for a <code>.apminject</code> file.</p> <p>This lets you keep project-local injection rules in the repo or workspace root.</p>"},{"location":"guides/inject/#simple-list-format","title":"Simple List Format","text":"<pre><code>- entry: github\n- entry: aws-prod\n</code></pre>"},{"location":"guides/inject/#wrapped-format","title":"Wrapped Format","text":"<pre><code>inject:\n  - entry: github\n  - entry: aws-prod\n</code></pre> <p>You can also use <code>entries:</code> instead of <code>inject:</code>.</p>"},{"location":"guides/inject/#custom-variable-names","title":"Custom Variable Names","text":"<pre><code>inject:\n  - entry: github\n    as: GITHUB_TOKEN\n  - entry: aws-prod\n    as: AWS_SECRET_ACCESS_KEY\n</code></pre> <p>If <code>as</code> is omitted, APM derives a variable name automatically by uppercasing the entry name and replacing separators with underscores.</p> <p>Examples:</p> <ul> <li><code>github</code> -&gt; <code>GITHUB</code></li> <li><code>aws-prod</code> -&gt; <code>AWS_PROD</code></li> <li><code>openai token</code> -&gt; <code>OPENAI_TOKEN</code></li> </ul>"},{"location":"guides/inject/#recommended-workflow","title":"Recommended Workflow","text":"<p>For one-off usage:</p> <pre><code>eval \"$(pm inject --inject github)\"\n</code></pre> <p>For project usage with a checked-in <code>.apminject</code> file:</p> <pre><code>eval \"$(pm inject)\"\n</code></pre> <p>When you are done:</p> <pre><code>eval \"$(pm inject kill)\"\n</code></pre> <p>On PowerShell:</p> <pre><code>pm inject kill | Invoke-Expression\n</code></pre> <p><code>pm inject kill</code> removes the injected variables and clears the tracked inject session.</p>"},{"location":"guides/inject/#setup-shell","title":"<code>setup-shell</code>","text":"<p>APM can install a small shell helper so you do not have to type <code>eval</code> manually every time:</p> <pre><code>pm inject setup-shell\n</code></pre> <p>This adds an <code>inject()</code> helper to the detected shell config:</p> <ul> <li><code>~/.bashrc</code> for Bash</li> <li><code>~/.zshrc</code> for Zsh</li> <li><code>~/.config/fish/config.fish</code> for Fish</li> <li><code>$PROFILE</code> for PowerShell</li> </ul> <p>After reloading your shell config, you can run:</p> <pre><code>inject --inject github\ninject\ninject kill\n</code></pre>"},{"location":"guides/inject/#session-behavior","title":"Session Behavior","text":"<p>APM tracks one active inject session at a time. If a session is already active, <code>pm inject</code> will stop and ask you to run <code>pm inject kill</code> first.</p> <p>The session record stores:</p> <ul> <li>A generated session ID</li> <li>The injected variable names</li> <li>Injection time</li> <li>Parent shell PID</li> </ul> <p>By default, this record lives under your user config directory:</p> <pre><code>&lt;config&gt;/apm/inject_session\n</code></pre> <p>If <code>APM_DATA_DIR</code> is set, APM stores it there instead.</p>"},{"location":"guides/inject/#security-notes","title":"Security Notes","text":"<ul> <li>Injected secrets live in your shell environment until you remove them or close the shell.</li> <li>Child processes inherit those environment variables.</li> <li>Prefer a dedicated shell for sensitive injection workflows.</li> <li>Use <code>pm inject kill</code> immediately after the dependent process exits.</li> <li>Do not commit <code>.apminject</code> files that reveal sensitive internal naming unless that is acceptable for the repo.</li> </ul> <p><code>pm inject</code> is convenient, but environment variables are still a wider exposure surface than keeping secrets only inside the vault.</p>"},{"location":"guides/inject/#troubleshooting","title":"Troubleshooting","text":""},{"location":"guides/inject/#no-apminject-file-found","title":"No <code>.apminject</code> file found","text":"<p>Use <code>--inject</code> explicitly, or create a <code>.apminject</code> file in the current directory or a parent directory.</p>"},{"location":"guides/inject/#unknown-shell-warning","title":"Unknown shell warning","text":"<p>APM falls back to Bash syntax if it cannot detect the shell from environment variables.</p>"},{"location":"guides/inject/#powershell-does-not-persist-variables","title":"PowerShell does not persist variables","text":"<p>Use:</p> <pre><code>pm inject --inject github | Invoke-Expression\n</code></pre> <p>Without <code>Invoke-Expression</code>, you will only print the export script instead of applying it.</p>"},{"location":"guides/inject/#a-session-is-already-active","title":"A session is already active","text":"<p>Run:</p> <pre><code>eval \"$(pm inject kill)\"\n</code></pre> <p>or on PowerShell:</p> <pre><code>pm inject kill | Invoke-Expression\n</code></pre>"},{"location":"guides/inject/#related-commands","title":"Related Commands","text":"<ul> <li><code>pm unlock</code> \u2014 Start the vault session required for secret access</li> <li><code>pm get</code> \u2014 View or copy a secret interactively</li> <li><code>pm inject kill</code> \u2014 Wipe injected variables from the active shell</li> <li><code>pm inject setup-shell</code> \u2014 Install the <code>inject()</code> shell helper</li> </ul>"},{"location":"guides/mcp-integration/","title":"MCP Integration","text":"<p>APM ships with a built-in MCP server under <code>pm mcp</code>. The current implementation is token-based, permission-scoped, and requires either an active unlock session or a delegated ephemeral session.</p>"},{"location":"guides/mcp-integration/#generate-a-token","title":"Generate a token","text":"<pre><code>pm mcp token\n</code></pre> <p>The setup flow asks for:</p> <ul> <li>a token name</li> <li>expiry in minutes, where <code>0</code> means no expiry</li> <li>one or more permission scopes</li> </ul> <p>The scopes exposed by the current code are:</p> <ul> <li><code>read</code></li> <li><code>secrets</code></li> <li><code>write</code></li> <li><code>admin</code></li> </ul>"},{"location":"guides/mcp-integration/#run-the-server","title":"Run the server","text":"<pre><code>pm mcp serve --token YOUR_TOKEN\n</code></pre> <p><code>pm mcp serve</code> is intended to be launched by an MCP client over stdio.</p> <p>Related commands:</p> <ul> <li><code>pm mcp config</code></li> <li><code>pm mcp token</code></li> <li><code>pm mcp list</code></li> <li><code>pm mcp revoke &lt;name_or_token&gt;</code></li> </ul>"},{"location":"guides/mcp-integration/#session-requirements","title":"Session requirements","text":"<p>The server does not unlock the vault by itself. It expects one of these:</p> <ul> <li>a normal unlocked session from <code>pm unlock</code></li> <li>an ephemeral delegated session provided through <code>APM_EPHEMERAL_ID</code></li> </ul> <p>Ephemeral sessions can also be bound to an agent name through <code>APM_EPHEMERAL_AGENT</code>.</p>"},{"location":"guides/mcp-integration/#mutation-guardrails","title":"Mutation guardrails","text":"<p>Write tools are not single-shot commits. The code uses a preview transaction flow:</p> <ol> <li>First call creates a pending transaction and returns a <code>tx_id</code>.</li> <li>Second call repeats the tool with <code>tx_id</code> and <code>approve: true</code>.</li> <li>Successful commits return a receipt string.</li> </ol> <p>This applies to mutation tools such as entry add, edit, and delete operations.</p>"},{"location":"guides/mcp-integration/#tool-categories","title":"Tool categories","text":"<p>The MCP implementation covers more than simple vault reads. The current server code includes tooling for:</p> <ul> <li>entry listing and search</li> <li>secret retrieval and TOTP access</li> <li>entry mutation</li> <li>spaces</li> <li>profiles</li> <li>plugin install flows</li> <li>cloud sync and cloud config</li> <li>audit or history access</li> </ul> <p>See the MCP tools reference for the exact schemas in this repo.</p>"},{"location":"guides/mcp-integration/#client-configuration","title":"Client configuration","text":"<p><code>pm mcp config</code> prints a first-run config snippet. <code>pm mcp token --auto</code> also tries to update known client config locations automatically.</p> <p>The code searches common config paths for tools such as:</p> <ul> <li>Claude Desktop</li> <li>Cursor</li> <li>VS Code / Cline-style MCP settings</li> </ul> <p>For manual configuration, the server entry is still <code>pm mcp serve --token ...</code>.</p>"},{"location":"guides/mcp-integration/#recommended-flow","title":"Recommended flow","text":"<pre><code>pm unlock\npm mcp token\npm mcp list\n</code></pre> <p>Then point your MCP client at the generated tokenized <code>serve</code> command.</p>"},{"location":"guides/plugins/","title":"Using Plugins","text":"<p>APM features a manifest-based plugin system that lets you extend the CLI with custom commands, hooks, and automation workflows. Plugins are distributed via a cloud-based marketplace or installed locally.</p>"},{"location":"guides/plugins/#installing-plugins","title":"Installing Plugins","text":""},{"location":"guides/plugins/#from-the-marketplace","title":"From the Marketplace","text":"<pre><code># Browse available plugins\npm plugins market\n\n# Install a plugin by name\npm plugins install vault_cleaner\n\n# Search for plugins\npm plugins search backup\n</code></pre>"},{"location":"guides/plugins/#from-a-local-directory","title":"From a Local Directory","text":"<pre><code>pm plugins local /path/to/plugin/directory\n</code></pre> <p>The directory must contain a valid <code>plugin.json</code> manifest.</p>"},{"location":"guides/plugins/#listing-installed-plugins","title":"Listing Installed Plugins","text":"<pre><code>pm plugins installed\n</code></pre>"},{"location":"guides/plugins/#managing-plugin-permissions","title":"Managing Plugin Permissions","text":"<p>Every plugin declares the permissions it needs in its <code>plugin.json</code> manifest. APM enforces these at runtime and stores permission overrides inside your encrypted vault.</p>"},{"location":"guides/plugins/#viewing-permissions","title":"Viewing Permissions","text":"<pre><code>pm plugins access\n</code></pre> <p>Shows all installed plugins and their current permission states.</p>"},{"location":"guides/plugins/#toggling-permissions","title":"Toggling Permissions","text":"<pre><code># Disable a specific permission for a plugin\npm plugins access vault_cleaner vault.delete off\n\n# Re-enable it\npm plugins access vault_cleaner vault.delete on\n</code></pre> <p>Permission Overrides Travel with Your Vault</p> <p>Permission overrides are stored inside the encrypted vault. When you sync your vault to cloud, the overrides travel with it \u2014 ensuring consistent security policy across devices.</p>"},{"location":"guides/plugins/#running-plugin-commands","title":"Running Plugin Commands","text":"<p>Plugins can register custom commands that appear in the APM CLI:</p> <pre><code>pm plugins run &lt;plugin_name&gt; &lt;command&gt;\n</code></pre> <p>Or, if the plugin registers hooks, they fire automatically during vault lifecycle events.</p>"},{"location":"guides/plugins/#publishing-plugins","title":"Publishing Plugins","text":"<pre><code>pm plugins push &lt;plugin_name&gt;\n</code></pre> <p>Uploads the plugin to the marketplace via your configured cloud provider. The plugin must have:</p> <ul> <li>A valid <code>plugin.json</code> manifest</li> <li>Version following semantic versioning (e.g., <code>1.0.0</code>)</li> </ul>"},{"location":"guides/plugins/#creating-a-plugin","title":"Creating a Plugin","text":""},{"location":"guides/plugins/#directory-structure","title":"Directory Structure","text":"<pre><code>my_plugin/\n\u2514\u2500\u2500 plugin.json\n</code></pre>"},{"location":"guides/plugins/#manifest-schema-pluginjson","title":"Manifest Schema (<code>plugin.json</code>)","text":"<pre><code>{\n  \"name\": \"my_plugin\",\n  \"version\": \"1.0.0\",\n  \"description\": \"A brief description of what this plugin does\",\n  \"author\": \"Your Name\",\n  \"permissions_required\": [\n    \"vault.read\",\n    \"vault.write\",\n    \"file.storage\"\n  ],\n  \"allowed_file_types\": [\".json\", \".txt\"],\n  \"commands\": [\"backup\", \"restore\"],\n  \"hooks\": [\"post:add\", \"pre:sync\"]\n}\n</code></pre>"},{"location":"guides/plugins/#manifest-fields","title":"Manifest Fields","text":"Field Type Required Description <code>name</code> <code>string</code> \u2705 Unique plugin identifier <code>version</code> <code>string</code> \u2705 Semantic version (<code>X.Y.Z</code>) <code>description</code> <code>string</code> \u274c Human-readable description <code>author</code> <code>string</code> \u274c Plugin author <code>permissions_required</code> <code>string[]</code> \u2705 List of required permissions <code>allowed_file_types</code> <code>string[]</code> \u274c File extensions the plugin may access <code>commands</code> <code>string[]</code> \u274c Commands the plugin exposes <code>hooks</code> <code>string[]</code> \u274c Lifecycle hooks to listen for"},{"location":"guides/plugins/#hook-types","title":"Hook Types","text":"<p>Hooks fire at specific points in the vault lifecycle:</p> Hook Pattern When It Fires <code>pre:add</code> Before an entry is added <code>post:add</code> After an entry is added <code>pre:sync</code> Before a cloud sync <code>post:sync</code> After a cloud sync <code>pre:delete</code> Before an entry is deleted <code>post:delete</code> After an entry is deleted"},{"location":"guides/plugins/#example-plugins","title":"Example Plugins","text":"<p>APM ships with example plugins in the <code>examples/plugins/</code> directory:</p>"},{"location":"guides/plugins/#quick_backup-automated-vault-backup","title":"<code>quick_backup</code> \u2014 Automated Vault Backup","text":"<pre><code>{\n  \"name\": \"quick_backup\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Creates a timestamped backup of the vault file\",\n  \"permissions_required\": [\"vault.read\", \"vault.backup\", \"file.storage\"],\n  \"commands\": [\"backup\"]\n}\n</code></pre>"},{"location":"guides/plugins/#clip_auto_clear-clipboard-auto-clear","title":"<code>clip_auto_clear</code> \u2014 Clipboard Auto-Clear","text":"<pre><code>{\n  \"name\": \"clip_auto_clear\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Clears clipboard after a configurable delay\",\n  \"permissions_required\": [\"system.clipboard.write\"],\n  \"commands\": [\"clear\"]\n}\n</code></pre>"},{"location":"guides/plugins/#cloud_sync_hook-auto-sync-after-add","title":"<code>cloud_sync_hook</code> \u2014 Auto-Sync After Add","text":"<pre><code>{\n  \"name\": \"cloud_sync_hook\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Triggers cloud sync after adding entries\",\n  \"permissions_required\": [\"vault.read\", \"cloud.sync\"],\n  \"hooks\": [\"post:add\"]\n}\n</code></pre>"},{"location":"guides/plugins/#security-considerations","title":"Security Considerations","text":"<p>Plugins Execute Code</p> <p>Plugins can execute arbitrary commands on your system within their declared permissions. Always:</p> <ol> <li>Review permissions before installing (<code>pm plugins access</code>)</li> <li>Disable unnecessary permissions (<code>pm plugins access &lt;name&gt; &lt;perm&gt; off</code>)</li> <li>Only install plugins from trusted sources</li> </ol>"},{"location":"guides/plugins/#next-steps","title":"Next Steps","text":"<ul> <li>Plugin API Reference \u2014 Full permission catalog and step commands</li> <li>Plugin Architecture \u2014 Deep technical details</li> </ul>"},{"location":"guides/sessions/","title":"Managing Sessions","text":"<p>APM uses explicit sessions as a security boundary \u2014 the vault is encrypted at rest and must be unlocked before any sensitive operation. Sessions prevent accidental long-term exposure of decrypted data.</p>"},{"location":"guides/sessions/#session-lifecycle","title":"Session Lifecycle","text":"<pre><code>stateDiagram-v2\n    [*] --&gt; Locked: Vault at rest\n    Locked --&gt; Unlocked: pm unlock\n    Unlocked --&gt; Locked: pm lock\n    Unlocked --&gt; Locked: Session expired\n    Unlocked --&gt; Locked: Inactivity timeout\n    Unlocked --&gt; Active: Sensitive command\n    Active --&gt; Unlocked: Command completes</code></pre>"},{"location":"guides/sessions/#unlocking","title":"Unlocking","text":"<pre><code>pm unlock\n</code></pre> <p>You are prompted for:</p> <ul> <li>Master password</li> <li>Session duration \u2014 How long before the session auto-expires (default: 30 minutes)</li> <li>Inactivity timeout \u2014 How long of idle time before auto-lock (default: 10 minutes)</li> <li>Read-only mode \u2014 Optional flag that prevents all write operations</li> </ul> <p>On success, APM:</p> <ol> <li>Decrypts the vault in memory</li> <li>Creates a session file  in the system temp directory</li> <li>Starts a background cleanup process that deletes the session file when expired</li> </ol>"},{"location":"guides/sessions/#locking","title":"Locking","text":"<pre><code>pm lock\n</code></pre> <p>Immediately:</p> <ol> <li>Deletes the session file</li> <li>Wipes decrypted data from memory</li> <li>Locks the autofill daemon (if running)</li> </ol>"},{"location":"guides/sessions/#shell-scoped-sessions","title":"Shell-Scoped Sessions","text":"<p>By default, APM uses a single global session. To run independent sessions per terminal, set the <code>APM_SESSION_ID</code> environment variable:</p> Bash / ZshPowerShell <pre><code>export APM_SESSION_ID=\"terminal1\"\npm unlock\n# This terminal has its own session\n</code></pre> <pre><code>$env:APM_SESSION_ID = \"terminal1\"\npm unlock\n</code></pre> <p>Each shell with a different <code>APM_SESSION_ID</code> gets its own session file and can be unlocked/locked independently.</p> <p>Session File Location</p> <ul> <li>Global session: <code>$TEMP/pm_session_global.json</code></li> <li>Scoped session: <code>$TEMP/pm_session_{SESSION_ID}.json</code></li> </ul> <p>The session ID is sanitized to contain only alphanumeric characters.</p>"},{"location":"guides/sessions/#ephemeral-delegated-sessions","title":"Ephemeral Delegated Sessions","text":"<p>Ephemeral sessions are short-lived, context-bound tokens that delegate vault access to another process or agent without sharing your master password directly.</p>"},{"location":"guides/sessions/#issuing-an-ephemeral-session","title":"Issuing an Ephemeral Session","text":"<pre><code>pm session issue --label \"CI Pipeline\" --scope read --ttl 15m\n</code></pre> <p>Options:</p> Flag Description Default <code>--label</code> Human-readable label (none) <code>--scope</code> Access scope: <code>read</code> or <code>write</code> <code>read</code> <code>--ttl</code> Time to live (e.g., <code>15m</code>, <code>1h</code>) Required <code>--bind-host</code> Restrict to current machine <code>false</code> <code>--bind-pid</code> Restrict to a specific process ID (none) <code>--agent</code> Restrict to a specific agent name (none) <p>The command outputs an ephemeral session ID like <code>eps_a1b2c3d4...</code>.</p>"},{"location":"guides/sessions/#using-an-ephemeral-session","title":"Using an Ephemeral Session","text":"<p>Set the <code>APM_EPHEMERAL_ID</code> environment variable to use the delegated session:</p> <pre><code>export APM_EPHEMERAL_ID=\"eps_a1b2c3d4...\"\npm get github  # Uses the ephemeral session instead of the regular one\n</code></pre>"},{"location":"guides/sessions/#security-bindings","title":"Security Bindings","text":"<p>Ephemeral sessions support three types of binding for additional security:</p> Binding What It Checks Host SHA-256 hash of hostname + username must match PID The calling process ID must match Agent The agent name (via <code>APM_ACTOR</code>) must match <p>If a binding mismatch is detected, the session is rejected.</p>"},{"location":"guides/sessions/#managing-ephemeral-sessions","title":"Managing Ephemeral Sessions","text":"<pre><code>pm session list        # List all active ephemeral sessions\npm session revoke ID   # Revoke a specific session\n</code></pre> <p>Revoked and expired sessions are automatically cleaned up.</p>"},{"location":"guides/sessions/#use-cases","title":"Use Cases","text":"<ul> <li>CI/CD pipelines \u2014 Issue a short-lived read-only session for automated credential retrieval</li> <li>MCP server \u2014 Provide an ephemeral session so the AI agent doesn't need your master password</li> <li>Pair programming \u2014 Delegate temporary read access to a teammate's terminal</li> </ul>"},{"location":"guides/sessions/#how-sessions-interact-with-other-features","title":"How Sessions Interact with Other Features","text":"Feature Session Requirement <code>pm add</code> Active write session (not read-only) <code>pm get</code> Active session (any type) <code>pm get</code> edit/delete actions Active write session <code>pm cloud sync</code> Active session Autofill daemon Follows CLI session unlock/lock state MCP server Requires active session or ephemeral session"},{"location":"guides/sessions/#next-steps","title":"Next Steps","text":"<ul> <li>MCP Integration \u2014 Using ephemeral sessions with AI agents</li> <li>Sessions Concept \u2014 Deep technical details</li> </ul>"},{"location":"guides/team-edition/","title":"Team Edition","text":"<p>The team edition is a separate CLI whose docs live under Team. Build it from the repository's <code>team/</code> directory:</p> <pre><code>cd team\ngo build -o pm-team .\n</code></pre>"},{"location":"guides/team-edition/#what-pm-team-currently-provides","title":"What <code>pm-team</code> currently provides","text":"<ul> <li>organization initialization</li> <li>login and short-lived team sessions</li> <li>departments</li> <li>roles and per-user permission overrides</li> <li>shared entry management</li> <li>approval queues</li> <li>export, audit, health, and info commands</li> </ul> <p>The team edition exists and is usable, but it is less mature than the personal <code>pm</code> CLI. Some behaviors are still uneven across entry types and approval paths.</p>"},{"location":"guides/team-edition/#initialize-an-organization","title":"Initialize an organization","text":"<p>The current command signature is:</p> <pre><code>pm-team init &lt;org_name&gt; &lt;admin_username&gt;\n</code></pre> <p>Example:</p> <pre><code>pm-team init \"Acme\" aarav\n</code></pre> <p>This creates:</p> <ul> <li>the team vault</li> <li>a default <code>general</code> department</li> <li>the initial admin user</li> </ul>"},{"location":"guides/team-edition/#authenticate","title":"Authenticate","text":"<pre><code>pm-team login &lt;username&gt;\npm-team whoami\npm-team logout\n</code></pre> <p>Team sessions are separate from personal <code>pm</code> sessions and currently expire after 15 minutes.</p>"},{"location":"guides/team-edition/#manage-departments","title":"Manage departments","text":"<pre><code>pm-team dept list\npm-team dept create Engineering\npm-team dept switch alice engineering\n</code></pre> <p>Department switching in the current CLI is an admin action that changes a user's active department.</p>"},{"location":"guides/team-edition/#manage-users","title":"Manage users","text":"<pre><code>pm-team user list\npm-team user add alice --role USER --dept general\npm-team user remove alice\npm-team user promote alice MANAGER\npm-team user roles\npm-team user permission grant alice add_entry\npm-team user permission revoke alice add_entry\n</code></pre> <p>Built-in roles in code today:</p> <ul> <li><code>ADMIN</code></li> <li><code>MANAGER</code></li> <li><code>USER</code></li> <li><code>AUDITOR</code></li> <li><code>SECURITY</code></li> </ul>"},{"location":"guides/team-edition/#shared-entries","title":"Shared entries","text":"<p>The team add flow currently supports 22 shared entry types, including passwords, TOTP, API keys, tokens, notes, SSH material, certificates, Wi-Fi, recovery codes, documents, cloud credentials, CI/CD secrets, licenses, and legal contracts.</p> <p>Useful commands:</p> <pre><code>pm-team add\npm-team list\npm-team get github\npm-team edit &lt;entry_name&gt;\npm-team delete &lt;entry_name&gt;\npm-team gen\n</code></pre> <p>Practical notes from the current code:</p> <ul> <li><code>pm-team list</code> only prints a subset of entry categories rather than every stored type.</li> <li><code>pm-team edit</code> is strongest for password and TOTP records; other types are less complete.</li> <li>There are also type-specific command groups such as <code>pm-team password ...</code>, <code>pm-team totp ...</code>, <code>pm-team apikey ...</code>, and similar namespaces with <code>add</code>, <code>list</code>, and <code>get</code>.</li> </ul>"},{"location":"guides/team-edition/#approvals","title":"Approvals","text":"<pre><code>pm-team approvals list\npm-team approvals approve &lt;idx&gt;\npm-team approvals deny &lt;idx&gt;\n</code></pre> <p>The approval system is part of the current command tree and is intended for gated changes or access flows around shared entries.</p> <p>Implementation note:</p> <ul> <li>approval handling is most complete for create flows</li> <li>edit and delete approval paths are present in the CLI but are less complete in behavior today</li> </ul>"},{"location":"guides/totp/","title":"Generating TOTP Codes","text":"<p>APM supports Time-Based One-Time Passwords (TOTP) for two-factor authentication. You can store TOTP secrets alongside your credentials, view live codes with countdown timers, and link them to the autofill daemon.</p>"},{"location":"guides/totp/#adding-a-totp-entry","title":"Adding a TOTP Entry","text":"<pre><code>pm add\n# Select type: 2 (TOTP)\n# Account: github.com\n# Secret: JBSWY3DPEHPK3PXP  (your TOTP secret key)\n</code></pre> <p>Tip</p> <p>Most services show the TOTP secret as a QR code during 2FA setup. Look for a \"Can't scan? Show secret key\" link to get the text secret for APM.</p>"},{"location":"guides/totp/#viewing-totp-codes","title":"Viewing TOTP Codes","text":""},{"location":"guides/totp/#interactive-list","title":"Interactive List","text":"<pre><code>pm totp\n</code></pre> <p>Opens an interactive view showing all your TOTP entries with:</p> <ul> <li>Live codes that update every 30 seconds</li> <li>Countdown timers showing time until the next code</li> <li>Persistent ordering \u2014 reorder entries and the order is saved to your vault</li> </ul> <p>Keyboard controls in the interactive TOTP view:</p> Key Action Enter Copy the selected code Up Move selection up Down Move selection down U Move entry up in the order D Move entry down in the order Q Quit the TOTP viewer"},{"location":"guides/totp/#direct-copy","title":"Direct Copy","text":"<p>Copy a specific TOTP code without entering the interactive view:</p> <pre><code>pm totp github\n</code></pre> <p>This fuzzy-matches the entry name and copies the current code to your clipboard.</p>"},{"location":"guides/totp/#totp-order-persistence","title":"TOTP Order Persistence","text":"<p>When you reorder entries in the interactive TOTP view (using U and D keys), the ordering is persisted in your encrypted vault. This means your most-used 2FA accounts stay at the top across sessions and devices (via cloud sync).</p>"},{"location":"guides/totp/#linking-totp-to-autofill-windows","title":"Linking TOTP to Autofill (Windows)","text":"<p>If you use the autofill daemon, you can link a TOTP entry to a domain so that fill sequences can include the OTP code:</p> <pre><code>pm autocomplete link-totp\n</code></pre> <p>This interactive command lets you select a TOTP entry and bind it to a domain/service. When the autofill daemon fills credentials for that domain, it can include the TOTP code in the sequence using the <code>{TOTP}</code> token.</p>"},{"location":"guides/totp/#how-totp-works","title":"How TOTP Works","text":"<p>APM generates TOTP codes using the standard RFC 6238 algorithm:</p> <ol> <li>The TOTP secret is stored in the vault as a base32-encoded string</li> <li>The current Unix timestamp is divided into 30-second intervals</li> <li>An HMAC-SHA1 hash is computed using the secret and the interval counter</li> <li>A 6-digit code is extracted from the hash using dynamic truncation</li> </ol> <p>The code refreshes every 30 seconds. APM's display includes a countdown timer so you know when to expect a new code.</p>"},{"location":"guides/totp/#next-steps","title":"Next Steps","text":"<ul> <li>Autofill on Windows \u2014 Set up autofill with TOTP linking</li> <li>Vault Management \u2014 Managing all entry types</li> </ul>"},{"location":"guides/vault-management/","title":"Managing Your Vault","text":"<p>This guide covers the day-to-day operations of working with your APM vault \u2014 adding entries, searching, editing, organizing with spaces, and using the vocabulary engine for notes.</p>"},{"location":"guides/vault-management/#adding-entries","title":"Adding Entries","text":""},{"location":"guides/vault-management/#interactive-add","title":"Interactive Add","text":"<pre><code>pm add\n</code></pre> <p>This opens an interactive menu showing all 25+ supported secret types. Select a type, then fill in the structured fields. Each type has a specific schema \u2014 for example, an SSH Config entry asks for alias, host, user, port, key path, and fingerprint.</p>"},{"location":"guides/vault-management/#adding-to-a-specific-space","title":"Adding to a Specific Space","text":"<p>If you have spaces configured, entries inherit the currently active space. Switch spaces before adding:</p> <pre><code>pm space switch Work\npm add    # This entry will be in the \"Work\" space\n</code></pre>"},{"location":"guides/vault-management/#field-validation","title":"Field Validation","text":"<p>APM validates fields as you enter them. If you have a password policy loaded, new passwords are validated against the policy's requirements (minimum length, uppercase, numbers, symbols).</p>"},{"location":"guides/vault-management/#searching-and-retrieving-entries","title":"Searching and Retrieving Entries","text":""},{"location":"guides/vault-management/#fuzzy-search","title":"Fuzzy Search","text":"<pre><code>pm get [query]\n</code></pre> <p>APM performs fuzzy search across all entry types simultaneously \u2014 matching against account names, usernames, service names, labels, and other identifiers. Results are ranked by match quality.</p> <p>Search Examples</p> <pre><code>pm get github        # Find GitHub credentials\npm get aws           # Find AWS cloud credentials, API keys, etc.\npm get \"john doe\"    # Find contacts named John Doe\npm get wifi          # Find Wi-Fi passwords\n</code></pre>"},{"location":"guides/vault-management/#interactive-browser","title":"Interactive Browser","text":"<p>When results are returned, APM enters an interactive browser mode. Keyboard controls:</p> Key Action Enter View full entry details C Copy password/secret to clipboard E Edit this entry D Delete this entry S Toggle show/hide sensitive fields Q Quicklook (preview notes, render photos) M View metadata (timestamps, trust score) Esc Exit the browser"},{"location":"guides/vault-management/#clipboard-expiry","title":"Clipboard Expiry","text":"<p>When you copy a secret to the clipboard, APM automatically clears it after a short timeout to prevent accidental exposure.</p>"},{"location":"guides/vault-management/#safe-display","title":"Safe Display","text":"<p>Sensitive fields are hidden by default in search results. To reveal them:</p> <pre><code>pm get github --show-pass\n</code></pre> <p>Or press S in the interactive view.</p>"},{"location":"guides/vault-management/#editing-and-deleting-entries","title":"Editing and Deleting Entries","text":"<p>Entry mutation happens from the interactive <code>pm get</code> browser.</p> <pre><code>pm get github\n</code></pre> <p>From the results view:</p> <ul> <li>Press E to edit the selected entry</li> <li>Press D to delete the selected entry</li> </ul> <p>Edits are tracked by the secret telemetry system, recording who modified the entry, when, and via what context. Deletions are recorded in the audit log.</p> <p>Warning</p> <p>Deletion is permanent. There is no trash or undo. If you need to preserve deleted entries, export your vault first.</p>"},{"location":"guides/vault-management/#organizing-with-spaces","title":"Organizing with Spaces","text":"<p>Spaces provide logical segmentation within a single vault file. They're useful for separating work credentials from personal ones, or organizing by project.</p>"},{"location":"guides/vault-management/#creating-and-managing-spaces","title":"Creating and Managing Spaces","text":"<pre><code># Create a new space\npm space create DevOps\n\n# List all spaces\npm space list\n\n# Switch to a space\npm space switch DevOps\n\n# See the currently active space\npm space current\n\n# Remove a space (entries in it are moved to default)\npm space remove DevOps\n</code></pre>"},{"location":"guides/vault-management/#how-spaces-affect-operations","title":"How Spaces Affect Operations","text":"<ul> <li>Adding \u2014 New entries inherit the active space</li> <li>Searching \u2014 <code>pm get</code> searches within the active space by default</li> <li>Cloud sync \u2014 Spaces can be excluded via <code>.apmignore</code></li> <li>TOTP \u2014 TOTP entries are space-aware</li> </ul>"},{"location":"guides/vault-management/#generating-passwords","title":"Generating Passwords","text":"<pre><code>pm gen\n</code></pre> <p>APM generates a high-entropy password using cryptographically secure randomness. The generated password is displayed and automatically copied to your clipboard.</p> <p>Tip</p> <p>Generate a password first with <code>pm gen</code>, then paste it when <code>pm add</code> asks for the password field.</p>"},{"location":"guides/vault-management/#notes-and-the-vocabulary-engine","title":"Notes and the Vocabulary Engine","text":"<p>Secure notes in APM are more than text blobs. The vocabulary engine builds a compressed index from your notes and provides autocomplete functionality.</p>"},{"location":"guides/vault-management/#enabling-vocabulary","title":"Enabling Vocabulary","text":"<pre><code>pm vocab enable     # Turn on vocabulary indexing\npm vocab disable    # Turn it off\npm vocab status     # Check if it's active\n</code></pre>"},{"location":"guides/vault-management/#writing-notes-with-autocomplete","title":"Writing Notes with Autocomplete","text":"<p>When adding or editing a secure note, APM offers autocomplete suggestions based on your vocabulary. Suggestions are ranked by frequency and user feedback (accepted suggestions increase a word's score; dismissed ones decrease it).</p>"},{"location":"guides/vault-management/#managing-the-vocabulary","title":"Managing the Vocabulary","text":"<pre><code># List all indexed words with scores\npm vocab\n\n# Create an alias (normalizes terms)\npm vocab alias k8s kubernetes\npm vocab alias tf terraform\n\n# List all aliases\npm vocab alias-list\n\n# Remove an alias\npm vocab alias-remove k8s\n\n# Adjust word ranking manually\npm vocab rank deploy +5    # Boost \"deploy\"\npm vocab rank temp -3      # Demote \"temp\"\n\n# Remove a word from the index\npm vocab remove obsolete_term\n\n# Rebuild the entire vocabulary from current notes\npm vocab reindex\n</code></pre>"},{"location":"guides/vault-management/#how-vocabulary-storage-works","title":"How Vocabulary Storage Works","text":"<p>The vocabulary is stored as a gzip-compressed JSON blob inside the encrypted vault (<code>Vault.VocabCompressed</code>). This means:</p> <ul> <li>It's encrypted at rest alongside all other vault data</li> <li>It can be stripped from cloud uploads using <code>ignore:vocab</code> in <code>.apmignore</code></li> <li>It's rebuilt from notes content during reindexing</li> </ul>"},{"location":"guides/vault-management/#vault-health-and-trust","title":"Vault Health and Trust","text":""},{"location":"guides/vault-management/#health-dashboard","title":"Health Dashboard","text":"<pre><code>pm health\n</code></pre> <p>Produces a numeric score (0\u2013100) based on:</p> <ul> <li>Encryption profile \u2014 Hardened/Paranoid profiles earn bonus points; Legacy loses points</li> <li>Alert configuration \u2014 Enabled alerts earn points</li> <li>Weak secrets \u2014 Passwords shorter than 8 characters are penalized</li> <li>High-risk secrets \u2014 Trust scores below 55 (high risk) or 35 (critical) add penalties</li> </ul>"},{"location":"guides/vault-management/#trust-scores","title":"Trust Scores","text":"<pre><code>pm trust\n</code></pre> <p>Every secret gets a trust score based on:</p> Factor Penalty Threshold Marked as exposed \u221245 Any No rotation &gt; 365 days \u221235 Last rotation too old No rotation &gt; 180 days \u221220 Last rotation aging No rotation &gt; 90 days \u221210 Last rotation recent Access count &gt; 200 \u221220 High frequency Access count &gt; 75 \u221210 Elevated frequency Privilege: critical \u221215 Root/admin secrets Privilege: elevated \u22128 Elevated access <p>Risk levels: Low (\u226580), Medium (\u226555), High (\u226535), Critical (&lt;35)</p>"},{"location":"guides/vault-management/#audit-log","title":"Audit Log","text":"<pre><code>pm audit\n</code></pre> <p>Every vault interaction is logged with:</p> <ul> <li>Timestamp \u2014 When the action occurred</li> <li>Action \u2014 What was done (e.g., <code>ENTRY_ADDED</code>, <code>VAULT_UNLOCKED</code>, <code>CLOUD_SYNCED</code>)</li> <li>User \u2014 System username</li> <li>Hostname \u2014 Machine name</li> </ul> <p>The audit log is stored outside the vault at <code>~/.config/apm/audit.json</code> and is append-only.</p>"},{"location":"guides/vault-management/#cryptographic-information","title":"Cryptographic Information","text":"<pre><code>pm cinfo\n</code></pre> <p>Displays the active cryptographic parameters:</p> <ul> <li>Profile name and Argon2id parameters</li> <li>Nonce sizes</li> <li>Vault format version</li> <li>Whether recovery is configured</li> </ul>"},{"location":"guides/vault-management/#next-steps","title":"Next Steps","text":"<ul> <li>Cloud Synchronization \u2014 Sync your vault across devices</li> <li>Sessions \u2014 Advanced session management</li> <li>CLI Reference \u2014 Every command in detail</li> </ul>"},{"location":"reference/","title":"Reference","text":"<p>This section is the factual lookup layer for the current repo state.</p> <ul> <li>CLI reference</li> <li>Storage reference</li> <li>Environment variables</li> <li>Plugin API</li> <li>MCP tools</li> <li>Policies</li> <li><code>.apmignore</code></li> </ul>"},{"location":"reference/#scope","title":"Scope","text":"<ul> <li><code>pm</code> and <code>pm-team</code> are separate binaries.</li> <li>The built-in <code>pm</code> command surface is already large and can grow further at runtime because plugins may register additional commands.</li> <li>The reference pages should be read as describing the current repository implementation, not older releases or screenshots.</li> </ul>"},{"location":"reference/apmignore/","title":".apmignore Reference","text":"<p>Complete format specification for the <code>.apmignore</code> file that controls cloud upload filtering.</p>"},{"location":"reference/apmignore/#file-resolution","title":"File Resolution","text":"<p>APM searches for <code>.apmignore</code> in this order:</p> <ol> <li>Same directory as the vault file</li> <li>Current working directory</li> </ol> <p>The first file found is used. Only one <code>.apmignore</code> file is loaded per sync operation.</p>"},{"location":"reference/apmignore/#syntax","title":"Syntax","text":"<ul> <li>Lines starting with <code>#</code> are comments</li> <li>Empty lines are ignored</li> <li>Section headers are enclosed in <code>[]</code></li> <li>Patterns support <code>*</code> glob wildcards</li> </ul>"},{"location":"reference/apmignore/#sections","title":"Sections","text":""},{"location":"reference/apmignore/#spaces","title":"<code>[spaces]</code>","text":"<p>List space names to exclude from uploads. One per line.</p> <pre><code>[spaces]\nprivate\ntemp_*\narchive\n</code></pre> Pattern Matches <code>private</code> Exactly \"private\" <code>temp_*</code> \"temp_1\", \"temp_staging\", etc. <code>*</code> All spaces (upload nothing)"},{"location":"reference/apmignore/#entries","title":"<code>[entries]</code>","text":"<p>Entry patterns in <code>space:type:name</code> format.</p> <pre><code>[entries]\nwork:password:legacy_admin\n*:notes:*draft*\npersonal:ssh_key:*\n*:*:test_*\n</code></pre> Component Description Wildcard <code>space</code> Space name <code>*</code> = any space <code>type</code> Entry type <code>*</code> = any type <code>name</code> Entry name <code>*</code> = any name <p>Type identifiers: <code>password</code>, <code>totp</code>, <code>token</code>, <code>notes</code>, <code>api_key</code>, <code>ssh_key</code>, <code>ssh_config</code>, <code>wifi</code>, <code>gov_id</code>, <code>medical</code>, <code>travel</code>, <code>contact</code>, <code>cloud_creds</code>, <code>k8s</code>, <code>docker</code>, <code>cicd</code>, <code>recovery_codes</code>, <code>certificate</code>, <code>banking</code>, <code>document</code>, <code>software_license</code>, <code>legal</code>, <code>photo</code>, <code>audio</code>, <code>video</code></p>"},{"location":"reference/apmignore/#cloud-specific-ignore","title":"<code>[cloud-specific-ignore]</code>","text":"<p>Provider-specific entry patterns in <code>provider:space:type:name</code> format.</p> <pre><code>[cloud-specific-ignore]\ndropbox:work:password:legacy_admin\ngithub:*:ssh_key:*\ngdrive:personal:notes:*journal*\n</code></pre> <p>Providers: <code>gdrive</code>, <code>github</code>, <code>dropbox</code></p>"},{"location":"reference/apmignore/#vocab","title":"<code>[vocab]</code>","text":"<p>Specific vocabulary words to exclude.</p> <pre><code>[vocab]\ninternal_codename\nsecret_project\n</code></pre>"},{"location":"reference/apmignore/#misc","title":"<code>[misc]</code>","text":"<p>Miscellaneous flags.</p> <pre><code>[misc]\nignore:vocab\n</code></pre> Flag Effect <code>ignore:vocab</code> Strip the entire compressed vocabulary from uploads"},{"location":"reference/apmignore/#complete-example","title":"Complete Example","text":"<pre><code># ============================================\n# .apmignore \u2014 Cloud Upload Filter\n# ============================================\n\n# Never sync these spaces\n[spaces]\nprivate\ntemp_*\n\n# Exclude specific entries from all providers\n[entries]\nwork:password:local_dev_db\n*:notes:*scratch*\n*:ssh_key:id_ed25519_local\n\n# Provider-specific exclusions\n[cloud-specific-ignore]\ndropbox:work:password:legacy_admin\ngithub:*:document:*confidential*\n\n# Words to strip from vocabulary\n[vocab]\ninternal_project_alpha\n\n# Strip entire vocabulary\n[misc]\nignore:vocab\n</code></pre>"},{"location":"reference/cli/","title":"CLI Reference","text":"<p>This reference reflects the command trees defined in the current source.</p>"},{"location":"reference/cli/#personal-binary-pm","title":"Personal binary: <code>pm</code>","text":""},{"location":"reference/cli/#core-vault-flow","title":"Core vault flow","text":"<ul> <li><code>pm setup</code></li> <li><code>pm unlock</code></li> <li><code>pm readonly &lt;mins&gt;</code></li> <li><code>pm lock</code></li> <li><code>pm mode</code></li> <li><code>pm cinfo</code></li> <li><code>pm info</code></li> <li><code>pm tui</code></li> </ul>"},{"location":"reference/cli/#entries-and-retrieval","title":"Entries and retrieval","text":"<ul> <li><code>pm add [type]</code></li> <li><code>pm get [query]</code></li> <li><code>pm gen</code></li> <li><code>pm totp [entry_name]</code></li> <li><code>pm import &lt;file&gt;</code></li> <li><code>pm export</code></li> </ul> <p><code>pm add</code> currently supports 25 types. <code>pm get</code> is the main interactive search and management flow.</p>"},{"location":"reference/cli/#sessions","title":"Sessions","text":"<ul> <li><code>pm session issue</code></li> <li><code>pm session list</code></li> <li><code>pm session revoke &lt;id&gt;</code></li> </ul> <p>Ephemeral sessions can be bound to host, PID, and agent identity.</p>"},{"location":"reference/cli/#recovery-and-auth","title":"Recovery and auth","text":"<ul> <li><code>pm auth email [address]</code></li> <li><code>pm auth recover</code></li> <li><code>pm auth reset</code></li> <li><code>pm auth change</code></li> <li><code>pm auth alerts</code></li> <li><code>pm auth level [1-3]</code></li> <li><code>pm auth quorum-setup</code></li> <li><code>pm auth quorum-recover</code></li> <li><code>pm auth passkey register</code></li> <li><code>pm auth passkey verify</code></li> <li><code>pm auth passkey disable</code></li> <li><code>pm auth codes generate</code></li> <li><code>pm auth codes status</code></li> </ul>"},{"location":"reference/cli/#profiles-spaces-and-policy","title":"Profiles, spaces, and policy","text":"<ul> <li><code>pm profile list</code></li> <li><code>pm profile current</code></li> <li><code>pm profile set &lt;name&gt;</code></li> <li><code>pm profile edit [name]</code></li> <li><code>pm profile create &lt;name&gt;</code></li> <li><code>pm space create [name]</code></li> <li><code>pm space switch [name]</code></li> <li><code>pm space list</code></li> <li><code>pm policy load [name]</code></li> <li><code>pm policy show</code></li> <li><code>pm policy clear</code></li> </ul>"},{"location":"reference/cli/#cloud","title":"Cloud","text":"<ul> <li><code>pm cloud init [gdrive|github|dropbox|all]</code></li> <li><code>pm cloud sync [gdrive|github|dropbox]</code></li> <li><code>pm cloud auto-sync</code></li> <li><code>pm cloud get [gdrive|github|dropbox] [retrieval_key|repo]</code></li> <li><code>pm cloud diff [gdrive|github|dropbox]</code></li> <li><code>pm cloud delete [gdrive]</code></li> <li><code>pm cloud reset</code></li> </ul> <p>Notes:</p> <ul> <li>Google Drive and Dropbox support <code>APM_PUBLIC</code> and <code>self_hosted</code> modes.</li> <li>GitHub uses token-based auth and a repository target.</li> <li><code>cloud get</code> can work with provider identifiers such as repo, file ID, or Dropbox path.</li> </ul>"},{"location":"reference/cli/#plugins","title":"Plugins","text":"<ul> <li><code>pm plugins installed</code></li> <li><code>pm plugins list</code></li> <li><code>pm plugins market</code></li> <li><code>pm plugins add [name]</code></li> <li><code>pm plugins install [name]</code></li> <li><code>pm plugins push [name]</code></li> <li><code>pm plugins remove [name]</code></li> <li><code>pm plugins search</code></li> <li><code>pm plugins local [path]</code></li> <li><code>pm plugins access [plugin] [permission] [on|off]</code></li> <li><code>pm plugins run [plugin] [command] [args...]</code></li> </ul> <p>Plugins may also register extra root-level commands.</p>"},{"location":"reference/cli/#mcp","title":"MCP","text":"<ul> <li><code>pm mcp config</code></li> <li><code>pm mcp token</code></li> <li><code>pm mcp list</code></li> <li><code>pm mcp revoke [name_or_token]</code></li> <li><code>pm mcp serve</code></li> </ul>"},{"location":"reference/cli/#autofill-and-autocomplete","title":"Autofill and autocomplete","text":"<ul> <li><code>pm autofill start</code></li> <li><code>pm autofill stop</code></li> <li><code>pm autofill status</code></li> <li><code>pm autofill list-profiles</code></li> <li><code>pm autofill daemon</code></li> <li><code>pm autocomplete enable</code></li> <li><code>pm autocomplete disable</code></li> <li><code>pm autocomplete start</code></li> <li><code>pm autocomplete stop</code></li> <li><code>pm autocomplete status</code></li> <li><code>pm autocomplete window enable</code></li> <li><code>pm autocomplete window disable</code></li> <li><code>pm autocomplete window status</code></li> <li><code>pm autocomplete link-totp</code></li> </ul> <p>These flows are primarily relevant on Windows.</p>"},{"location":"reference/cli/#notes-and-vocabulary","title":"Notes and vocabulary","text":"<ul> <li><code>pm vocab</code></li> <li><code>pm vocab enable</code></li> <li><code>pm vocab disable</code></li> <li><code>pm vocab status</code></li> <li><code>pm vocab alias</code></li> <li><code>pm vocab alias-list</code></li> <li><code>pm vocab alias-remove [alias]</code></li> <li><code>pm vocab rank [word] [delta]</code></li> <li><code>pm vocab remove [word]</code></li> <li><code>pm vocab reindex</code></li> </ul>"},{"location":"reference/cli/#auditing-and-diagnostics","title":"Auditing and diagnostics","text":"<ul> <li><code>pm health</code></li> <li><code>pm trust</code></li> <li><code>pm audit</code></li> <li><code>pm loaded</code></li> <li><code>pm brutetest [minutes]</code></li> <li><code>pm compromise</code></li> <li><code>pm update</code></li> </ul>"},{"location":"reference/cli/#optional-command-trees","title":"Optional command trees","text":"<p>Depending on build and runtime state, <code>pm</code> also exposes:</p> <ul> <li><code>pm inject ...</code></li> <li><code>pm faceid ...</code></li> <li>plugin-defined root commands</li> </ul>"},{"location":"reference/cli/#team-binary-pm-team","title":"Team binary: <code>pm-team</code>","text":""},{"location":"reference/cli/#session-and-identity","title":"Session and identity","text":"<ul> <li><code>pm-team init &lt;org_name&gt; &lt;admin_username&gt;</code></li> <li><code>pm-team login &lt;username&gt;</code></li> <li><code>pm-team whoami</code></li> <li><code>pm-team logout</code></li> </ul>"},{"location":"reference/cli/#departments","title":"Departments","text":"<ul> <li><code>pm-team dept list</code></li> <li><code>pm-team dept create &lt;name&gt;</code></li> <li><code>pm-team dept switch &lt;username&gt; &lt;dept_id&gt;</code></li> </ul>"},{"location":"reference/cli/#users","title":"Users","text":"<ul> <li><code>pm-team user list</code></li> <li><code>pm-team user add &lt;username&gt;</code></li> <li><code>pm-team user remove &lt;username&gt;</code></li> <li><code>pm-team user promote &lt;username&gt; &lt;role&gt;</code></li> <li><code>pm-team user roles</code></li> <li><code>pm-team user permission grant &lt;username&gt; &lt;permission&gt;</code></li> <li><code>pm-team user permission revoke &lt;username&gt; &lt;permission&gt;</code></li> </ul>"},{"location":"reference/cli/#shared-vault-operations","title":"Shared vault operations","text":"<ul> <li><code>pm-team add</code></li> <li><code>pm-team list</code></li> <li><code>pm-team get [query]</code></li> <li><code>pm-team gen</code></li> <li><code>pm-team edit &lt;entry_name&gt;</code></li> <li><code>pm-team delete &lt;entry_name&gt;</code></li> </ul> <p>Implementation notes:</p> <ul> <li><code>pm-team list</code> currently prints only some shared entry categories.</li> <li><code>pm-team get</code> is the main search path for broader retrieval.</li> <li><code>pm-team edit</code> is only fully implemented for a smaller subset of entry types.</li> </ul>"},{"location":"reference/cli/#approvals-and-reporting","title":"Approvals and reporting","text":"<ul> <li><code>pm-team approvals list</code></li> <li><code>pm-team approvals approve &lt;idx&gt;</code></li> <li><code>pm-team approvals deny &lt;idx&gt;</code></li> <li><code>pm-team export</code></li> <li><code>pm-team audit</code></li> <li><code>pm-team health</code></li> <li><code>pm-team info</code></li> </ul>"},{"location":"reference/cli/#type-specific-shared-namespaces","title":"Type-specific shared namespaces","text":"<p>The team binary also registers type-focused command groups with <code>add</code>, <code>list</code>, and <code>get</code> subcommands:</p> <ul> <li><code>pm-team password</code></li> <li><code>pm-team totp</code></li> <li><code>pm-team apikey</code></li> <li><code>pm-team token</code></li> <li><code>pm-team note</code></li> <li><code>pm-team ssh</code></li> <li><code>pm-team cert</code></li> <li><code>pm-team wifi</code></li> <li><code>pm-team recovery</code></li> <li><code>pm-team banking</code></li> <li><code>pm-team doc</code></li> <li><code>pm-team gov</code></li> <li><code>pm-team medical</code></li> <li><code>pm-team travel</code></li> <li><code>pm-team contact</code></li> <li><code>pm-team cloud</code></li> <li><code>pm-team k8s</code></li> <li><code>pm-team docker</code></li> <li><code>pm-team ssh-config</code></li> <li><code>pm-team cicd</code></li> <li><code>pm-team license</code></li> <li><code>pm-team legal</code></li> </ul>"},{"location":"reference/environment-variables/","title":"Environment Variables","text":"<p>All environment variables supported by APM.</p>"},{"location":"reference/environment-variables/#core-variables","title":"Core Variables","text":"Variable Description Default <code>APM_VAULT_PATH</code> Override the vault file location <code>./vault.dat</code> <code>APM_SESSION_ID</code> Scope sessions to a specific shell instance <code>global</code> <code>APM_EPHEMERAL_ID</code> Use an ephemeral delegated session instead of the global (none)"},{"location":"reference/environment-variables/#context-variables","title":"Context Variables","text":"Variable Description Default <code>APM_ACTOR</code> Identifies the actor in telemetry and audit logs (system user) <code>APM_CONTEXT</code> Execution context identifier (e.g., <code>mcp</code>, <code>ci</code>) (none)"},{"location":"reference/environment-variables/#variable-details","title":"Variable Details","text":""},{"location":"reference/environment-variables/#apm_vault_path","title":"<code>APM_VAULT_PATH</code>","text":"<p>Overrides the default vault file location for all commands:</p> <pre><code>export APM_VAULT_PATH=\"/secure/vault.dat\"\npm unlock  # Uses /secure/vault.dat instead of ./vault.dat\n</code></pre>"},{"location":"reference/environment-variables/#apm_session_id","title":"<code>APM_SESSION_ID</code>","text":"<p>Creates independent sessions per terminal. Each unique value gets its own session file:</p> <pre><code># Terminal 1\nexport APM_SESSION_ID=\"dev\"\npm unlock\n\n# Terminal 2\nexport APM_SESSION_ID=\"ops\"\npm unlock\n# Both terminals have independent sessions\n</code></pre> <p>Session file naming: <code>$TEMP/pm_session_{APM_SESSION_ID}.json</code></p>"},{"location":"reference/environment-variables/#apm_ephemeral_id","title":"<code>APM_EPHEMERAL_ID</code>","text":"<p>Uses a previously issued ephemeral session for authentication:</p> <pre><code>export APM_EPHEMERAL_ID=\"eps_a1b2c3d4...\"\npm get github  # Authenticated via ephemeral session\n</code></pre> <p>When set, APM validates the ephemeral session's bindings (host, PID, agent) instead of checking the regular session file.</p>"},{"location":"reference/environment-variables/#apm_actor","title":"<code>APM_ACTOR</code>","text":"<p>Tags audit log entries and telemetry with the actor name:</p> <pre><code>export APM_ACTOR=\"github-actions\"\npm get aws_credentials  # Audit log shows actor = \"github-actions\"\n</code></pre> <p>Used by ephemeral sessions to verify agent binding.</p>"},{"location":"reference/environment-variables/#apm_context","title":"<code>APM_CONTEXT</code>","text":"<p>Identifies the execution context:</p> <pre><code>export APM_CONTEXT=\"mcp\"  # Marks operations as MCP-initiated\nexport APM_CONTEXT=\"ci\"   # Marks operations as CI/CD-initiated\n</code></pre> <p>Affects telemetry recording (e.g., <code>last_accessor</code> field in secret telemetry).</p>"},{"location":"reference/mcp-tools/","title":"MCP Tools Reference","text":"<p>Complete reference for every tool exposed by the APM MCP server, including schemas, required permissions, and transaction behavior.</p>"},{"location":"reference/mcp-tools/#read-tools","title":"Read Tools","text":""},{"location":"reference/mcp-tools/#list_entries","title":"<code>list_entries</code>","text":"<p>List all entries in the vault (metadata only).</p> <p>Required scope: <code>read</code></p> <p>Returns: Array of entry summaries with IDs, names, types, spaces, and creation dates. Sensitive fields (passwords, keys) are not included.</p>"},{"location":"reference/mcp-tools/#search_entries","title":"<code>search_entries</code>","text":"<p>Search entries by query string.</p> <p>Required scope: <code>read</code></p> <p>Parameters:</p> Parameter Type Required Description <code>query</code> <code>string</code> \u2705 Search query (fuzzy) <code>space</code> <code>string</code> \u274c Filter by space <code>type</code> <code>string</code> \u274c Filter by entry type <p>Returns: Matching entries with relevance scoring.</p>"},{"location":"reference/mcp-tools/#get_entry","title":"<code>get_entry</code>","text":"<p>Get a specific entry by ID or name.</p> <p>Required scope: <code>read</code> (metadata) or <code>secrets</code> (sensitive fields)</p> <p>Parameters:</p> Parameter Type Required Description <code>id</code> <code>string</code> \u274c Entry UUID <code>name</code> <code>string</code> \u274c Entry name (fuzzy match) <code>include_secret</code> <code>bool</code> \u274c Include sensitive fields <p>Notes: If <code>include_secret</code> is true, the <code>secrets</code> scope is required. Without it, passwords, keys, and tokens are redacted.</p>"},{"location":"reference/mcp-tools/#secret-tools","title":"Secret Tools","text":""},{"location":"reference/mcp-tools/#decrypt_entry","title":"<code>decrypt_entry</code>","text":"<p>Retrieve the full entry including all sensitive fields.</p> <p>Required scope: <code>secrets</code></p> <p>Parameters:</p> Parameter Type Required Description <code>id</code> <code>string</code> \u2705 Entry UUID"},{"location":"reference/mcp-tools/#get_totp","title":"<code>get_totp</code>","text":"<p>Generate a TOTP code for a specific entry.</p> <p>Required scope: <code>secrets</code></p> <p>Parameters:</p> Parameter Type Required Description <code>account</code> <code>string</code> \u2705 TOTP account name <p>Returns: Current 6-digit TOTP code and seconds remaining.</p>"},{"location":"reference/mcp-tools/#write-tools","title":"Write Tools","text":"<p>All write tools use transaction guardrails.</p>"},{"location":"reference/mcp-tools/#add_entry","title":"<code>add_entry</code>","text":"<p>Add a new entry to the vault.</p> <p>Required scope: <code>write</code></p> <p>Parameters:</p> Parameter Type Required Description <code>type</code> <code>string</code> \u2705 Entry type <code>fields</code> <code>object</code> \u2705 Entry fields (type-specific) <code>space</code> <code>string</code> \u274c Target space <code>tx_id</code> <code>string</code> \u274c Transaction ID (for commit) <code>approve</code> <code>bool</code> \u274c Commit flag <p>Transaction flow:</p> <ol> <li>First call (no <code>tx_id</code>) \u2192 Returns preview + <code>tx_id</code></li> <li>Second call (<code>tx_id</code> + <code>approve: true</code>) \u2192 Commits and returns receipt ID</li> </ol>"},{"location":"reference/mcp-tools/#edit_entry","title":"<code>edit_entry</code>","text":"<p>Edit an existing entry.</p> <p>Required scope: <code>write</code></p> <p>Parameters:</p> Parameter Type Required Description <code>id</code> <code>string</code> \u2705 Entry UUID <code>fields</code> <code>object</code> \u2705 Updated fields <code>tx_id</code> <code>string</code> \u274c Transaction ID <code>approve</code> <code>bool</code> \u274c Commit flag"},{"location":"reference/mcp-tools/#delete_entry","title":"<code>delete_entry</code>","text":"<p>Delete an entry.</p> <p>Required scope: <code>write</code></p> <p>Parameters:</p> Parameter Type Required Description <code>id</code> <code>string</code> \u2705 Entry UUID <code>tx_id</code> <code>string</code> \u274c Transaction ID <code>approve</code> <code>bool</code> \u274c Commit flag"},{"location":"reference/mcp-tools/#manage_spaces","title":"<code>manage_spaces</code>","text":"<p>Create, switch, or remove spaces.</p> <p>Required scope: <code>write</code></p> <p>Parameters:</p> Parameter Type Required Description <code>action</code> <code>string</code> \u2705 <code>create</code>, <code>switch</code>, or <code>remove</code> <code>name</code> <code>string</code> \u2705 Space name"},{"location":"reference/mcp-tools/#install_plugin","title":"<code>install_plugin</code>","text":"<p>Install a plugin from the marketplace.</p> <p>Required scope: <code>write</code></p>"},{"location":"reference/mcp-tools/#cloud_sync","title":"<code>cloud_sync</code>","text":"<p>Trigger a cloud synchronization.</p> <p>Required scope: <code>write</code></p>"},{"location":"reference/mcp-tools/#admin-tools","title":"Admin Tools","text":""},{"location":"reference/mcp-tools/#manage_profiles","title":"<code>manage_profiles</code>","text":"<p>View or change the encryption profile.</p> <p>Required scope: <code>admin</code></p>"},{"location":"reference/mcp-tools/#cloud_config","title":"<code>cloud_config</code>","text":"<p>View or modify cloud provider configuration.</p> <p>Required scope: <code>admin</code></p>"},{"location":"reference/mcp-tools/#get_history","title":"<code>get_history</code>","text":"<p>Retrieve vault modification history.</p> <p>Required scope: <code>admin</code></p>"},{"location":"reference/mcp-tools/#get_audit_logs","title":"<code>get_audit_logs</code>","text":"<p>Read the audit log.</p> <p>Required scope: <code>admin</code></p>"},{"location":"reference/mcp-tools/#transaction-guardrails-summary","title":"Transaction Guardrails Summary","text":"Tool Uses Transaction Preview Phase Commit Phase <code>add_entry</code> \u2705 Returns preview + tx_id Requires tx_id + approve: true <code>edit_entry</code> \u2705 Returns preview + tx_id Requires tx_id + approve: true <code>delete_entry</code> \u2705 Returns preview + tx_id Requires tx_id + approve: true <code>manage_spaces</code> \u274c \u2014 Executes immediately All read tools \u274c \u2014 Executes immediately"},{"location":"reference/mcp-tools/#error-codes","title":"Error Codes","text":"Code Description <code>PERMISSION_DENIED</code> Token lacks required scope <code>SESSION_REQUIRED</code> No active APM session <code>ENTRY_NOT_FOUND</code> Entry ID/name not found <code>TRANSACTION_EXPIRED</code> tx_id expired or already committed <code>INVALID_PARAMETERS</code> Missing or malformed parameters <code>VAULT_LOCKED</code> Vault is not unlocked"},{"location":"reference/plugin-api/","title":"Plugin API Reference","text":"<p>Complete reference for the APM plugin system \u2014 manifest schema, permission catalog, step commands, and hook system.</p>"},{"location":"reference/plugin-api/#manifest-schema","title":"Manifest Schema","text":"<pre><code>{\n  \"name\": \"string (required)\",\n  \"version\": \"string (required, semver)\",\n  \"description\": \"string\",\n  \"author\": \"string\",\n  \"permissions_required\": [\"string[]\"],\n  \"allowed_file_types\": [\"string[]\"],\n  \"commands\": [\"string[]\"],\n  \"hooks\": [\"string[]\"]\n}\n</code></pre>"},{"location":"reference/plugin-api/#field-rules","title":"Field Rules","text":"Field Required Validation <code>name</code> \u2705 Non-empty string <code>version</code> \u2705 Semantic versioning (<code>X.Y.Z[-prerelease]</code>) <code>description</code> \u274c Free text <code>author</code> \u274c Free text <code>permissions_required</code> \u2705 Only known permissions or <code>category.*</code> <code>allowed_file_types</code> \u274c File extensions with leading dot <code>commands</code> \u274c Command name strings <code>hooks</code> \u274c <code>pre:command</code> or <code>post:command</code> patterns"},{"location":"reference/plugin-api/#complete-permission-catalog","title":"Complete Permission Catalog","text":""},{"location":"reference/plugin-api/#vault-permissions","title":"Vault Permissions","text":"Permission Description <code>vault.read</code> Read vault entries and metadata <code>vault.write</code> Write/modify vault entries <code>vault.delete</code> Delete vault entries <code>vault.import</code> Import data into the vault <code>vault.export</code> Export data from the vault <code>vault.backup</code> Create vault backups <code>vault.restore</code> Restore from vault backups <code>vault.history</code> Access vault modification history <code>vault.lock</code> Lock the vault <code>vault.unlock</code> Unlock the vault <code>vault.sync</code> Trigger cloud synchronization"},{"location":"reference/plugin-api/#vault-item-permissions","title":"Vault Item Permissions","text":"Permission Description <code>vault.item.create</code> Create new vault entries <code>vault.item.read</code> Read existing entries <code>vault.item.update</code> Update existing entries <code>vault.item.delete</code> Delete entries <code>vault.item.move</code> Move entries between spaces <code>vault.item.copy</code> Copy entries <code>vault.item.share</code> Share entries (team edition)"},{"location":"reference/plugin-api/#vault-field-permissions","title":"Vault Field Permissions","text":"Permission Description <code>vault.item.field.password.read</code> Read password fields <code>vault.item.field.password.write</code> Write password fields <code>vault.item.field.username.read</code> Read username fields <code>vault.item.field.username.write</code> Write username fields <code>vault.item.field.url.read</code> Read URL fields <code>vault.item.field.url.write</code> Write URL fields <code>vault.item.field.notes.read</code> Read note content <code>vault.item.field.notes.write</code> Write note content <code>vault.item.field.totp.read</code> Read TOTP secrets <code>vault.item.field.totp.write</code> Write TOTP secrets <code>vault.item.field.tags.read</code> Read entry tags <code>vault.item.field.tags.write</code> Write entry tags <code>vault.item.field.metadata.read</code> Read entry metadata <code>vault.item.field.metadata.write</code> Write entry metadata <code>vault.item.field.custom.read</code> Read custom fields <code>vault.item.field.custom.write</code> Write custom fields"},{"location":"reference/plugin-api/#network-permissions","title":"Network Permissions","text":"Permission Description <code>network.outbound</code> General outbound network access <code>network.inbound</code> Accept inbound connections <code>network.http</code> HTTP requests <code>network.https</code> HTTPS requests <code>network.ftp</code> FTP connections <code>network.sftp</code> SFTP connections <code>network.ssh</code> SSH connections <code>network.ws</code> WebSocket connections <code>network.wss</code> Secure WebSocket connections <code>network.tcp</code> Raw TCP connections <code>network.udp</code> UDP connections <code>network.icmp</code> ICMP (ping) <code>network.proxy</code> Proxy connections <code>network.dns</code> DNS queries <code>network.api.rest</code> REST API calls <code>network.api.grpc</code> gRPC API calls"},{"location":"reference/plugin-api/#system-permissions","title":"System Permissions","text":"Permission Description <code>system.read</code> Read system information <code>system.write</code> Write system files <code>system.exec</code> Execute system commands <code>system.env.read</code> Read environment variables <code>system.env.write</code> Set environment variables <code>system.process.read</code> Read process information <code>system.process.write</code> Modify processes <code>system.process.kill</code> Kill processes <code>system.clipboard.read</code> Read from clipboard <code>system.clipboard.write</code> Write to clipboard <code>system.notification</code> Show system notifications <code>system.audio.record</code> Record audio <code>system.audio.play</code> Play audio <code>system.camera</code> Access camera <code>system.location</code> Access location services <code>system.power</code> Control power state <code>system.usb.read</code> Read USB devices <code>system.usb.write</code> Write to USB devices <code>system.bluetooth</code> Access Bluetooth <code>system.wifi</code> Access Wi-Fi interfaces"},{"location":"reference/plugin-api/#cryptography-permissions","title":"Cryptography Permissions","text":"Permission Description <code>crypto.use</code> General cryptography access <code>crypto.hash</code> Compute hashes <code>crypto.random</code> Generate random data <code>crypto.encrypt</code> Encrypt data <code>crypto.decrypt</code> Decrypt data <code>crypto.sign</code> Create digital signatures <code>crypto.verify</code> Verify digital signatures <code>crypto.key.generate</code> Generate cryptographic keys <code>crypto.key.store</code> Store keys <code>crypto.key.load</code> Load stored keys <code>crypto.key.delete</code> Delete stored keys <code>crypto.cert.generate</code> Generate certificates <code>crypto.cert.validate</code> Validate certificates"},{"location":"reference/plugin-api/#file-storage","title":"File Storage","text":"Permission Description <code>file.storage</code> Read and write files (limited to allowed types)"},{"location":"reference/plugin-api/#plugin-management-permissions","title":"Plugin Management Permissions","text":"Permission Description <code>plugin.list</code> List installed plugins <code>plugin.install</code> Install plugins <code>plugin.uninstall</code> Remove plugins <code>plugin.update</code> Update plugins <code>plugin.config.read</code> Read plugin configuration <code>plugin.config.write</code> Write plugin configuration <code>plugin.reload</code> Reload plugin state"},{"location":"reference/plugin-api/#ui-permissions","title":"UI Permissions","text":"Permission Description <code>ui.prompt</code> Show user prompts <code>ui.alert</code> Show alert dialogs <code>ui.confirm</code> Show confirmation dialogs <code>ui.toast</code> Show toast notifications <code>ui.dialog</code> Show custom dialogs <code>ui.window.open</code> Open windows <code>ui.window.close</code> Close windows <code>ui.window.maximize</code> Maximize windows <code>ui.window.minimize</code> Minimize windows <code>ui.menu.add</code> Add menu items <code>ui.menu.remove</code> Remove menu items <code>ui.theme.set</code> Change UI theme <code>ui.font.set</code> Change UI font"},{"location":"reference/plugin-api/#user-session-permissions","title":"User &amp; Session Permissions","text":"Permission Description <code>user.read</code> Read user information <code>user.write</code> Modify user information <code>user.auth</code> Trigger authentication <code>user.session.read</code> Read session data <code>user.session.write</code> Modify session data <code>user.profile.read</code> Read user profile <code>user.profile.write</code> Modify user profile <code>user.biometric</code> Access biometric authentication"},{"location":"reference/plugin-api/#audit-permissions","title":"Audit Permissions","text":"Permission Description <code>audit.read</code> Read audit data <code>audit.write</code> Write audit entries <code>audit.log.read</code> Read audit logs <code>audit.log.write</code> Write to audit logs <code>audit.alert.read</code> Read audit alerts <code>audit.report</code> Generate audit reports"},{"location":"reference/plugin-api/#database-permissions","title":"Database Permissions","text":"Permission Description <code>db.read</code> Read internal database <code>db.write</code> Write to internal database <code>db.query</code> Execute database queries <code>db.schema.read</code> Read database schema <code>db.schema.write</code> Modify database schema"},{"location":"reference/plugin-api/#ai-ml-permissions","title":"AI / ML Permissions","text":"Permission Description <code>ai.model.load</code> Load ML models <code>ai.predict</code> Run model predictions <code>ai.train</code> Train models"},{"location":"reference/plugin-api/#iot-hardware-permissions","title":"IoT / Hardware Permissions","text":"Permission Description <code>iot.scan</code> Scan for IoT devices <code>iot.connect</code> Connect to IoT devices <code>iot.control</code> Control IoT devices"},{"location":"reference/plugin-api/#cloud-permissions","title":"Cloud Permissions","text":"Permission Description <code>cloud.sync</code> Trigger cloud sync <code>cloud.backup</code> Create cloud backups <code>cloud.restore</code> Restore from cloud <code>cloud.config.read</code> Read cloud configuration <code>cloud.config.write</code> Modify cloud configuration"},{"location":"reference/plugin-api/#wildcard-permissions","title":"Wildcard Permissions","text":"<p>Use <code>category.*</code> syntax for group-level access:</p> <pre><code>\"permissions_required\": [\"vault.*\", \"network.*\", \"system.*\"]\n</code></pre>"},{"location":"reference/plugin-api/#step-commands","title":"Step Commands","text":"<p>The step executor supports these built-in commands:</p> Command Required Permission Description <code>vault.list</code> <code>vault.read</code> List vault entries <code>vault.get</code> <code>vault.read</code> Get a specific entry <code>vault.add</code> <code>vault.write</code> Add a new entry <code>vault.edit</code> <code>vault.write</code> Edit an existing entry <code>vault.delete</code> <code>vault.delete</code> Delete an entry <code>vault.search</code> <code>vault.read</code> Search entries <code>file.read</code> <code>file.storage</code> Read a file <code>file.write</code> <code>file.storage</code> Write a file <code>file.delete</code> <code>file.storage</code> Delete a file <code>file.list</code> <code>file.storage</code> List files <code>exec</code> <code>system.exec</code> Execute shell command <code>http.get</code> <code>network.http</code> HTTP GET request <code>http.post</code> <code>network.http</code> HTTP POST request <code>http.download</code> <code>network.http</code> Download a file <code>crypto.hash</code> <code>crypto.hash</code> Compute SHA-256 hash <code>crypto.random</code> <code>crypto.random</code> Generate random bytes <code>set</code> (none) Set a variable <code>print</code> (none) Output text <code>prompt</code> <code>ui.prompt</code> Ask user for input <code>confirm</code> <code>ui.confirm</code> Ask yes/no question <code>toast</code> <code>ui.toast</code> Show notification <code>clipboard</code> <code>system.clipboard.*</code> Copy to clipboard"},{"location":"reference/plugin-api/#variable-substitution","title":"Variable Substitution","text":"<p>Steps can reference variables from previous steps:</p> <pre><code>${variable_name}    \u2192 Value of the variable\n${output.0}         \u2192 First line of previous step output\n</code></pre>"},{"location":"reference/plugin-api/#hook-system","title":"Hook System","text":""},{"location":"reference/plugin-api/#hook-patterns","title":"Hook Patterns","text":"<pre><code>pre:&lt;command&gt;   \u2014 Fires before the command executes\npost:&lt;command&gt;  \u2014 Fires after the command completes\n</code></pre>"},{"location":"reference/plugin-api/#available-hooks","title":"Available Hooks","text":"Hook Fires When <code>pre:add</code> Before an entry is added <code>post:add</code> After an entry is added <code>pre:edit</code> Before an entry is edited <code>post:edit</code> After an entry is edited <code>pre:delete</code> Before an entry is deleted <code>post:delete</code> After an entry is deleted <code>pre:sync</code> Before cloud sync <code>post:sync</code> After cloud sync <code>pre:unlock</code> Before vault unlock <code>post:unlock</code> After vault unlock <code>pre:lock</code> Before vault lock <code>post:lock</code> After vault lock <code>pre:export</code> Before export <code>post:export</code> After export <code>pre:import</code> Before import <code>post:import</code> After import <p>If a <code>pre:</code> hook returns an error, the associated command is aborted.</p>"},{"location":"reference/plugin-api/#runtime-permission-overrides","title":"Runtime Permission Overrides","text":"<p>Users can override plugin permissions after installation:</p> <pre><code># Interactive toggle list (space key to select/deselect)\npm plugins access\n\n# Direct toggle\npm plugins access &lt;plugin&gt; &lt;permission&gt; on|off\n</code></pre> <p>Overrides are stored inside the encrypted vault under <code>plugin_permission_overrides</code> and synced across devices.</p> <p>The effective permissions for a plugin are:</p> <pre><code>effective = declared_permissions \u2229 user_overrides\n</code></pre> <p>If a user disables a permission, the plugin cannot use it regardless of what the manifest declares.</p>"},{"location":"reference/policies/","title":"Policies Reference","text":"<p>YAML policy schema for the APM policy engine.</p>"},{"location":"reference/policies/#schema","title":"Schema","text":"<pre><code>name: string (required)\ndescription: string (optional)\n\npassword_policy:\n  min_length: integer (default: 8)\n  require_uppercase: boolean (default: false)\n  require_numbers: boolean (default: false)\n  require_symbols: boolean (default: false)\n\nrotation_policy:\n  rotate_every_days: integer (default: 0, 0 = disabled)\n  notify_before_days: integer (default: 0)\n\nclassification:\n  entry_name: privilege_level\n  # privilege_level: critical | root | admin | elevated | normal\n</code></pre>"},{"location":"reference/policies/#field-details","title":"Field Details","text":""},{"location":"reference/policies/#password_policy","title":"<code>password_policy</code>","text":"Field Type Default Description <code>min_length</code> <code>int</code> 8 Minimum characters <code>require_uppercase</code> <code>bool</code> false At least one uppercase letter (A-Z) <code>require_numbers</code> <code>bool</code> false At least one digit (0-9) <code>require_symbols</code> <code>bool</code> false At least one special character"},{"location":"reference/policies/#rotation_policy","title":"<code>rotation_policy</code>","text":"Field Type Default Description <code>rotate_every_days</code> <code>int</code> 0 Rotation interval (0 = disabled) <code>notify_before_days</code> <code>int</code> 0 Pre-rotation notification window"},{"location":"reference/policies/#classification","title":"<code>classification</code>","text":"<p>Maps entry names to privilege levels. Used by the trust scoring system:</p> Level Trust Penalty Description <code>critical</code> \u221215 Highest privilege <code>root</code> \u221215 Root-level access <code>admin</code> \u221212 Administrative access <code>elevated</code> \u22128 Above-normal privilege <code>normal</code> 0 Standard access"},{"location":"reference/policies/#loading-policies","title":"Loading Policies","text":"<pre><code>pm policy load ./policies/\n</code></pre> <p>Loads all <code>.yaml</code> and <code>.yml</code> files from the directory. Multiple files are merged in filesystem order.</p>"},{"location":"reference/policies/#examples","title":"Examples","text":""},{"location":"reference/policies/#minimal-policy","title":"Minimal Policy","text":"<pre><code>name: basic\npassword_policy:\n  min_length: 12\n</code></pre>"},{"location":"reference/policies/#enterprise-policy","title":"Enterprise Policy","text":"<pre><code>name: enterprise\ndescription: Corporate security standard\n\npassword_policy:\n  min_length: 16\n  require_uppercase: true\n  require_numbers: true\n  require_symbols: true\n\nrotation_policy:\n  rotate_every_days: 60\n  notify_before_days: 14\n\nclassification:\n  aws_root_key: critical\n  production_db: critical\n  staging_api: elevated\n  ci_token: normal\n</code></pre>"},{"location":"reference/policies/#developer-team-policy","title":"Developer Team Policy","text":"<pre><code>name: dev-team\ndescription: Balanced policy for development teams\n\npassword_policy:\n  min_length: 12\n  require_uppercase: true\n  require_numbers: true\n  require_symbols: false\n\nrotation_policy:\n  rotate_every_days: 180\n  notify_before_days: 30\n\nclassification:\n  prod_ssh_key: critical\n  dev_api_key: normal\n</code></pre>"},{"location":"reference/storage/","title":"Storage Reference","text":"<p>File locations and data layout for all APM artifacts.</p>"},{"location":"reference/storage/#vault-file","title":"Vault File","text":"Item Location Default vault <code>./vault.dat</code> (current working directory) Override <code>APM_VAULT_PATH</code> environment variable <p>The vault is a single binary file in V4 format containing all encrypted entries, configuration, cloud tokens, MCP tokens, plugin overrides, vocabulary, and telemetry.</p>"},{"location":"reference/storage/#session-files","title":"Session Files","text":"File Purpose <code>$TEMP/pm_session_global.json</code> Global session (default) <code>$TEMP/pm_session_{SESSION_ID}.json</code> Shell-scoped session <p>Session files contain:</p> <ul> <li>Session ID</li> <li>Unlock/expiry timestamps</li> <li>Inactivity timeout</li> <li>Read-only flag</li> <li>Hashed master password</li> </ul>"},{"location":"reference/storage/#ephemeral-session-store","title":"Ephemeral Session Store","text":"File Purpose <code>$TEMP/.apm_ephemeral_sessions.json</code> All ephemeral sessions <p>Contains an array of ephemeral session objects with IDs, bindings, expiry, and revocation status.</p>"},{"location":"reference/storage/#audit-log","title":"Audit Log","text":"File Purpose <code>~/.config/apm/audit.json</code> Tamper-evident audit log <p>Append-only log of vault interactions with timestamps, actions, users, and hostnames.</p>"},{"location":"reference/storage/#autofill-state","title":"Autofill State","text":"File Purpose <code>$TEMP/.apm_autofill_state.json</code> Daemon PID, port, and token <p>Created when the autofill daemon starts. Contains the PID, loopback address, bearer token, and start time.</p>"},{"location":"reference/storage/#plugin-directory","title":"Plugin Directory","text":"Location Purpose <code>~/.config/apm/plugins/</code> Installed plugin directories <code>~/.config/apm/plugins/&lt;name&gt;/plugin.json</code> Plugin manifest"},{"location":"reference/storage/#cloud-configuration","title":"Cloud Configuration","text":"<p>Cloud provider credentials (OAuth tokens, PATs) are stored inside the encrypted vault \u2014 not in separate files. This ensures they're protected by the same encryption and travel with the vault during sync.</p>"},{"location":"reference/storage/#apmignore","title":".apmignore","text":"Location Purpose Same directory as vault file Primary location Current working directory Fallback location"},{"location":"reference/storage/#policy-files","title":"Policy Files","text":"<p>Policy files are loaded on demand from a user-specified directory. They are not persisted in the vault.</p> <pre><code>pm policy load ./policies/\n</code></pre>"},{"location":"reference/storage/#temporary-files","title":"Temporary Files","text":"Pattern Purpose <code>$TEMP/apm_export_*</code> Export files <code>$TEMP/apm_recovery_*</code> Recovery ceremony data <p>Temporary files are cleaned up after use.</p>"},{"location":"team/","title":"Team Edition","text":"<p>APM Team Edition (<code>pm-team</code>) extends the personal password manager for organizational use with multi-user support, role-based access control, departmental isolation, and approval workflows.</p>"},{"location":"team/#in-this-section","title":"In This Section","text":"<ul> <li>RBAC and Roles \u2014 Role-based access control system</li> <li>Departments \u2014 Isolated encryption domains</li> <li>Approval Workflows \u2014 Gated access to sensitive credentials</li> </ul>"},{"location":"team/#overview","title":"Overview","text":"<p>The Team Edition shares the same cryptographic foundation as the personal edition (Argon2id + AEAD vault encryption + HMAC-SHA256) but adds an organizational layer:</p> Feature Personal (<code>pm</code>) Team (<code>pm-team</code>) Users Single user Multiple users with roles Vault encryption Single master key Per-department keys Access control Session-based RBAC + approval workflows Audit scope Local machine Organization-wide Entry sharing Not applicable Controlled sharing via roles"},{"location":"team/#quick-start","title":"Quick Start","text":"<pre><code># Initialize an organization\npm-team init \"Acme Corp\"\n\n# Create departments\npm-team dept create Engineering\npm-team dept create Finance\n\n# Add users\npm-team user add alice --role admin --dept Engineering\npm-team user add bob --role member --dept Finance\n\n# Add shared entries\npm-team add\n\n# Search entries\npm-team get [query]\n\n# Check organizational health\npm-team health\n</code></pre>"},{"location":"team/approvals/","title":"Approval Workflows","text":"<p>The approval system gates access to sensitive credentials, ensuring that high-value secrets are only accessed with explicit authorization from an authorized approver.</p>"},{"location":"team/approvals/#how-it-works","title":"How It Works","text":"<pre><code>sequenceDiagram\n    participant Member\n    participant APM\n    participant Manager\n\n    Member-&gt;&gt;APM: Request access to sensitive entry\n    APM-&gt;&gt;APM: Check if approval required\n    APM--&gt;&gt;Member: Access pending approval\n    APM-&gt;&gt;Manager: Notification of pending request\n    Manager-&gt;&gt;APM: pm-team approvals approve &lt;id&gt;\n    APM--&gt;&gt;Member: Access granted</code></pre>"},{"location":"team/approvals/#managing-approvals","title":"Managing Approvals","text":""},{"location":"team/approvals/#viewing-pending-requests","title":"Viewing Pending Requests","text":"<pre><code>pm-team approvals list\n</code></pre> <p>Shows all pending approval requests with:</p> <ul> <li>Request ID</li> <li>Requester name and role</li> <li>Entry requested</li> <li>Timestamp</li> <li>Status</li> </ul>"},{"location":"team/approvals/#approving-a-request","title":"Approving a Request","text":"<pre><code>pm-team approvals approve &lt;request_id&gt;\n</code></pre>"},{"location":"team/approvals/#denying-a-request","title":"Denying a Request","text":"<pre><code>pm-team approvals deny &lt;request_id&gt;\n</code></pre>"},{"location":"team/approvals/#who-can-approve","title":"Who Can Approve","text":"Role Can Approve Owner \u2705 Admin \u2705 Manager \u2705 Member \u274c Viewer \u274c"},{"location":"team/approvals/#audit-trail","title":"Audit Trail","text":"<p>All approval actions (requests, approvals, denials) are recorded in the organizational audit log with:</p> <ul> <li>Timestamp</li> <li>Action (<code>APPROVAL_REQUESTED</code>, <code>APPROVAL_GRANTED</code>, <code>APPROVAL_DENIED</code>)</li> <li>Requester</li> <li>Approver</li> <li>Entry details</li> </ul>"},{"location":"team/departments/","title":"Departments","text":"<p>Departments provide isolated encryption domains within the organization. Each department has its own encryption context, ensuring secrets in one department are cryptographically isolated from others.</p>"},{"location":"team/departments/#creating-departments","title":"Creating Departments","text":"<pre><code>pm-team dept create Engineering\npm-team dept create Finance\npm-team dept create Operations\n</code></pre>"},{"location":"team/departments/#managing-departments","title":"Managing Departments","text":"<pre><code># List all departments\npm-team dept list\n\n# View department details\npm-team dept info Engineering\n</code></pre>"},{"location":"team/departments/#isolation-model","title":"Isolation Model","text":"<pre><code>graph TB\n    subgraph Org[\"Organization: Acme Corp\"]\n        subgraph Eng[\"Engineering\"]\n            A[SSH Keys]\n            B[API Tokens]\n            C[Cloud Creds]\n        end\n        subgraph Fin[\"Finance\"]\n            D[Banking Creds]\n            E[Payment APIs]\n        end\n        subgraph Ops[\"Operations\"]\n            F[Server Access]\n            G[Monitoring Keys]\n        end\n    end</code></pre> <p>Each department:</p> <ul> <li>Has its own encryption key derived from the organizational master</li> <li>Has members who are assigned via roles</li> <li>Contains entries visible only to its members</li> <li>Maintains its own audit trail</li> </ul>"},{"location":"team/departments/#cross-department-access","title":"Cross-Department Access","text":"<p>By default, entries in one department are invisible to members of other departments. Cross-department access requires:</p> <ol> <li>An admin or owner to grant it explicitly</li> <li>Or an approval workflow to be triggered</li> </ol> <p>This ensures that even within the same organization, credential isolation is enforced at the cryptographic level.</p>"},{"location":"team/rbac/","title":"RBAC and Roles","text":"<p>The Team Edition uses Role-Based Access Control (RBAC) to manage permissions across the organization.</p>"},{"location":"team/rbac/#role-hierarchy","title":"Role Hierarchy","text":"<pre><code>graph TB\n    A[Owner] --&gt; B[Admin]\n    B --&gt; C[Manager]\n    C --&gt; D[Member]\n    D --&gt; E[Viewer]</code></pre> Role Capabilities Owner Full control \u2014 manage org, departments, users, all entries Admin Manage departments, users, entries within assigned departments Manager Manage entries, approve access requests within department Member Read/write entries within department, request approval for others Viewer Read-only access to department entries"},{"location":"team/rbac/#assigning-roles","title":"Assigning Roles","text":"<pre><code># Add user with a role\npm-team user add alice --role admin --dept Engineering\n\n# Change a user's role\npm-team user role alice manager\n</code></pre>"},{"location":"team/rbac/#permission-matrix","title":"Permission Matrix","text":"Action Owner Admin Manager Member Viewer Create departments \u2705 \u2705 \u274c \u274c \u274c Add users \u2705 \u2705 \u274c \u274c \u274c Assign roles \u2705 \u2705 \u274c \u274c \u274c Add entries \u2705 \u2705 \u2705 \u2705 \u274c Read entries \u2705 \u2705 \u2705 \u2705 \u2705 Edit entries \u2705 \u2705 \u2705 \u2705 \u274c Delete entries \u2705 \u2705 \u2705 \u274c \u274c Approve requests \u2705 \u2705 \u2705 \u274c \u274c View audit logs \u2705 \u2705 \u2705 \u274c \u274c Manage cloud config \u2705 \u2705 \u274c \u274c \u274c"}]}